---
phase: 26-openclaw-sandbox-cr-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workloads/openclaw-sandbox/base/kustomization.yaml
  - workloads/openclaw-sandbox/base/sandbox.yaml
  - workloads/openclaw-sandbox/base/configmap.yaml
  - workloads/openclaw-sandbox/base/httproute.yaml
  - workloads/openclaw-sandbox/base/networkpolicy.yaml
  - workloads/openclaw-sandbox/overlays/dev/kustomization.yaml
  - bootstrap/kind/workload-openclaw-sandbox.yaml
  - bootstrap/kinder/workload-openclaw-sandbox.yaml
autonomous: true
requirements: [MIGR-01, MIGR-02, MIGR-03, MIGR-06, MIGR-07]

must_haves:
  truths:
    - "Sandbox CR manifest exists with correct apiVersion, kind, container spec, probes, and volumeClaimTemplates"
    - "HTTPRoute targets the controller-created headless Service (openclaw-sandbox) in openshell namespace"
    - "NetworkPolicy isolates the OpenClaw sandbox pod without affecting the OpenShell gateway pod"
    - "ArgoCD Application uses openshell project at wave 10 with SSA enabled"
    - "Dev overlay pins the OpenClaw image tag to 2026.3.13-1"
  artifacts:
    - path: "workloads/openclaw-sandbox/base/sandbox.yaml"
      provides: "Sandbox CR with full OpenClaw container spec"
      contains: "kind: Sandbox"
    - path: "workloads/openclaw-sandbox/base/configmap.yaml"
      provides: "OpenClaw seed config and gateway token"
      contains: "name: openclaw-config"
    - path: "workloads/openclaw-sandbox/base/httproute.yaml"
      provides: "Gateway API routing to sandbox pod"
      contains: "name: openclaw-sandbox"
    - path: "workloads/openclaw-sandbox/base/networkpolicy.yaml"
      provides: "Pod-scoped network isolation"
      contains: "openclaw-deny-all"
    - path: "workloads/openclaw-sandbox/base/kustomization.yaml"
      provides: "Kustomize base listing all resources"
      contains: "namespace: openshell"
    - path: "workloads/openclaw-sandbox/overlays/dev/kustomization.yaml"
      provides: "Image tag pinning overlay"
      contains: "2026.3.13-1"
    - path: "bootstrap/kind/workload-openclaw-sandbox.yaml"
      provides: "ArgoCD Application for KIND provider"
      contains: "sync-wave: \"10\""
    - path: "bootstrap/kinder/workload-openclaw-sandbox.yaml"
      provides: "ArgoCD Application for Kinder provider (byte-identical)"
      contains: "sync-wave: \"10\""
  key_links:
    - from: "workloads/openclaw-sandbox/base/httproute.yaml"
      to: "controller-created headless Service"
      via: "backendRefs.name matching Sandbox CR metadata.name"
      pattern: "name: openclaw-sandbox"
    - from: "workloads/openclaw-sandbox/base/networkpolicy.yaml"
      to: "Sandbox pod"
      via: "podSelector matching podTemplate.metadata.labels"
      pattern: "app.kubernetes.io/name: openclaw-gateway"
    - from: "bootstrap/kind/workload-openclaw-sandbox.yaml"
      to: "workloads/openclaw-sandbox/overlays/dev"
      via: "ArgoCD source.path"
      pattern: "path: workloads/openclaw-sandbox/overlays/dev"
---

<objective>
Create the complete OpenClaw Sandbox CR stack -- Kustomize manifests, ArgoCD Application, and dev overlay -- that replaces the old StatefulSet-based deployment.

Purpose: OpenClaw must run as a Sandbox CR so the agent-sandbox controller manages its Pod lifecycle (stable hostname, headless Service, PVC), enabling future supervisor binary injection (Phase 27).

Output: New `workloads/openclaw-sandbox/` directory with Sandbox CR, ConfigMap, HTTPRoute, NetworkPolicy, and corresponding ArgoCD Application in both bootstrap directories.
</objective>

<execution_context>
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-openclaw-sandbox-cr-migration/26-RESEARCH.md

@workloads/openclaw/base/statefulset.yaml
@workloads/openclaw/base/configmap.yaml
@workloads/openclaw/base/httproute.yaml
@workloads/openclaw/base/networkpolicy.yaml
@workloads/openclaw/base/kustomization.yaml
@workloads/openclaw/overlays/dev/kustomization.yaml
@bootstrap/kind/workload-openclaw.yaml

<interfaces>
<!-- The agent-sandbox controller creates resources from the Sandbox CR: -->
<!-- Pod name = Sandbox CR name (openclaw-sandbox) -->
<!-- Headless Service name = Sandbox CR name (openclaw-sandbox) -->
<!-- PVC name = {vct-name}-{sandbox-name} = data-openclaw-sandbox -->
<!-- Stable FQDN = openclaw-sandbox.openshell.svc.cluster.local -->

From bootstrap/kind/projects/openshell-project.yaml:
- AppProject `openshell` allows namespaces: openshell, agent-sandbox-system
- clusterResourceWhitelist: Namespace, CRD, ClusterRole, ClusterRoleBinding
- namespaceResourceBlacklist: [] (all namespaced resources allowed, including Sandbox CR)

From 26-RESEARCH.md Pattern 1 (Sandbox CR):
- apiVersion: agents.x-k8s.io/v1alpha1
- kind: Sandbox
- spec.podTemplate mirrors StatefulSet template spec
- spec.volumeClaimTemplates mirrors StatefulSet VCTs
- Pod labels we control: app.kubernetes.io/name: openclaw-gateway, sandbox: openclaw-sandbox

From 26-RESEARCH.md Pattern 5 (ArgoCD Application):
- project: openshell (NOT workloads -- Sandbox CR is custom resource in openshell namespace)
- SSA=true (controller updates status fields)
- CreateNamespace=false (namespace already exists from Phase 23)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Sandbox CR Kustomize base and dev overlay</name>
  <files>
    workloads/openclaw-sandbox/base/kustomization.yaml,
    workloads/openclaw-sandbox/base/sandbox.yaml,
    workloads/openclaw-sandbox/base/configmap.yaml,
    workloads/openclaw-sandbox/base/httproute.yaml,
    workloads/openclaw-sandbox/base/networkpolicy.yaml,
    workloads/openclaw-sandbox/overlays/dev/kustomization.yaml
  </files>
  <action>
    Create the directory structure `workloads/openclaw-sandbox/base/` and `workloads/openclaw-sandbox/overlays/dev/`.

    **sandbox.yaml** (MIGR-01, MIGR-02): Create a Sandbox CR adapted from the current `workloads/openclaw/base/statefulset.yaml`. Use the exact Sandbox CR YAML from 26-RESEARCH.md "Code Examples > Sandbox CR (OpenClaw)" section -- it has been pre-validated against the current StatefulSet. Key fields:
    - apiVersion: agents.x-k8s.io/v1alpha1, kind: Sandbox
    - metadata.name: openclaw-sandbox, metadata.namespace: openshell
    - spec.podTemplate.metadata.labels: `app.kubernetes.io/name: openclaw-gateway` AND `sandbox: openclaw-sandbox`
    - spec.podTemplate.spec: same initContainers, containers, volumes, securityContext as current StatefulSet
    - spec.volumeClaimTemplates: same as current StatefulSet (name: data, 20Gi, ReadWriteOnce)
    - Include header comment explaining this is a Sandbox CR reconciled by agent-sandbox controller

    **configmap.yaml** (MIGR-01): Copy from current `workloads/openclaw/base/configmap.yaml`. Change `namespace: openclaw` to `namespace: openshell`. Keep all data fields identical (OPENCLAW_GATEWAY_TOKEN and openclaw.json).

    **httproute.yaml** (MIGR-03, MIGR-06): Create an HTTPRoute targeting the controller-created headless Service. Use the exact YAML from 26-RESEARCH.md "Pattern 3: HTTPRoute Targeting Controller-Created Service". Key differences from current:
    - metadata.namespace: openshell (was openclaw)
    - spec.rules[0].backendRefs[0].name: openclaw-sandbox (was openclaw-gateway)
    - parentRefs: same (eg in envoy-gateway-system)
    - port: 18789 (same)
    - Include header comment noting the Service is created by the agent-sandbox controller, not manually

    **networkpolicy.yaml** (MIGR-07): Create two NetworkPolicy resources from 26-RESEARCH.md "NetworkPolicy for Sandbox Pod in openshell Namespace". Key differences from current openclaw networkpolicy.yaml:
    - namespace: openshell (was openclaw)
    - deny-all: podSelector targets `app.kubernetes.io/name: openclaw-gateway` ONLY (was `{}` matching all pods) -- this is critical because the openshell namespace also contains the OpenShell gateway pod which must NOT be denied
    - allow: same ingress (envoy-gateway-system on 18789), same egress (DNS, LiteLLM proxy in nemoclaw, HTTPS 443)
    - Rename deny policy from `default-deny-all` to `openclaw-deny-all` to clarify it scopes to the OpenClaw pod only
    - Include header comment explaining the pod-scoped targeting

    **kustomization.yaml**: Standard Kustomize base listing all 4 resources. Include `namespace: openshell` transformer -- all resources here are namespace-scoped (Sandbox, ConfigMap, HTTPRoute, NetworkPolicy), so the transformer is safe. Reference: 26-RESEARCH.md "Kustomization for Sandbox CR Base".

    **overlays/dev/kustomization.yaml**: Standard overlay pinning OpenClaw image to 2026.3.13-1 (current tag from existing overlay). Reference: 26-RESEARCH.md "Dev Overlay (image tag pinning)".
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/pincer-ops && kubectl kustomize workloads/openclaw-sandbox/overlays/dev | grep -c 'kind:' && kubectl kustomize workloads/openclaw-sandbox/overlays/dev | grep '2026.3.13-1' && kubectl kustomize workloads/openclaw-sandbox/overlays/dev | grep 'namespace: openshell'</automated>
  </verify>
  <done>Kustomize build of `workloads/openclaw-sandbox/overlays/dev` succeeds. Output contains Sandbox CR (kind: Sandbox), ConfigMap, HTTPRoute, and 2 NetworkPolicy resources, all with namespace: openshell. Image tag is 2026.3.13-1.</done>
</task>

<task type="auto">
  <name>Task 2: Create ArgoCD Application for both providers</name>
  <files>
    bootstrap/kind/workload-openclaw-sandbox.yaml,
    bootstrap/kinder/workload-openclaw-sandbox.yaml
  </files>
  <action>
    Create `bootstrap/kind/workload-openclaw-sandbox.yaml` using the exact YAML from 26-RESEARCH.md "ArgoCD Application (workload-openclaw-sandbox)". Key fields:
    - metadata.name: workload-openclaw-sandbox
    - annotations: sync-wave "10", manifest-generate-paths: workloads/openclaw-sandbox, notification subscriptions (on-sync-failed, on-health-degraded, on-sync-status-unknown)
    - finalizers: resources-finalizer.argocd.argoproj.io
    - spec.project: openshell (NOT workloads)
    - spec.source.path: workloads/openclaw-sandbox/overlays/dev
    - spec.destination.namespace: openshell
    - syncPolicy: automated (selfHeal + prune), ServerSideApply=true, CreateNamespace=false
    - Include header comment with sync wave map (from research)

    Copy the file byte-identically to `bootstrap/kinder/workload-openclaw-sandbox.yaml` (provider parity convention).
  </action>
  <verify>
    <automated>diff /Users/patrykattc/work/git/pincer-ops/bootstrap/kind/workload-openclaw-sandbox.yaml /Users/patrykattc/work/git/pincer-ops/bootstrap/kinder/workload-openclaw-sandbox.yaml && grep 'sync-wave: "10"' /Users/patrykattc/work/git/pincer-ops/bootstrap/kind/workload-openclaw-sandbox.yaml && grep 'project: openshell' /Users/patrykattc/work/git/pincer-ops/bootstrap/kind/workload-openclaw-sandbox.yaml && grep 'ServerSideApply=true' /Users/patrykattc/work/git/pincer-ops/bootstrap/kind/workload-openclaw-sandbox.yaml</automated>
  </verify>
  <done>ArgoCD Application files exist in both providers, are byte-identical, use openshell project, wave 10, SSA enabled, and target workloads/openclaw-sandbox/overlays/dev.</done>
</task>

</tasks>

<verification>
1. `kubectl kustomize workloads/openclaw-sandbox/overlays/dev` -- builds without errors, produces 5 resources (Sandbox, ConfigMap, HTTPRoute, 2x NetworkPolicy)
2. `diff bootstrap/kind/workload-openclaw-sandbox.yaml bootstrap/kinder/workload-openclaw-sandbox.yaml` -- byte-identical
3. All resources have namespace: openshell
4. HTTPRoute backendRefs.name is "openclaw-sandbox" (matches Sandbox CR name)
5. NetworkPolicy deny targets `app.kubernetes.io/name: openclaw-gateway` (pod-scoped, not namespace-wide)
</verification>

<success_criteria>
- Sandbox CR manifest faithful to current StatefulSet (same probes, resources, env vars, volumes, securityContext)
- HTTPRoute targets controller-created Service name matching Sandbox CR name
- NetworkPolicy scoped to OpenClaw pod only via label selector
- ArgoCD Application at wave 10 with SSA, openshell project, both providers
- Dev overlay pins image to 2026.3.13-1
- `kubectl kustomize` builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/26-openclaw-sandbox-cr-migration/26-01-SUMMARY.md`
</output>
