---
phase: 26-openclaw-sandbox-cr-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bootstrap/kind/workload-openclaw.yaml
  - bootstrap/kinder/workload-openclaw.yaml
  - bootstrap/kind/projects/workloads.yaml
  - bootstrap/kinder/projects/workloads.yaml
  - workloads/openclaw/base/statefulset.yaml
  - workloads/openclaw/base/service.yaml
  - workloads/openclaw/base/configmap.yaml
  - workloads/openclaw/base/httproute.yaml
  - workloads/openclaw/base/networkpolicy.yaml
  - workloads/openclaw/base/backup-rbac.yaml
  - workloads/openclaw/base/backup-cronjob.yaml
  - workloads/openclaw/base/kustomization.yaml
  - workloads/openclaw/overlays/dev/kustomization.yaml
  - scripts/bootstrap.sh
  - scripts/validate-manifests.sh
  - Makefile
autonomous: true
requirements: [MIGR-04, MIGR-05]

must_haves:
  truths:
    - "workload-openclaw.yaml does not exist in either bootstrap directory"
    - "workloads/openclaw/ directory tree is fully deleted"
    - "workloads AppProject no longer lists openclaw namespace as a destination"
    - "bootstrap.sh Step 16 waits for Sandbox CR readiness instead of StatefulSet rollout"
    - "validate-manifests.sh validates openclaw-sandbox overlay instead of openclaw overlay"
    - "Makefile targets reference openshell namespace and sandbox pod"
  artifacts:
    - path: "bootstrap/kind/projects/workloads.yaml"
      provides: "Updated AppProject without openclaw destination"
      contains: "nemoclaw"
    - path: "scripts/bootstrap.sh"
      provides: "Updated Step 16 for Sandbox CR wait"
      contains: "openclaw-sandbox"
    - path: "scripts/validate-manifests.sh"
      provides: "Updated kustomize validation path"
      contains: "openclaw-sandbox"
    - path: "Makefile"
      provides: "Updated operational targets"
      contains: "openshell"
  key_links:
    - from: "scripts/bootstrap.sh"
      to: "Sandbox CR in openshell namespace"
      via: "kubectl wait sandbox/openclaw-sandbox"
      pattern: "sandbox/openclaw-sandbox.*openshell"
    - from: "scripts/validate-manifests.sh"
      to: "workloads/openclaw-sandbox/overlays/dev"
      via: "validate_kustomize function call"
      pattern: "openclaw-sandbox/overlays/dev"
---

<objective>
Remove the old openclaw StatefulSet stack -- delete ArgoCD Application from bootstrap, remove the workloads/openclaw/ directory tree, clean up AppProject, and update all operational scripts to target the new Sandbox CR in the openshell namespace.

Purpose: The old `workload-openclaw` Application and `openclaw` namespace are replaced by the Sandbox CR stack created in Plan 01. Scripts and Makefile must reference the new resource names and namespace.

Output: Clean removal of old resources plus updated bootstrap.sh, validate-manifests.sh, and Makefile.
</objective>

<execution_context>
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-openclaw-sandbox-cr-migration/26-RESEARCH.md

@bootstrap/kind/workload-openclaw.yaml
@bootstrap/kinder/workload-openclaw.yaml
@bootstrap/kind/projects/workloads.yaml
@scripts/bootstrap.sh
@scripts/validate-manifests.sh
@Makefile

<interfaces>
<!-- After Plan 01, the new resources exist at: -->
<!-- workloads/openclaw-sandbox/base/ (Sandbox CR, ConfigMap, HTTPRoute, NetworkPolicy) -->
<!-- bootstrap/{kind,kinder}/workload-openclaw-sandbox.yaml (ArgoCD Application) -->

<!-- Controller creates: -->
<!-- Pod: openclaw-sandbox in openshell namespace -->
<!-- Service: openclaw-sandbox (headless) in openshell namespace -->
<!-- PVC: data-openclaw-sandbox in openshell namespace -->

<!-- bootstrap.sh Step 16 currently waits for: -->
<!-- statefulset/openclaw-gateway -n openclaw -->
<!-- Must change to: sandbox/openclaw-sandbox -n openshell -->

<!-- validate-manifests.sh currently validates: -->
<!-- workloads/openclaw/overlays/dev -->
<!-- Must change to: workloads/openclaw-sandbox/overlays/dev -->

<!-- Makefile currently uses: -->
<!-- OPENCLAW_POD := openclaw-gateway-0 -->
<!-- OPENCLAW_NS  := openclaw -->
<!-- kubectl logs -n openclaw statefulset/openclaw-gateway -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove old openclaw resources and update AppProject</name>
  <files>
    bootstrap/kind/workload-openclaw.yaml,
    bootstrap/kinder/workload-openclaw.yaml,
    bootstrap/kind/projects/workloads.yaml,
    bootstrap/kinder/projects/workloads.yaml,
    workloads/openclaw/
  </files>
  <action>
    **Delete old ArgoCD Application** (MIGR-04):
    - Delete `bootstrap/kind/workload-openclaw.yaml`
    - Delete `bootstrap/kinder/workload-openclaw.yaml`

    **Delete old workload directory** (MIGR-05):
    - Delete the entire `workloads/openclaw/` directory tree (base/ and overlays/)
    - This removes: statefulset.yaml, service.yaml, configmap.yaml, httproute.yaml, networkpolicy.yaml, backup-rbac.yaml, backup-cronjob.yaml, kustomization.yaml, overlays/dev/kustomization.yaml

    **Update workloads AppProject** (MIGR-05):
    - Edit `bootstrap/kind/projects/workloads.yaml`:
      - Remove the `openclaw` namespace destination entry (lines with `namespace: 'openclaw'`)
      - Update the description from "openclaw, nemoclaw" to "nemoclaw" to reflect the remaining namespace
      - Keep the `nemoclaw` destination (needed by workload-litellm until Phase 28)
    - Copy the updated file byte-identically to `bootstrap/kinder/projects/workloads.yaml` (provider parity)
  </action>
  <verify>
    <automated>test ! -f /Users/patrykattc/work/git/pincer-ops/bootstrap/kind/workload-openclaw.yaml && test ! -f /Users/patrykattc/work/git/pincer-ops/bootstrap/kinder/workload-openclaw.yaml && test ! -d /Users/patrykattc/work/git/pincer-ops/workloads/openclaw && grep -v openclaw /Users/patrykattc/work/git/pincer-ops/bootstrap/kind/projects/workloads.yaml | grep nemoclaw && diff /Users/patrykattc/work/git/pincer-ops/bootstrap/kind/projects/workloads.yaml /Users/patrykattc/work/git/pincer-ops/bootstrap/kinder/projects/workloads.yaml</automated>
  </verify>
  <done>Old workload-openclaw.yaml deleted from both providers. workloads/openclaw/ directory tree deleted. workloads AppProject only has nemoclaw destination. Both provider copies byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Update bootstrap.sh, validate-manifests.sh, and Makefile</name>
  <files>
    scripts/bootstrap.sh,
    scripts/validate-manifests.sh,
    Makefile
  </files>
  <action>
    **bootstrap.sh Step 16** (MIGR-05):
    - Find the "Step 16: Deploy OpenClaw" section (around line 454)
    - Change `kubectl apply -f "${BOOTSTRAP_DIR}/workload-openclaw.yaml"` to `kubectl apply -f "${BOOTSTRAP_DIR}/workload-openclaw-sandbox.yaml"`
    - Replace the StatefulSet polling loop (`kubectl get statefulset openclaw-gateway -n openclaw`) with a Sandbox CR polling loop: `kubectl get sandbox openclaw-sandbox -n openshell`
    - Replace `kubectl rollout status statefulset/openclaw-gateway -n openclaw --timeout=300s` with `kubectl wait --for=condition=Ready sandbox/openclaw-sandbox -n openshell --timeout=300s`
    - Update the log message: "OpenClaw sandbox is running" (was "OpenClaw gateway is running")
    - Update the final summary message: `"OpenClaw:   openclaw-sandbox in openshell namespace (port 18789)"` (was "openclaw-gateway in openclaw namespace")
    - Also update the kustomize fallback path in the ComparisonError handler (if it exists): change `workloads/openclaw/overlays/dev` to `workloads/openclaw-sandbox/overlays/dev`

    **validate-manifests.sh** (MIGR-05):
    - Find line ~90: `validate_kustomize "workloads/openclaw/overlays/dev" "openclaw/dev"`
    - Change to: `validate_kustomize "workloads/openclaw-sandbox/overlays/dev" "openclaw-sandbox/dev"`

    **Makefile** (MIGR-05, operational updates):
    - Update `logs` target: change `kubectl logs -n openclaw statefulset/openclaw-gateway` to `kubectl logs -n openshell -l app.kubernetes.io/name=openclaw-gateway -f --tail=50` (uses label selector since Sandbox pods are bare pods, not StatefulSets)
    - Update `doctor` target: change `kubectl get statefulset openclaw-gateway -n openclaw` to `kubectl get sandbox openclaw-sandbox -n openshell` for health check
    - Update `OPENCLAW_POD` variable: change from `openclaw-gateway-0` to `openclaw-sandbox`
    - Update `OPENCLAW_NS` variable: change from `openclaw` to `openshell`
    - Update `openclaw-health` target: keep `curl -sf http://localhost/health` (unchanged -- routing goes through Envoy regardless of backend)
    - Do NOT update `make openclaw-onboard` semantics -- it will use the updated OPENCLAW_POD and OPENCLAW_NS variables automatically
  </action>
  <verify>
    <automated>grep 'openclaw-sandbox' /Users/patrykattc/work/git/pincer-ops/scripts/bootstrap.sh && grep 'openclaw-sandbox/overlays/dev' /Users/patrykattc/work/git/pincer-ops/scripts/validate-manifests.sh && grep 'openshell' /Users/patrykattc/work/git/pincer-ops/Makefile | grep -c 'OPENCLAW_NS\|logs'</automated>
  </verify>
  <done>bootstrap.sh Step 16 deploys workload-openclaw-sandbox.yaml and waits for Sandbox CR Ready condition. validate-manifests.sh validates the new openclaw-sandbox overlay path. Makefile targets use openshell namespace and openclaw-sandbox pod name.</done>
</task>

</tasks>

<verification>
1. `test ! -f bootstrap/kind/workload-openclaw.yaml` -- old Application gone
2. `test ! -d workloads/openclaw` -- old directory tree gone
3. `grep openclaw bootstrap/kind/projects/workloads.yaml` returns no matches -- AppProject cleaned
4. `grep openclaw-sandbox scripts/bootstrap.sh` -- Step 16 updated
5. `grep openclaw-sandbox scripts/validate-manifests.sh` -- validation path updated
6. `grep 'OPENCLAW_NS.*openshell' Makefile` -- Makefile targets updated
</verification>

<success_criteria>
- No references to old `workload-openclaw` Application in bootstrap directories
- No `workloads/openclaw/` directory exists
- workloads AppProject only allows nemoclaw namespace
- bootstrap.sh deploys new Application and waits for Sandbox CR readiness
- validate-manifests.sh validates new overlay path
- Makefile operational targets work with new pod name and namespace
</success_criteria>

<output>
After completion, create `.planning/phases/26-openclaw-sandbox-cr-migration/26-02-SUMMARY.md`
</output>
