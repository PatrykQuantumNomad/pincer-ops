---
phase: 26-openclaw-sandbox-cr-migration
plan: 03
type: execute
wave: 2
depends_on: [26-01, 26-02]
files_modified:
  - tests/unit/openshell-manifests.bats
  - tests/unit/bootstrap.bats
autonomous: true
requirements: [MIGR-01, MIGR-02, MIGR-03, MIGR-04, MIGR-05, MIGR-06, MIGR-07]

must_haves:
  truths:
    - "BATS tests verify Sandbox CR exists with correct apiVersion, kind, image, ports, probes, and volumeClaimTemplates"
    - "BATS tests verify HTTPRoute targets openclaw-sandbox service in openshell namespace"
    - "BATS tests verify NetworkPolicy targets openclaw-gateway pods (not namespace-wide)"
    - "BATS tests verify workload-openclaw.yaml is absent and workload-openclaw-sandbox.yaml is present"
    - "BATS tests verify workloads/openclaw/ directory does not exist"
    - "bootstrap.bats expected_files arrays updated with workload-openclaw-sandbox.yaml"
    - "All BATS tests pass (make test)"
  artifacts:
    - path: "tests/unit/openshell-manifests.bats"
      provides: "Structural tests for Sandbox CR migration (MIGR-01 through MIGR-07)"
      min_lines: 520
    - path: "tests/unit/bootstrap.bats"
      provides: "Updated expected_files arrays with workload-openclaw-sandbox.yaml"
      contains: "workload-openclaw-sandbox.yaml"
  key_links:
    - from: "tests/unit/openshell-manifests.bats"
      to: "workloads/openclaw-sandbox/base/sandbox.yaml"
      via: "grep assertions on static YAML"
      pattern: "openclaw-sandbox/base/sandbox.yaml"
    - from: "tests/unit/bootstrap.bats"
      to: "bootstrap/kind/workload-openclaw-sandbox.yaml"
      via: "expected_files array and file existence assertions"
      pattern: "workload-openclaw-sandbox.yaml"
---

<objective>
Add BATS structural tests for all Phase 26 requirements and update bootstrap.bats expected file lists. Validates that the Sandbox CR stack is correctly structured and the old openclaw resources are removed.

Purpose: Structural tests catch manifest regressions without a running cluster. Every MIGR requirement must have at least one test.

Output: Extended openshell-manifests.bats with MIGR-01 through MIGR-07 tests, updated bootstrap.bats with new expected_files.
</objective>

<execution_context>
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-openclaw-sandbox-cr-migration/26-RESEARCH.md

@tests/unit/openshell-manifests.bats
@tests/unit/bootstrap.bats
@tests/test_helper.bash

<interfaces>
<!-- After Plans 01 and 02, the following state exists: -->

New files created (Plan 01):
- workloads/openclaw-sandbox/base/sandbox.yaml (apiVersion: agents.x-k8s.io/v1alpha1, kind: Sandbox)
- workloads/openclaw-sandbox/base/configmap.yaml (name: openclaw-config, namespace: openshell)
- workloads/openclaw-sandbox/base/httproute.yaml (backendRefs.name: openclaw-sandbox, port: 18789)
- workloads/openclaw-sandbox/base/networkpolicy.yaml (openclaw-deny-all + openclaw-allow targeting app.kubernetes.io/name: openclaw-gateway)
- workloads/openclaw-sandbox/base/kustomization.yaml
- workloads/openclaw-sandbox/overlays/dev/kustomization.yaml
- bootstrap/kind/workload-openclaw-sandbox.yaml (sync-wave: "10", project: openshell, SSA=true)
- bootstrap/kinder/workload-openclaw-sandbox.yaml (byte-identical)

Files deleted (Plan 02):
- bootstrap/kind/workload-openclaw.yaml (REMOVED)
- bootstrap/kinder/workload-openclaw.yaml (REMOVED)
- workloads/openclaw/ (entire directory tree REMOVED)

Files modified (Plan 02):
- bootstrap/{kind,kinder}/projects/workloads.yaml (openclaw destination removed)

<!-- BATS test patterns: -->
<!-- grep-based assertions on static YAML files -->
<!-- $PROJECT_ROOT variable from test_helper.bash -->
<!-- @test "description" { run grep ...; assert_success } -->
<!-- bootstrap.bats: expected_files array and file count assertion -->

<!-- bootstrap.bats current file counts: 16 kind, 13 kinder -->
<!-- After migration: still 16 kind / 13 kinder (1:1 replacement) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Sandbox CR migration tests to openshell-manifests.bats</name>
  <files>tests/unit/openshell-manifests.bats</files>
  <action>
    Append a new section to `tests/unit/openshell-manifests.bats` after the existing "Provider Parity (Phase 25 additions)" section. Add tests for each MIGR requirement:

    **MIGR-01 (Sandbox CR structure):**
    - "sandbox CR has correct apiVersion" -- grep `agents.x-k8s.io/v1alpha1` in sandbox.yaml
    - "sandbox CR has kind Sandbox" -- grep `kind: Sandbox` in sandbox.yaml
    - "sandbox CR has name openclaw-sandbox" -- grep `name: openclaw-sandbox` in sandbox.yaml
    - "sandbox CR has namespace openshell" -- grep `namespace: openshell` in sandbox.yaml
    - "sandbox CR has openclaw-gateway container image" -- grep `ghcr.io/openclaw/openclaw` in sandbox.yaml
    - "sandbox CR has gateway port 18789" -- grep `containerPort: 18789` in sandbox.yaml
    - "sandbox CR has startupProbe" -- grep `startupProbe:` in sandbox.yaml
    - "sandbox CR has livenessProbe" -- grep `livenessProbe:` in sandbox.yaml
    - "sandbox CR has readinessProbe" -- grep `readinessProbe:` in sandbox.yaml
    - "sandbox CR has resource requests" -- grep `requests:` in sandbox.yaml
    - "sandbox CR has resource limits" -- grep `limits:` in sandbox.yaml
    - "sandbox CR has securityContext runAsNonRoot" -- grep `runAsNonRoot: true` in sandbox.yaml
    - "sandbox CR has readOnlyRootFilesystem" -- grep `readOnlyRootFilesystem: true` in sandbox.yaml
    - "sandbox CR has bind lan flag" -- grep `\-\-bind.*lan` in sandbox.yaml

    **MIGR-02 (volumeClaimTemplates):**
    - "sandbox CR has volumeClaimTemplates" -- grep `volumeClaimTemplates:` in sandbox.yaml
    - "sandbox CR has 20Gi storage" -- grep `storage: 20Gi` in sandbox.yaml
    - "sandbox CR has ReadWriteOnce access mode" -- grep `ReadWriteOnce` in sandbox.yaml

    **MIGR-03 (HTTPRoute):**
    - "sandbox HTTPRoute targets openclaw-sandbox service" -- grep `name: openclaw-sandbox` in httproute.yaml
    - "sandbox HTTPRoute has port 18789" -- grep `port: 18789` in httproute.yaml
    - "sandbox HTTPRoute has namespace openshell" -- grep `namespace: openshell` in httproute.yaml

    **MIGR-06 (Envoy Gateway routing):**
    - "sandbox HTTPRoute references eg gateway" -- grep `name: eg` in httproute.yaml
    - "sandbox HTTPRoute references envoy-gateway-system" -- grep `envoy-gateway-system` in httproute.yaml

    **MIGR-07 (NetworkPolicy):**
    - "sandbox NetworkPolicy has openclaw-deny-all" -- grep `name: openclaw-deny-all` in networkpolicy.yaml
    - "sandbox NetworkPolicy has openclaw-allow" -- grep `name: openclaw-allow` in networkpolicy.yaml
    - "sandbox deny-all targets openclaw-gateway pods" -- grep `app.kubernetes.io/name: openclaw-gateway` in networkpolicy.yaml (verify pod-scoped, not namespace-wide)
    - "sandbox NetworkPolicy allows envoy-gateway ingress" -- grep `envoy-gateway-system` in networkpolicy.yaml
    - "sandbox NetworkPolicy allows DNS egress" -- grep `port: 53` in networkpolicy.yaml
    - "sandbox NetworkPolicy allows HTTPS egress" -- grep `port: 443` in networkpolicy.yaml
    - "sandbox NetworkPolicy allows LiteLLM egress" -- grep `port: 4000` in networkpolicy.yaml

    **ConfigMap:**
    - "sandbox ConfigMap has name openclaw-config" -- grep `name: openclaw-config` in configmap.yaml
    - "sandbox ConfigMap has namespace openshell" -- grep `namespace: openshell` in configmap.yaml

    **ArgoCD Application:**
    - "workload-openclaw-sandbox has sync-wave 10" -- grep `sync-wave: "10"` in workload-openclaw-sandbox.yaml
    - "workload-openclaw-sandbox uses project openshell" -- grep `project: openshell` in workload-openclaw-sandbox.yaml
    - "workload-openclaw-sandbox has ServerSideApply" -- grep `ServerSideApply=true` in workload-openclaw-sandbox.yaml
    - "workload-openclaw-sandbox has CreateNamespace false" -- grep `CreateNamespace=false` in workload-openclaw-sandbox.yaml
    - "workload-openclaw-sandbox source path is overlays/dev" -- grep `path: workloads/openclaw-sandbox/overlays/dev` in workload-openclaw-sandbox.yaml

    **Provider parity:**
    - "workload-openclaw-sandbox.yaml byte-identical across providers" -- diff kind vs kinder

    **Removal verification:**
    - "workloads/openclaw directory does not exist" -- test ! -d "$PROJECT_ROOT/workloads/openclaw"; assert success
    - "workload-openclaw.yaml removed from kind" -- test ! -f "$PROJECT_ROOT/bootstrap/kind/workload-openclaw.yaml"; assert success
    - "workload-openclaw.yaml removed from kinder" -- test ! -f "$PROJECT_ROOT/bootstrap/kinder/workload-openclaw.yaml"; assert success

    All test paths use `${PROJECT_ROOT}/workloads/openclaw-sandbox/base/` for new manifests and `${PROJECT_ROOT}/bootstrap/kind/` for ArgoCD Applications. Use section header comments to group by requirement (same pattern as existing SAND-* sections).
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/pincer-ops && bats tests/unit/openshell-manifests.bats</automated>
  </verify>
  <done>All openshell-manifests.bats tests pass, covering MIGR-01 through MIGR-07 with at least one test per requirement.</done>
</task>

<task type="auto">
  <name>Task 2: Update bootstrap.bats expected file lists</name>
  <files>tests/unit/bootstrap.bats</files>
  <action>
    Update `tests/unit/bootstrap.bats` expected_files arrays:

    **KIND expected_files array** (around line 128-146):
    - Replace `workload-openclaw.yaml` with `workload-openclaw-sandbox.yaml`
    - Keep all other entries unchanged
    - File count stays at 16 (1:1 replacement)

    **Kinder expected_files array** (around line 172-188):
    - Replace `workload-openclaw.yaml` with `workload-openclaw-sandbox.yaml`
    - Keep all other entries unchanged
    - File count stays at 13 (1:1 replacement)

    **Shared file identity test** (around line 210-214):
    - If there is an entry checking `workload-openclaw.yaml` byte-identity, replace it with `workload-openclaw-sandbox.yaml`
    - The old shared file list may contain `workload-openclaw.yaml` -- update to `workload-openclaw-sandbox.yaml`

    Do NOT change any file count assertions -- they remain 16 kind / 13 kinder because this is a 1:1 file replacement.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/pincer-ops && bats tests/unit/bootstrap.bats</automated>
  </verify>
  <done>bootstrap.bats passes with updated expected_files arrays containing workload-openclaw-sandbox.yaml instead of workload-openclaw.yaml. File counts unchanged (16 kind, 13 kinder).</done>
</task>

</tasks>

<verification>
1. `bats tests/unit/openshell-manifests.bats` -- all tests pass (existing + new MIGR-* tests)
2. `bats tests/unit/bootstrap.bats` -- all tests pass with updated file lists
3. `make test` -- full test suite passes (no regressions)
</verification>

<success_criteria>
- Every MIGR requirement (MIGR-01 through MIGR-07) has at least one BATS test
- Removal tests verify old workload-openclaw.yaml and workloads/openclaw/ are gone
- bootstrap.bats expected_files arrays match actual files in bootstrap directories
- `make test` passes with no regressions across the full 116+ test suite
</success_criteria>

<output>
After completion, create `.planning/phases/26-openclaw-sandbox-cr-migration/26-03-SUMMARY.md`
</output>
