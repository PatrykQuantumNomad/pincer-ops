---
phase: 10-mcp-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - scripts/bootstrap.sh
autonomous: false
requirements:
  - MCP-01
  - MCP-02
  - MCP-03

must_haves:
  truths:
    - "bootstrap.sh applies argocd-rbac-cm.yaml alongside argocd-cm.yaml"
    - "Claude Code can query pod status via the kubernetes MCP server"
    - "Claude Code can view ArgoCD application sync status via the argocd MCP server"
    - "Both MCP servers reject write/destructive operations in default configuration"
  artifacts:
    - path: "scripts/bootstrap.sh"
      provides: "Bootstrap with RBAC ConfigMap application"
      contains: "argocd-rbac-cm"
  key_links:
    - from: "scripts/bootstrap.sh"
      to: "bootstrap/argocd-rbac-cm.yaml"
      via: "kubectl apply in bootstrap step 7"
      pattern: "argocd-rbac-cm"
---

<objective>
Wire the ArgoCD RBAC ConfigMap into bootstrap.sh, then verify the complete MCP integration end-to-end: run setup-mcp.sh to generate tokens, confirm both MCP servers connect and respond to queries in Claude Code.

Purpose: Proves the MCP integration actually works against a live cluster. Configuration without verification is not a feature.
Output: Updated bootstrap.sh, verified MCP server connectivity
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-mcp-integration/10-RESEARCH.md
@.planning/phases/10-mcp-integration/10-01-SUMMARY.md
@scripts/bootstrap.sh
@bootstrap/argocd-rbac-cm.yaml
@.mcp.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RBAC ConfigMap to bootstrap.sh</name>
  <files>scripts/bootstrap.sh</files>
  <action>
    In bootstrap.sh, find the step where argocd-cm.yaml is applied (step 7, line ~116: `run_cmd kubectl apply -n argocd -f "${BOOTSTRAP_DIR}/argocd-cm.yaml"`).

    Add a line immediately after to also apply the RBAC ConfigMap:
    ```bash
    run_cmd kubectl apply -n argocd -f "${BOOTSTRAP_DIR}/argocd-rbac-cm.yaml"
    ```

    This ensures the RBAC policy is present before ArgoCD starts managing applications. The argocd-self Application will also keep it in sync from Git going forward, but the initial apply during bootstrap ensures it is available from the first boot.

    Do NOT modify any other part of bootstrap.sh.
  </action>
  <verify>
    Run: `grep 'argocd-rbac-cm' scripts/bootstrap.sh` -- must match the new apply line.
    Run: `bash -n scripts/bootstrap.sh` -- must pass syntax check.
  </verify>
  <done>
    bootstrap.sh applies argocd-rbac-cm.yaml in the ArgoCD configuration step, ensuring RBAC is present from first boot.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify MCP integration end-to-end</name>
  <files>n/a (verification only)</files>
  <action>
    This is a human verification checkpoint. Claude should apply the updated ArgoCD configs to the running cluster if available:
    ```bash
    kubectl apply -n argocd -f bootstrap/argocd-cm.yaml
    kubectl apply -n argocd -f bootstrap/argocd-rbac-cm.yaml
    ```
    Then present the verification steps below to the user and pause for confirmation.

    Verification steps for the user:

    1. Run the MCP setup script:
       ```bash
       ./scripts/setup-mcp.sh
       ```
       Expected: Script generates an ArgoCD API token and prints export instructions.

    2. Export the token as instructed by the script:
       ```bash
       export ARGOCD_API_TOKEN="<token from script output>"
       export ARGOCD_BASE_URL="https://localhost:8080"
       ```

    3. Start the ArgoCD port-forward (if not already running):
       ```bash
       kubectl port-forward svc/argocd-server -n argocd 8080:443 &
       ```

    4. Restart Claude Code (or open a new session) so it picks up the .mcp.json config.

    5. In Claude Code, check MCP server status via `/mcp`.
       Expected: Both "kubernetes" and "argocd" servers show as connected.

    6. Test kubernetes MCP -- ask Claude Code: "List all pods in the argocd namespace"
       Expected: Claude uses the kubernetes MCP tool and returns pod names/statuses.

    7. Test argocd MCP -- ask Claude Code: "Show me the sync status of all ArgoCD applications"
       Expected: Claude uses the argocd MCP tool and returns application list with sync/health status.

    8. Verify read-only default -- ask Claude Code: "Delete the workload-openclaw ArgoCD application"
       Expected: The operation should be refused or unavailable because MCP_READ_ONLY=true.
  </action>
  <verify>
    User confirms: both MCP servers connect, return real data, and read-only mode blocks write operations.
    Resume signal: Type "approved" if MCP servers connect and respond correctly, or describe issues encountered.
  </verify>
  <done>
    Both MCP servers are connected and responding. Kubernetes MCP returns live pod data. ArgoCD MCP returns application sync status. Read-only mode is confirmed working. MCP integration is operational.
  </done>
</task>

</tasks>

<verification>
1. `grep 'argocd-rbac-cm' scripts/bootstrap.sh` matches
2. `bash -n scripts/bootstrap.sh` passes
3. Both MCP servers connect in Claude Code (/mcp shows green)
4. Kubernetes MCP returns real pod data
5. ArgoCD MCP returns real application data
6. Write operations are blocked by default
</verification>

<success_criteria>
bootstrap.sh applies RBAC ConfigMap during bootstrap. User has verified that both MCP servers connect to the live cluster and return real data. Read-only mode is confirmed working -- destructive operations are refused. The complete MCP integration is operational and reproducible.
</success_criteria>

<output>
After completion, create `.planning/phases/10-mcp-integration/10-02-SUMMARY.md`
</output>
