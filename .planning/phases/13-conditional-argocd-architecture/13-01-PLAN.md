---
phase: 13-conditional-argocd-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bootstrap/kind/root-app.yaml
  - bootstrap/kind/argocd-self.yaml
  - bootstrap/kind/argocd-cm.yaml
  - bootstrap/kind/argocd-rbac-cm.yaml
  - bootstrap/kind/argocd-notifications-cm.yaml
  - bootstrap/kind/infra-metallb.yaml
  - bootstrap/kind/infra-envoy-gateway.yaml
  - bootstrap/kind/infra-envoy-gateway-config.yaml
  - bootstrap/kind/infra-cert-manager.yaml
  - bootstrap/kind/infra-sealed-secrets.yaml
  - bootstrap/kind/workload-openclaw.yaml
  - bootstrap/kind/projects/infrastructure.yaml
  - bootstrap/kind/projects/workloads.yaml
  - bootstrap/kinder/root-app.yaml
  - bootstrap/kinder/argocd-self.yaml
  - bootstrap/kinder/argocd-cm.yaml
  - bootstrap/kinder/argocd-rbac-cm.yaml
  - bootstrap/kinder/argocd-notifications-cm.yaml
  - bootstrap/kinder/infra-envoy-gateway-config.yaml
  - bootstrap/kinder/infra-sealed-secrets.yaml
  - bootstrap/kinder/workload-openclaw.yaml
  - bootstrap/kinder/projects/infrastructure.yaml
  - bootstrap/kinder/projects/workloads.yaml
autonomous: true

must_haves:
  truths:
    - "bootstrap/kind/ contains all 11 Application YAMLs + 2 project YAMLs (exact v1.0 set)"
    - "bootstrap/kinder/ contains exactly 8 files (no infra-metallb, infra-envoy-gateway, infra-cert-manager) + 2 project YAMLs"
    - "KIND root-app.yaml and argocd-self.yaml point to bootstrap/kind"
    - "Kinder root-app.yaml and argocd-self.yaml point to bootstrap/kinder"
    - "Shared files (configmaps, projects, shared apps) are byte-identical across both directories"
    - "Old flat bootstrap/ files are removed (directory only contains kind/ and kinder/ subdirectories)"
  artifacts:
    - path: "bootstrap/kind/root-app.yaml"
      provides: "KIND root-app with path: bootstrap/kind"
      contains: "path: bootstrap/kind"
    - path: "bootstrap/kind/argocd-self.yaml"
      provides: "KIND argocd-self with path: bootstrap/kind"
      contains: "path: bootstrap/kind"
    - path: "bootstrap/kinder/root-app.yaml"
      provides: "Kinder root-app with path: bootstrap/kinder"
      contains: "path: bootstrap/kinder"
    - path: "bootstrap/kinder/argocd-self.yaml"
      provides: "Kinder argocd-self with path: bootstrap/kinder"
      contains: "path: bootstrap/kinder"
  key_links:
    - from: "bootstrap/kind/root-app.yaml"
      to: "bootstrap/kind/"
      via: "spec.source.path: bootstrap/kind"
      pattern: "path: bootstrap/kind"
    - from: "bootstrap/kinder/root-app.yaml"
      to: "bootstrap/kinder/"
      via: "spec.source.path: bootstrap/kinder"
      pattern: "path: bootstrap/kinder"
---

<objective>
Create dual-provider bootstrap directory structure with provider-specific root-app scanning

Purpose: ArgoCD discovers only the correct set of child Applications per provider by scanning provider-specific directories. This keeps the provider choice as a runtime decision (bootstrap.sh selects which root-app.yaml to apply) rather than a Git-committed property of the root-app.

Output: `bootstrap/kind/` with full v1.0 Application set; `bootstrap/kinder/` with reduced set (no MetalLB, Envoy GW controller, cert-manager); old flat `bootstrap/` files removed.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-conditional-argocd-architecture/13-RESEARCH.md
@bootstrap/root-app.yaml
@bootstrap/argocd-self.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bootstrap/kind/ with full v1.0 Application set</name>
  <files>
    bootstrap/kind/root-app.yaml
    bootstrap/kind/argocd-self.yaml
    bootstrap/kind/argocd-cm.yaml
    bootstrap/kind/argocd-rbac-cm.yaml
    bootstrap/kind/argocd-notifications-cm.yaml
    bootstrap/kind/infra-metallb.yaml
    bootstrap/kind/infra-envoy-gateway.yaml
    bootstrap/kind/infra-envoy-gateway-config.yaml
    bootstrap/kind/infra-cert-manager.yaml
    bootstrap/kind/infra-sealed-secrets.yaml
    bootstrap/kind/workload-openclaw.yaml
    bootstrap/kind/projects/infrastructure.yaml
    bootstrap/kind/projects/workloads.yaml
  </files>
  <action>
    1. Create directories: `mkdir -p bootstrap/kind/projects`
    2. Copy ALL existing bootstrap files into `bootstrap/kind/`:
       - `cp bootstrap/*.yaml bootstrap/kind/`
       - `cp bootstrap/projects/*.yaml bootstrap/kind/projects/`
    3. Update `bootstrap/kind/root-app.yaml`: Change `path: bootstrap` to `path: bootstrap/kind` in `spec.source.path`. All other fields remain identical (name: root, no finalizers, prune: false, selfHeal: true, recurse: true).
    4. Update `bootstrap/kind/argocd-self.yaml`: Change `path: bootstrap` to `path: bootstrap/kind` in `spec.source.path`. All other fields remain identical (name: argocd-self, wave -10, finalizers, prune: true, recurse: true).
    5. All other files (argocd-cm, argocd-rbac-cm, argocd-notifications-cm, all infra-*.yaml, workload-openclaw.yaml, projects/) are BYTE-IDENTICAL copies -- no modifications.

    CRITICAL: The only changes from v1.0 are the `spec.source.path` fields in root-app.yaml and argocd-self.yaml. Everything else must be a verbatim copy to satisfy ARGO-02 (no regressions from v1.0).
  </action>
  <verify>
    - `ls bootstrap/kind/*.yaml | wc -l` should return 11
    - `ls bootstrap/kind/projects/*.yaml | wc -l` should return 2
    - `grep 'path: bootstrap/kind' bootstrap/kind/root-app.yaml` matches
    - `grep 'path: bootstrap/kind' bootstrap/kind/argocd-self.yaml` matches
    - `diff <(grep -v '^    path:' bootstrap/kind/infra-metallb.yaml) <(grep -v '^    path:' bootstrap/infra-metallb.yaml 2>/dev/null || cat bootstrap/kind/infra-metallb.yaml)` -- infra files unchanged
  </verify>
  <done>bootstrap/kind/ contains all 13 files (11 YAMLs + 2 project YAMLs), root-app and argocd-self point to bootstrap/kind, all other files are byte-identical to v1.0 originals</done>
</task>

<task type="auto">
  <name>Task 2: Create bootstrap/kinder/ with reduced Application set and remove old flat files</name>
  <files>
    bootstrap/kinder/root-app.yaml
    bootstrap/kinder/argocd-self.yaml
    bootstrap/kinder/argocd-cm.yaml
    bootstrap/kinder/argocd-rbac-cm.yaml
    bootstrap/kinder/argocd-notifications-cm.yaml
    bootstrap/kinder/infra-envoy-gateway-config.yaml
    bootstrap/kinder/infra-sealed-secrets.yaml
    bootstrap/kinder/workload-openclaw.yaml
    bootstrap/kinder/projects/infrastructure.yaml
    bootstrap/kinder/projects/workloads.yaml
  </files>
  <action>
    1. Create directories: `mkdir -p bootstrap/kinder/projects`
    2. Copy SHARED files from `bootstrap/kind/` to `bootstrap/kinder/`:
       - Shared non-Application resources: argocd-cm.yaml, argocd-rbac-cm.yaml, argocd-notifications-cm.yaml
       - Shared Application YAMLs: infra-envoy-gateway-config.yaml, infra-sealed-secrets.yaml, workload-openclaw.yaml
       - Projects: projects/infrastructure.yaml, projects/workloads.yaml
    3. Create `bootstrap/kinder/root-app.yaml`:
       - Copy from bootstrap/kind/root-app.yaml
       - Change `path: bootstrap/kind` to `path: bootstrap/kinder`
       - Keep all other fields identical (name: root, no finalizers, prune: false, selfHeal: true, recurse: true)
    4. Create `bootstrap/kinder/argocd-self.yaml`:
       - Copy from bootstrap/kind/argocd-self.yaml
       - Change `path: bootstrap/kind` to `path: bootstrap/kinder`
       - Keep all other fields identical (name: argocd-self, wave -10, finalizers, prune: true, recurse: true)
    5. Do NOT copy these KIND-only files to kinder/: infra-metallb.yaml, infra-envoy-gateway.yaml, infra-cert-manager.yaml
    6. Remove old flat bootstrap files: `git rm bootstrap/*.yaml && git rm bootstrap/projects/*.yaml`
       - After removal, bootstrap/ should contain only kind/ and kinder/ subdirectories
       - Use `git rm` (not plain rm) so Git tracks the deletion properly

    CRITICAL: Kinder directory must NOT contain infra-metallb.yaml, infra-envoy-gateway.yaml, or infra-cert-manager.yaml (ARGO-01). Shared files must be byte-identical to their kind/ counterparts.
  </action>
  <verify>
    - `ls bootstrap/kinder/*.yaml | wc -l` should return 8 (root-app, argocd-self, argocd-cm, argocd-rbac-cm, argocd-notifications-cm, infra-envoy-gateway-config, infra-sealed-secrets, workload-openclaw)
    - `ls bootstrap/kinder/projects/*.yaml | wc -l` should return 2
    - `grep 'path: bootstrap/kinder' bootstrap/kinder/root-app.yaml` matches
    - `grep 'path: bootstrap/kinder' bootstrap/kinder/argocd-self.yaml` matches
    - `test ! -f bootstrap/kinder/infra-metallb.yaml` -- KIND-only file absent
    - `test ! -f bootstrap/kinder/infra-envoy-gateway.yaml` -- KIND-only file absent
    - `test ! -f bootstrap/kinder/infra-cert-manager.yaml` -- KIND-only file absent
    - `test ! -f bootstrap/root-app.yaml` -- old flat file removed
    - `diff bootstrap/kind/argocd-cm.yaml bootstrap/kinder/argocd-cm.yaml` -- shared files identical
    - `diff bootstrap/kind/infra-sealed-secrets.yaml bootstrap/kinder/infra-sealed-secrets.yaml` -- shared apps identical
    - `diff bootstrap/kind/projects/infrastructure.yaml bootstrap/kinder/projects/infrastructure.yaml` -- shared projects identical
  </verify>
  <done>bootstrap/kinder/ contains exactly 10 files (8 YAMLs + 2 project YAMLs), excludes 3 KIND-only Applications, root-app and argocd-self point to bootstrap/kinder, old flat bootstrap/ files removed, shared files byte-identical across both directories</done>
</task>

</tasks>

<verification>
1. `ls bootstrap/kind/ bootstrap/kinder/` -- both directories exist with correct file sets
2. `ls bootstrap/*.yaml 2>/dev/null` -- returns nothing (old flat files removed)
3. KIND: `grep -c 'path: bootstrap/kind' bootstrap/kind/root-app.yaml bootstrap/kind/argocd-self.yaml` -- both files have correct path
4. Kinder: `grep -c 'path: bootstrap/kinder' bootstrap/kinder/root-app.yaml bootstrap/kinder/argocd-self.yaml` -- both files have correct path
5. Shared file identity: `diff bootstrap/kind/argocd-cm.yaml bootstrap/kinder/argocd-cm.yaml && diff bootstrap/kind/argocd-rbac-cm.yaml bootstrap/kinder/argocd-rbac-cm.yaml && diff bootstrap/kind/argocd-notifications-cm.yaml bootstrap/kinder/argocd-notifications-cm.yaml && diff bootstrap/kind/infra-envoy-gateway-config.yaml bootstrap/kinder/infra-envoy-gateway-config.yaml && diff bootstrap/kind/infra-sealed-secrets.yaml bootstrap/kinder/infra-sealed-secrets.yaml && diff bootstrap/kind/workload-openclaw.yaml bootstrap/kinder/workload-openclaw.yaml && diff bootstrap/kind/projects/infrastructure.yaml bootstrap/kinder/projects/infrastructure.yaml && diff bootstrap/kind/projects/workloads.yaml bootstrap/kinder/projects/workloads.yaml` -- all identical
6. KIND-only files absent from kinder: `test ! -f bootstrap/kinder/infra-metallb.yaml && test ! -f bootstrap/kinder/infra-envoy-gateway.yaml && test ! -f bootstrap/kinder/infra-cert-manager.yaml`
</verification>

<success_criteria>
- bootstrap/kind/ contains 13 files (11 YAMLs + 2 projects) matching the full v1.0 Application set
- bootstrap/kinder/ contains 10 files (8 YAMLs + 2 projects) excluding MetalLB, Envoy GW controller, cert-manager
- Both root-app.yaml variants point to their own provider directory
- Both argocd-self.yaml variants point to their own provider directory
- 8 shared files are byte-identical across both directories
- Old flat bootstrap/ files no longer exist
</success_criteria>

<output>
After completion, create `.planning/phases/13-conditional-argocd-architecture/13-01-SUMMARY.md`
</output>
