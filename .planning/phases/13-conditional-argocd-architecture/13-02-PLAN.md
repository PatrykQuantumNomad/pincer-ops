---
phase: 13-conditional-argocd-architecture
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - tests/unit/bootstrap.bats
autonomous: true

must_haves:
  truths:
    - "BATS tests verify kinder directory excludes KIND-only Applications (infra-metallb, infra-envoy-gateway, infra-cert-manager)"
    - "BATS tests verify kind directory includes all v1.0 Applications"
    - "BATS tests verify shared files are byte-identical across both provider directories"
    - "BATS tests verify both root-app.yaml variants point to their own provider directory"
    - "All existing BATS tests still pass (no regressions)"
  artifacts:
    - path: "tests/unit/bootstrap.bats"
      provides: "Provider directory structure validation tests"
      contains: "shared files are identical across provider directories"
  key_links:
    - from: "tests/unit/bootstrap.bats"
      to: "bootstrap/kind/"
      via: "file existence assertions"
      pattern: "bootstrap/kind"
    - from: "tests/unit/bootstrap.bats"
      to: "bootstrap/kinder/"
      via: "file existence assertions and absence checks"
      pattern: "bootstrap/kinder"
---

<objective>
Add BATS tests that enforce the dual-provider bootstrap directory structure invariants

Purpose: Prevent shared file drift between provider directories, catch accidental inclusion of KIND-only Applications in the kinder directory, and verify both root-app variants point to correct paths. These tests act as guardrails for the controlled duplication strategy.

Output: New test cases in tests/unit/bootstrap.bats validating directory structure, file sets, shared file identity, and path correctness.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-conditional-argocd-architecture/13-01-PLAN.md
@tests/unit/bootstrap.bats
@tests/test_helper.bash
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add provider directory structure BATS tests</name>
  <files>tests/unit/bootstrap.bats</files>
  <action>
    Append new test cases to the existing tests/unit/bootstrap.bats file. Add a clearly commented section header for the new tests.

    Tests to add (7 tests):

    1. **"kind bootstrap directory contains all v1.0 Applications"**
       - List YAML files in bootstrap/kind/ (excluding projects/ subdirectory)
       - Assert exactly 11 files exist
       - Assert each expected file is present: root-app.yaml, argocd-self.yaml, argocd-cm.yaml, argocd-rbac-cm.yaml, argocd-notifications-cm.yaml, infra-metallb.yaml, infra-envoy-gateway.yaml, infra-envoy-gateway-config.yaml, infra-cert-manager.yaml, infra-sealed-secrets.yaml, workload-openclaw.yaml

    2. **"kind bootstrap directory contains both project files"**
       - Assert bootstrap/kind/projects/infrastructure.yaml exists
       - Assert bootstrap/kind/projects/workloads.yaml exists

    3. **"kinder bootstrap directory excludes KIND-only Applications"**
       - Assert bootstrap/kinder/infra-metallb.yaml does NOT exist
       - Assert bootstrap/kinder/infra-envoy-gateway.yaml does NOT exist (exact filename, not config variant)
       - Assert bootstrap/kinder/infra-cert-manager.yaml does NOT exist

    4. **"kinder bootstrap directory contains shared Applications"**
       - List YAML files in bootstrap/kinder/ (excluding projects/ subdirectory)
       - Assert exactly 8 files exist
       - Assert each expected file is present: root-app.yaml, argocd-self.yaml, argocd-cm.yaml, argocd-rbac-cm.yaml, argocd-notifications-cm.yaml, infra-envoy-gateway-config.yaml, infra-sealed-secrets.yaml, workload-openclaw.yaml

    5. **"kinder bootstrap directory contains both project files"**
       - Assert bootstrap/kinder/projects/infrastructure.yaml exists
       - Assert bootstrap/kinder/projects/workloads.yaml exists

    6. **"shared files are identical across provider directories"**
       - For each shared file (argocd-cm.yaml, argocd-rbac-cm.yaml, argocd-notifications-cm.yaml, infra-envoy-gateway-config.yaml, infra-sealed-secrets.yaml, workload-openclaw.yaml, projects/infrastructure.yaml, projects/workloads.yaml):
       - Run `diff` between bootstrap/kind/{file} and bootstrap/kinder/{file}
       - Assert diff returns 0 (files identical)
       - If any diff fails, the test reports WHICH file drifted

    7. **"provider root-apps point to their own directory"**
       - Assert bootstrap/kind/root-app.yaml contains `path: bootstrap/kind`
       - Assert bootstrap/kind/argocd-self.yaml contains `path: bootstrap/kind`
       - Assert bootstrap/kinder/root-app.yaml contains `path: bootstrap/kinder`
       - Assert bootstrap/kinder/argocd-self.yaml contains `path: bootstrap/kinder`
       - Use grep to verify -- these are the ONLY lines that should differ between the provider variants of root-app and argocd-self

    Use `$PROJECT_ROOT` for file paths (from test_helper.bash). Use `assert_file_exists` from bats-file where applicable. Use `run` + `assert_success`/`assert_failure` for diff commands.

    Do NOT modify or remove any existing tests in bootstrap.bats. Only append new tests after the existing ones.
  </action>
  <verify>
    - Run `cd /Users/patrykattc/work/git/pincer-ops && bats tests/unit/bootstrap.bats` -- all tests pass (both old and new)
    - New tests appear in output with correct names
    - If bootstrap/kind/ or bootstrap/kinder/ directories don't exist yet (Plan 01 not executed), tests will fail -- this is expected and correct
  </verify>
  <done>7 new BATS tests in bootstrap.bats verify: kind has all 11 apps + 2 projects, kinder has 8 apps + 2 projects (no KIND-only apps), 8 shared files are byte-identical, and both root-app variants point to their own provider directory. All existing tests still pass.</done>
</task>

</tasks>

<verification>
1. `bats tests/unit/bootstrap.bats` -- all tests pass (including existing tests)
2. `bats tests/unit/bootstrap.bats --filter "kind bootstrap"` -- KIND directory tests pass
3. `bats tests/unit/bootstrap.bats --filter "kinder bootstrap"` -- Kinder directory tests pass
4. `bats tests/unit/bootstrap.bats --filter "shared files"` -- Shared file identity test passes
5. `bats tests/unit/bootstrap.bats --filter "provider root-apps"` -- Path correctness test passes
</verification>

<success_criteria>
- 7 new BATS tests added to tests/unit/bootstrap.bats
- All 7 new tests pass when run against the dual-provider directory structure from Plan 01
- All pre-existing tests in bootstrap.bats still pass (no regressions)
- Tests catch: wrong file count, missing files, extra files in kinder, shared file drift, wrong root-app paths
</success_criteria>

<output>
After completion, create `.planning/phases/13-conditional-argocd-architecture/13-02-SUMMARY.md`
</output>
