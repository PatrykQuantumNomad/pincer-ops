---
phase: 27-supervisor-binary-side-loading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infrastructure/openshell/supervisor/kustomization.yaml
  - infrastructure/openshell/supervisor/daemonset.yaml
  - bootstrap/kind/infra-openshell-supervisor.yaml
  - bootstrap/kinder/infra-openshell-supervisor.yaml
  - scripts/bootstrap.sh
  - scripts/validate-manifests.sh
autonomous: true
requirements: [SUPV-01]

must_haves:
  truths:
    - "DaemonSet manifest extracts openshell-sandbox binary from cluster image to /opt/openshell/bin/ on every node"
    - "ArgoCD Application deploys DaemonSet at sync wave 3 (before sandbox at wave 10)"
    - "Bootstrap script loads cluster image into KIND/Kinder before DaemonSet starts"
    - "Kustomize build succeeds for infrastructure/openshell/supervisor"
  artifacts:
    - path: "infrastructure/openshell/supervisor/daemonset.yaml"
      provides: "DaemonSet with init container copying supervisor binary"
      contains: "kind: DaemonSet"
    - path: "infrastructure/openshell/supervisor/kustomization.yaml"
      provides: "Kustomize root for supervisor manifests"
      contains: "kind: Kustomization"
    - path: "bootstrap/kind/infra-openshell-supervisor.yaml"
      provides: "ArgoCD Application at sync wave 3"
      contains: "sync-wave: \"3\""
    - path: "bootstrap/kinder/infra-openshell-supervisor.yaml"
      provides: "Byte-identical copy for Kinder provider"
      contains: "sync-wave: \"3\""
  key_links:
    - from: "bootstrap/kind/infra-openshell-supervisor.yaml"
      to: "infrastructure/openshell/supervisor"
      via: "ArgoCD Application source path"
      pattern: "path: infrastructure/openshell/supervisor"
    - from: "scripts/bootstrap.sh"
      to: "ghcr.io/nvidia/openshell/cluster:0.0.12"
      via: "Image load before DaemonSet deploy"
      pattern: "openshell/cluster"
---

<objective>
Create the supervisor binary DaemonSet infrastructure that distributes the openshell-sandbox binary to all cluster nodes via init container + hostPath pattern.

Purpose: SUPV-01 requires the supervisor binary at /opt/openshell/bin/openshell-sandbox on every node. The DaemonSet extracts it from the ghcr.io/nvidia/openshell/cluster:0.0.12 image. ArgoCD manages the DaemonSet at sync wave 3 (after CRD controller at wave 2, before sandbox at wave 10). Bootstrap.sh must load the cluster image into KIND/Kinder before the DaemonSet can start.

Output: DaemonSet Kustomize root, ArgoCD Applications for both providers, bootstrap.sh image loading step, validate-manifests.sh update.
</objective>

<execution_context>
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-supervisor-binary-side-loading/27-RESEARCH.md

<interfaces>
<!-- Existing ArgoCD Application pattern (from infra-openshell.yaml): -->
<!-- repoURL: https://github.com/PatrykQuantumNomad/pincer-ops.git -->
<!-- targetRevision: main -->
<!-- project: openshell -->
<!-- syncPolicy: automated selfHeal+prune, CreateNamespace=false -->

<!-- Existing sync wave map: -->
<!-- Wave 0: infra-openshell (namespace) -->
<!-- Wave 2: infra-agent-sandbox (CRD controller) -->
<!-- Wave 3: infra-openshell-supervisor [NEW - this plan] -->
<!-- Wave 5: workload-openshell-gateway -->
<!-- Wave 10: workload-openclaw-sandbox -->

<!-- Bootstrap.sh Step 16 deploys OpenClaw sandbox (workload-openclaw-sandbox.yaml) -->
<!-- New image load step goes BEFORE Step 16, after cert-manager (Step 15) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create supervisor DaemonSet Kustomize root and ArgoCD Applications</name>
  <files>
    infrastructure/openshell/supervisor/kustomization.yaml,
    infrastructure/openshell/supervisor/daemonset.yaml,
    bootstrap/kind/infra-openshell-supervisor.yaml,
    bootstrap/kinder/infra-openshell-supervisor.yaml
  </files>
  <action>
Create the supervisor DaemonSet infrastructure:

**infrastructure/openshell/supervisor/daemonset.yaml:**
- apiVersion: apps/v1, kind: DaemonSet, name: openshell-supervisor, namespace: openshell
- Labels: `app.kubernetes.io/name: openshell-supervisor`
- Init container `copy-supervisor`:
  - image: `ghcr.io/nvidia/openshell/cluster:0.0.12`, imagePullPolicy: IfNotPresent
  - command: `["cp", "/opt/openshell/bin/openshell-sandbox", "/host-bin/openshell-sandbox"]`
  - volumeMount: `host-bin` at `/host-bin`
  - securityContext: runAsUser: 0, allowPrivilegeEscalation: false, capabilities.drop: ["ALL"], readOnlyRootFilesystem: true
- Main container `pause`:
  - image: `registry.k8s.io/pause:3.10`, imagePullPolicy: IfNotPresent
  - resources: requests cpu: 1m, memory: 4Mi; limits cpu: 10m, memory: 16Mi
- Volume `host-bin`: hostPath path: `/opt/openshell/bin`, type: `DirectoryOrCreate`
- tolerations: `- operator: Exists` (run on ALL nodes including control-plane)

**infrastructure/openshell/supervisor/kustomization.yaml:**
- apiVersion: kustomize.config.k8s.io/v1beta1, kind: Kustomization
- namespace: openshell
- resources: [daemonset.yaml]

**bootstrap/kind/infra-openshell-supervisor.yaml:**
- Follow exact pattern of existing infra-openshell.yaml but with:
  - name: infra-openshell-supervisor
  - sync-wave: "3" (between CRD controller at wave 2 and gateway at wave 5)
  - manifest-generate-paths: infrastructure/openshell/supervisor
  - project: openshell
  - path: infrastructure/openshell/supervisor
  - destination namespace: openshell
  - syncPolicy: automated selfHeal+prune, CreateNamespace=false
  - Do NOT include ServerSideApply=true (DaemonSet is not CRD-heavy)
  - Include notification annotations (on-sync-failed, on-health-degraded, on-sync-status-unknown) matching existing pattern
  - Include resources-finalizer

**bootstrap/kinder/infra-openshell-supervisor.yaml:**
- Byte-identical copy of the kind version

Update the sync wave comment in the infra-openshell-supervisor.yaml header to show the current v2.0 sync wave map including wave 3 for supervisor.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/pincer-ops && kubectl kustomize infrastructure/openshell/supervisor && diff bootstrap/kind/infra-openshell-supervisor.yaml bootstrap/kinder/infra-openshell-supervisor.yaml</automated>
  </verify>
  <done>DaemonSet manifest renders via kustomize, ArgoCD Applications are byte-identical across providers, sync wave 3 annotation present</done>
</task>

<task type="auto">
  <name>Task 2: Update bootstrap.sh and validate-manifests.sh for supervisor</name>
  <files>
    scripts/bootstrap.sh,
    scripts/validate-manifests.sh
  </files>
  <action>
**scripts/bootstrap.sh:**
Add a cluster image loading step BEFORE Step 16 (Deploy OpenClaw Sandbox). Insert between the cert-manager block and Step 16:

```
# Step 15b: Load OpenShell cluster image (supervisor binary source)
# The DaemonSet init container copies the supervisor binary from this image.
# Must be loaded BEFORE ArgoCD syncs the DaemonSet (sync wave 3).
log_step "Loading OpenShell cluster image..."
OPENSHELL_CLUSTER_IMAGE="ghcr.io/nvidia/openshell/cluster:0.0.12"
if docker image inspect "${OPENSHELL_CLUSTER_IMAGE}" >/dev/null 2>&1; then
  run_cmd "${PROVIDER_BIN}" load docker-image "${OPENSHELL_CLUSTER_IMAGE}" --name "${CLUSTER_NAME}"
  log_info "OpenShell cluster image loaded"
else
  log_warn "OpenShell cluster image not found locally -- DaemonSet will pull from registry"
  log_warn "Pre-pull with: docker pull ${OPENSHELL_CLUSTER_IMAGE}"
fi
```

This must come after cluster creation but before Step 16 where ArgoCD syncs the sandbox. The DaemonSet at wave 3 will need this image available. Use the same `"${PROVIDER_BIN}" load docker-image` pattern that `make load-image` uses (both kinder and kind support this syntax).

Also load the pause:3.10 image if not already available:
```
PAUSE_IMAGE="registry.k8s.io/pause:3.10"
if docker image inspect "${PAUSE_IMAGE}" >/dev/null 2>&1; then
  run_cmd "${PROVIDER_BIN}" load docker-image "${PAUSE_IMAGE}" --name "${CLUSTER_NAME}"
fi
```

**scripts/validate-manifests.sh:**
Add a new validation line after the `openshell-gateway` block:

```
# openshell supervisor: DaemonSet for binary distribution
validate_kustomize "infrastructure/openshell/supervisor" "openshell-supervisor"
```
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/pincer-ops && grep 'openshell/cluster' scripts/bootstrap.sh && grep 'openshell-supervisor' scripts/validate-manifests.sh && bash scripts/validate-manifests.sh 2>&1 | tail -5</automated>
  </verify>
  <done>bootstrap.sh has image loading step for cluster image before Step 16, validate-manifests.sh validates the supervisor Kustomize path without errors</done>
</task>

</tasks>

<verification>
1. `kubectl kustomize infrastructure/openshell/supervisor` renders a valid DaemonSet YAML
2. `diff bootstrap/kind/infra-openshell-supervisor.yaml bootstrap/kinder/infra-openshell-supervisor.yaml` returns 0
3. `grep 'sync-wave: "3"' bootstrap/kind/infra-openshell-supervisor.yaml` finds the annotation
4. `grep 'openshell/cluster' scripts/bootstrap.sh` confirms image loading step
5. `bash scripts/validate-manifests.sh` passes (includes supervisor path)
</verification>

<success_criteria>
- DaemonSet with init container extracting supervisor binary from cluster:0.0.12 to /opt/openshell/bin/
- ArgoCD Application at wave 3 for both providers (byte-identical)
- Bootstrap.sh loads cluster image into KIND/Kinder before DaemonSet deploy
- validate-manifests.sh includes supervisor Kustomize path
</success_criteria>

<output>
After completion, create `.planning/phases/27-supervisor-binary-side-loading/27-01-SUMMARY.md`
</output>
