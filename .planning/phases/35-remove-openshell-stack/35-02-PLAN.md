---
phase: 35-remove-openshell-stack
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - scripts/bootstrap.sh
  - scripts/validate-manifests.sh
  - Makefile
  - tests/unit/bootstrap.bats
  - tests/unit/validate-manifests.bats
autonomous: true
requirements: [REM-05, REM-04]

must_haves:
  truths:
    - "bootstrap.sh has no generate_tls_artifacts function"
    - "bootstrap.sh has no OpenShell namespace creation step"
    - "bootstrap.sh has no OpenShell image loading step"
    - "bootstrap.sh has no supervisor DaemonSet wait step"
    - "bootstrap.sh has no gateway StatefulSet wait step"
    - "bootstrap.sh has no Sandbox CRD wait or Sandbox deployment step"
    - "bootstrap.sh retains cert-manager readiness wait and ClusterIssuer apply"
    - "bootstrap.sh retains all non-OpenShell steps unchanged"
    - "Makefile has no verify-supervisor target"
    - "Makefile doctor target has no openshell or agent-sandbox namespace checks"
    - "Makefile OpenClaw CLI targets reference openclaw namespace (not openshell)"
    - "validate-manifests.sh has no openshell or agent-sandbox validation entries"
    - "bootstrap.bats has no tests referencing OpenShell functions or files"
    - "validate-manifests.bats assertions are consistent with updated validate-manifests.sh"
  artifacts:
    - path: "scripts/bootstrap.sh"
      provides: "Bootstrap without OpenShell steps"
      contains: "cert-manager readiness"
    - path: "Makefile"
      provides: "Developer workflow without OpenShell targets"
    - path: "scripts/validate-manifests.sh"
      provides: "Manifest validation without OpenShell entries"
    - path: "tests/unit/bootstrap.bats"
      provides: "Bootstrap tests without OpenShell assertions"
    - path: "tests/unit/validate-manifests.bats"
      provides: "Validation tests without OpenShell references"
  key_links:
    - from: "scripts/bootstrap.sh"
      to: "bootstrap/kind/ and bootstrap/kinder/"
      via: "BOOTSTRAP_DIR variable"
      pattern: "kubectl apply -f.*BOOTSTRAP_DIR"
    - from: "Makefile"
      to: "scripts/bootstrap.sh"
      via: "make up target"
      pattern: "CLUSTER_PROVIDER.*bootstrap.sh"
---

<objective>
Remove all OpenShell references from bootstrap.sh, Makefile, validate-manifests.sh, and their test files.

Purpose: After Plan 01 deleted the OpenShell files, these scripts and tests still reference them. This plan cleans those references so the scripts work correctly without OpenShell.
Output: Updated scripts and tests with no OpenShell references.
</objective>

<execution_context>
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-remove-openshell-stack/35-01-SUMMARY.md
</context>

<interfaces>
<!-- Current bootstrap.sh structure that will be modified -->
<!-- Key sections to remove (line references from current file): -->
<!-- Lines 36-70: generate_tls_artifacts() function -->
<!-- Lines 222-238: Step 8b - Create OpenShell namespaces -->
<!-- Lines 240-270: Step 8c/8d - cert-manager wait + TLS artifacts call -->
<!--   NOTE: Keep step 8c cert-manager readiness wait (lines 245-260) -->
<!--   NOTE: Keep selfsigned-clusterissuer apply (lines 262-267) -->
<!--   REMOVE: generate_tls_artifacts call (line 270) -->
<!-- Lines 513-529: Step 15b - Load OpenShell cluster image + pause image -->
<!-- Lines 531-547: Step 16 - Wait for supervisor DaemonSet -->
<!-- Lines 549-563: Step 17 - Wait for OpenShell gateway -->
<!-- Lines 565-607: Step 18 - Deploy OpenClaw Sandbox -->
<!-- Lines 629: Summary line "OpenClaw: openclaw-sandbox in openshell namespace" -->

<!-- Current Makefile sections to modify: -->
<!-- Lines 122-124: verify-supervisor target (DELETE) -->
<!-- Lines 158-226: doctor target OpenShell Infrastructure section (DELETE) -->
<!-- Lines 244-245: logs target references openshell namespace -->
<!-- Lines 255-316: OpenClaw CLI section references openshell namespace -->

<!-- Current validate-manifests.sh sections to remove: -->
<!-- Lines 92: validate_kustomize "workloads/openclaw-sandbox/overlays/dev" -->
<!-- Lines 101-108: openshell validate_kustomize calls -->
<!-- Lines 113-122: agent-sandbox skip note and echo lines -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Clean bootstrap.sh of all OpenShell-specific steps</name>
  <files>scripts/bootstrap.sh</files>
  <action>
Modify scripts/bootstrap.sh to remove all OpenShell-specific code (REM-05). The following sections must be REMOVED:

1. **DELETE generate_tls_artifacts() function** (lines 36-70) -- The entire function definition including the comment block above it.

2. **DELETE Step 8b** (lines 222-238) -- "Creating OpenShell namespaces" block. Remove the entire step that creates openshell and agent-sandbox-system namespaces.

3. **In Step 8c/8d area** (lines 240-270):
   - KEEP the cert-manager readiness wait (lines 245-260: CRD wait loop, deployment waits for cert-manager and cert-manager-webhook). This is needed for future TLS.
   - KEEP the selfsigned-clusterissuer apply block (lines 262-267).
   - DELETE the "Step 8d" comment and the `generate_tls_artifacts` call (lines 269-270).
   - Update the step 8c comment to remove OpenShell references. Change it to something like: "Step 8c: Ensure cert-manager is ready and selfsigned-issuer exists" (keep this wording, just remove the lines about "We need TLS artifacts now").

4. **DELETE Step 15b** (lines 513-529) -- "Loading OpenShell cluster image" block. Remove the entire step including the pause image loading.

5. **DELETE Step 16** (lines 531-547) -- "Waiting for supervisor DaemonSet" block.

6. **DELETE Step 17** (lines 549-563) -- "Waiting for OpenShell gateway" block.

7. **DELETE Step 18** (lines 565-607) -- "Deploying OpenClaw sandbox" block. This is the entire Sandbox CRD wait + workload-openclaw-sandbox Application + Sandbox CR wait.

8. **UPDATE the summary banner** (lines 612-632):
   - Remove the line: `echo "  OpenClaw:   openclaw-sandbox in openshell namespace (port 18789)"`
   - This is a placeholder -- Phase 36 will add the restored OpenClaw summary line.

IMPORTANT PRESERVATIONS:
- Keep ALL steps 0-7 (cluster creation, network detection, MetalLB, ArgoCD install, ArgoCD config, ArgoCD readiness) unchanged.
- Keep Step 9 (root Application) and Step 9b (child Applications) unchanged.
- Keep Steps 10-12 (MetalLB, L2 pool, Envoy Gateway controller) -- KIND-only steps unchanged.
- Keep Step 13 (Gateway API configuration) unchanged.
- Keep Step 14 (Sealed Secrets) unchanged.
- Keep Step 15 (cert-manager deploy, KIND-only) unchanged.
- Keep the cert-manager readiness wait in step 8c -- just remove the TLS generation call after it.

After all removals, the bootstrap flow should be:
Steps 0-7 -> Step 8 (ArgoCD readiness) -> Step 8c (cert-manager readiness, both providers) -> Step 9/9b (root app + children) -> Steps 10-15 (KIND-only infra + shared infra) -> Done summary.
  </action>
  <verify>
    <automated>bash -c 'grep -c "openshell\|agent-sandbox\|openclaw-sandbox\|generate_tls_artifacts\|supervisor DaemonSet\|OpenShell gateway\|OpenClaw sandbox" scripts/bootstrap.sh && echo "FAIL: OpenShell references remain" || echo "PASS: no OpenShell references"'</automated>
  </verify>
  <done>bootstrap.sh contains zero references to openshell, agent-sandbox, openclaw-sandbox, generate_tls_artifacts, supervisor, or OpenShell gateway. cert-manager readiness wait and ClusterIssuer apply are preserved. Script syntax is valid (bash -n scripts/bootstrap.sh passes).</done>
</task>

<task type="auto">
  <name>Task 2: Clean Makefile and validate-manifests.sh of OpenShell references</name>
  <files>Makefile, scripts/validate-manifests.sh</files>
  <action>
**Makefile changes:**

1. DELETE the `verify-supervisor` target (lines 122-124):
   ```
   .PHONY: verify-supervisor
   verify-supervisor: ## Run runtime supervisor isolation verification tests
   	@CLUSTER_PROVIDER=$(CLUSTER_PROVIDER) ./scripts/verify-supervisor.sh
   ```

2. UPDATE the `doctor` target (lines 127-226):
   - Remove the entire "--- OpenShell Infrastructure ---" section (lines 180-218) which checks: openshell namespace, agent-sandbox-system namespace, openshell PSS, sandbox PSS, and Landlock availability.
   - Update the OpenClaw health check. Currently it checks `kubectl get sandbox openclaw-sandbox -n openshell` (lines 158-163). REMOVE this check entirely -- Phase 36 will add the restored OpenClaw health check. Adjust TOTAL count accordingly (remove 1 from the count for OpenClaw check removal).
   - The remaining doctor checks should be: ArgoCD, Envoy DaemonSet, Sealed Secrets, and KIND-only MetalLB + cert-manager.

3. UPDATE the `logs` target (line 245):
   - Currently: `kubectl logs -n openshell -l app.kubernetes.io/name=openclaw-gateway -f --tail=50`
   - Change to: `kubectl logs -n openclaw -l app.kubernetes.io/name=openclaw-gateway -f --tail=50`
   - Note: Phase 36 will create the actual OpenClaw workload in the openclaw namespace, but we update the namespace reference now to be correct going forward.

4. UPDATE the OpenClaw CLI section (lines 255-316):
   - Change `OPENCLAW_NS  := openshell` to `OPENCLAW_NS  := openclaw`
   - Change `OPENCLAW_POD := openclaw-sandbox` to `OPENCLAW_POD := openclaw-gateway-0` (Phase 36 will create the StatefulSet with this name)

**validate-manifests.sh changes:**

1. REMOVE the line validating openclaw-sandbox overlay (line 92):
   `validate_kustomize "workloads/openclaw-sandbox/overlays/dev" "openclaw-sandbox/dev"`

2. REMOVE the openshell validation block (lines 101-108):
   ```
   # openshell: namespace only
   validate_kustomize "infrastructure/openshell/base" "openshell"
   # openshell gateway: local resources only (pre-rendered from Helm chart)
   validate_kustomize "infrastructure/openshell/gateway" "openshell-gateway"
   # openshell supervisor: DaemonSet for binary distribution
   validate_kustomize "infrastructure/openshell/supervisor" "openshell-supervisor"
   ```

3. REMOVE the agent-sandbox skip note from the echo block (lines 113-122). Remove the lines:
   ```
   echo "  - infrastructure/agent-sandbox/base (remote: github.com/kubernetes-sigs/agent-sandbox)"
   echo "    NOTE: Sandbox CR validation uses local schema (schemas/agents.x-k8s.io/sandbox_v1alpha1.json)"
   ```
   Also remove the comment lines 113-116 above that reference agent-sandbox and Sandbox CR validation.

After modifications, validate-manifests.sh should validate:
- bootstrap/kind/ (raw)
- bootstrap/kinder/ (raw)
- infrastructure/envoy-gateway/base (kustomize)
- Skipped: metallb, sealed-secrets, cert-manager (remote resources)
  </action>
  <verify>
    <automated>bash -c 'FAIL=0; grep -q "verify-supervisor" Makefile && FAIL=1; grep -q "openshell" Makefile && FAIL=1; grep -q "agent-sandbox" Makefile && FAIL=1; grep -q "openshell\|agent-sandbox\|openclaw-sandbox" scripts/validate-manifests.sh && FAIL=1; if [ $FAIL -eq 0 ]; then echo "PASS"; else echo "FAIL: references remain"; fi'</automated>
  </verify>
  <done>Makefile has no verify-supervisor target, no openshell/agent-sandbox references in doctor, and OpenClaw CLI points to openclaw namespace. validate-manifests.sh validates only envoy-gateway kustomize overlay (no openshell/agent-sandbox/openclaw-sandbox entries).</done>
</task>

<task type="auto">
  <name>Task 3: Update test files to remove OpenShell assertions</name>
  <files>tests/unit/bootstrap.bats, tests/unit/validate-manifests.bats</files>
  <action>
**bootstrap.bats changes:**

1. UPDATE the "kind bootstrap directory contains all v1.0 Applications" test (lines 127-156):
   - Remove from expected_files array: `infra-openshell.yaml`, `infra-agent-sandbox.yaml`, `infra-openshell-supervisor.yaml`, `workload-openclaw-sandbox.yaml`, `workload-openshell-gateway.yaml`
   - Update actual_count assertion from `[ "${actual_count}" -eq 15 ]` to `[ "${actual_count}" -eq 10 ]` (15 minus 5 OpenShell files)

2. UPDATE the "kind bootstrap directory contains all project files" test (lines 158-161):
   - Remove assertion: `assert_file_exists "${PROJECT_ROOT}/bootstrap/kind/projects/openshell-project.yaml"`

3. UPDATE the "kinder bootstrap directory contains shared Applications" test (lines 169-195):
   - Remove from expected_files array: `infra-openshell.yaml`, `infra-agent-sandbox.yaml`, `infra-openshell-supervisor.yaml`, `workload-openclaw-sandbox.yaml`, `workload-openshell-gateway.yaml`
   - Update actual_count assertion from `[ "${actual_count}" -eq 12 ]` to `[ "${actual_count}" -eq 7 ]` (12 minus 5 OpenShell files)

4. UPDATE the "kinder bootstrap directory contains all project files" test (lines 197-200):
   - Remove assertion: `assert_file_exists "${PROJECT_ROOT}/bootstrap/kinder/projects/openshell-project.yaml"`

5. UPDATE the "shared files are identical across provider directories" test (lines 202-222):
   - Remove from shared_files array: `infra-openshell.yaml`, `infra-agent-sandbox.yaml`, `infra-openshell-supervisor.yaml`, `workload-openclaw-sandbox.yaml`, `workload-openshell-gateway.yaml`, `projects/openshell-project.yaml`

6. DELETE the entire "OpenShell bootstrap" test section (lines 340-367):
   - Delete: "bootstrap.sh defines generate_tls_artifacts function"
   - Delete: "bootstrap.sh calls generate_tls_artifacts"
   - Delete: "bootstrap.sh creates openshell namespace before root-app"
   - Delete: "bootstrap.sh creates agent-sandbox-system namespace before root-app"

7. DELETE the entire "TLS Activation" test section (lines 369-382):
   - Delete: "bootstrap.sh generate_tls_artifacts is active"
   - Delete: "bootstrap.sh generate_tls_artifacts is not placeholder"

8. UPDATE the "bootstrap.sh with kinder" test (lines 242-275):
   - Remove the assertion: `assert_output --partial "Deploying OpenClaw"` (line 274, since Step 18 is removed)

**validate-manifests.bats changes:**

The tests at lines 82-135 assert output from validate-manifests.sh. Since we removed the openclaw-sandbox/dev validation call, these assertions referencing "openclaw-sandbox/dev" need updating.

1. UPDATE "validate_kustomize: FAIL when pipeline fails" test (line 89):
   - Change `assert_output --partial "FAIL: openclaw-sandbox/dev"` to `assert_output --partial "FAIL: envoy-gateway"` (envoy-gateway is now the first kustomize validation target)

2. UPDATE "EXIT_CODE: first passes, second fails -> exit 1" test (line 103):
   - The `_mock_kubeconform_fail_on_call 3` makes the 3rd kubeconform invocation fail. With openclaw-sandbox removed, the 3rd call is now envoy-gateway (after bootstrap/kind and bootstrap/kinder). Update the assertion from `"FAIL: openclaw-sandbox/dev"` to `"FAIL: envoy-gateway"`.

3. UPDATE "kubectl kustomize failure propagates as FAIL" test (line 118):
   - Change `assert_output --partial "FAIL: openclaw-sandbox/dev"` to `assert_output --partial "FAIL: envoy-gateway"`

4. UPDATE "all validations pass -> exit 0 with summary" test (line 132):
   - Remove `assert_output --partial "PASS: openclaw-sandbox/dev"`
   - Keep `assert_output --partial "PASS: envoy-gateway"`
  </action>
  <verify>
    <automated>bash -c 'FAIL=0; grep -q "openshell" tests/unit/bootstrap.bats && FAIL=1; grep -q "agent-sandbox" tests/unit/bootstrap.bats && FAIL=1; grep -q "generate_tls_artifacts" tests/unit/bootstrap.bats && FAIL=1; grep -q "openclaw-sandbox" tests/unit/validate-manifests.bats && FAIL=1; if [ $FAIL -eq 0 ]; then echo "PASS"; else echo "FAIL: references remain"; fi'</automated>
  </verify>
  <done>bootstrap.bats has no OpenShell test assertions. File count assertions match post-deletion state (kind: 10, kinder: 7). validate-manifests.bats assertions reference envoy-gateway instead of openclaw-sandbox/dev. No references to openshell, agent-sandbox, generate_tls_artifacts, or openclaw-sandbox remain in test files.</done>
</task>

</tasks>

<verification>
- `grep -r "openshell\|agent-sandbox\|openclaw-sandbox\|verify-supervisor\|generate_tls_artifacts" scripts/ tests/ Makefile` returns no results
- `bash -n scripts/bootstrap.sh` passes (valid syntax)
- `bash -n scripts/validate-manifests.sh` passes (valid syntax)
- bootstrap.sh still contains "cert-manager readiness" wait logic
- bootstrap.sh still contains "selfsigned-clusterissuer" apply
- Makefile `make help` runs without errors
</verification>

<success_criteria>
All five files modified with zero OpenShell references remaining. bootstrap.sh retains cert-manager readiness wait and ClusterIssuer apply. Makefile doctor checks only non-OpenShell components. validate-manifests.sh validates only envoy-gateway kustomize overlay. All test assertions updated to match post-deletion file structure.
</success_criteria>

<output>
After completion, create `.planning/phases/35-remove-openshell-stack/35-02-SUMMARY.md`
</output>
