---
phase: 35-remove-openshell-stack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infrastructure/openshell/ (DELETE entire directory)
  - infrastructure/agent-sandbox/ (DELETE entire directory)
  - workloads/openclaw-sandbox/ (DELETE entire directory)
  - schemas/agents.x-k8s.io/ (DELETE entire directory)
  - bootstrap/kind/infra-openshell.yaml (DELETE)
  - bootstrap/kind/infra-openshell-supervisor.yaml (DELETE)
  - bootstrap/kind/infra-agent-sandbox.yaml (DELETE)
  - bootstrap/kind/workload-openshell-gateway.yaml (DELETE)
  - bootstrap/kind/workload-openclaw-sandbox.yaml (DELETE)
  - bootstrap/kind/projects/openshell-project.yaml (DELETE)
  - bootstrap/kinder/infra-openshell.yaml (DELETE)
  - bootstrap/kinder/infra-openshell-supervisor.yaml (DELETE)
  - bootstrap/kinder/infra-agent-sandbox.yaml (DELETE)
  - bootstrap/kinder/workload-openshell-gateway.yaml (DELETE)
  - bootstrap/kinder/workload-openclaw-sandbox.yaml (DELETE)
  - bootstrap/kinder/projects/openshell-project.yaml (DELETE)
  - scripts/verify-supervisor.sh (DELETE)
  - tests/unit/openshell-manifests.bats (DELETE)
autonomous: true
requirements: [REM-01, REM-02, REM-03, REM-04, REM-06]

must_haves:
  truths:
    - "infrastructure/openshell/ directory does not exist"
    - "infrastructure/agent-sandbox/ directory does not exist"
    - "workloads/openclaw-sandbox/ directory does not exist"
    - "No ArgoCD Application YAML referencing OpenShell exists in either bootstrap directory"
    - "openshell-project.yaml does not exist in either provider's projects/"
    - "scripts/verify-supervisor.sh does not exist"
    - "tests/unit/openshell-manifests.bats does not exist"
    - "schemas/agents.x-k8s.io/ directory does not exist"
  artifacts: []
  key_links: []
---

<objective>
Delete all OpenShell infrastructure, workload, bootstrap, test, and schema files from the repository.

Purpose: Remove every OpenShell-related file so the repository no longer contains any OpenShell components. This is pure deletion -- no modifications to remaining files (handled by Plan 02).
Output: All OpenShell directories and files deleted from git.
</objective>

<execution_context>
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete all OpenShell infrastructure and workload directories</name>
  <files>
    infrastructure/openshell/ (DELETE)
    infrastructure/agent-sandbox/ (DELETE)
    workloads/openclaw-sandbox/ (DELETE)
    schemas/agents.x-k8s.io/ (DELETE)
  </files>
  <action>
Delete the following four directories entirely (rm -rf):
- infrastructure/openshell/ -- gateway StatefulSet, supervisor DaemonSet, namespace, RBAC, TLS chain, SealedSecret (REM-01)
- infrastructure/agent-sandbox/ -- CRD controller, namespace, patches (REM-02)
- workloads/openclaw-sandbox/ -- Sandbox CR, policy ConfigMap, registration Job, service, httproute, networkpolicy (REM-03)
- schemas/agents.x-k8s.io/ -- local Sandbox CRD JSON schema (no longer needed without agent-sandbox)

Use `git rm -rf` for each directory so they are staged for commit.
  </action>
  <verify>
    <automated>test ! -d infrastructure/openshell && test ! -d infrastructure/agent-sandbox && test ! -d workloads/openclaw-sandbox && test ! -d schemas/agents.x-k8s.io && echo "PASS: all directories deleted" || echo "FAIL"</automated>
  </verify>
  <done>All four directories and their contents no longer exist in the working tree or git index.</done>
</task>

<task type="auto">
  <name>Task 2: Delete all OpenShell ArgoCD Applications, AppProject, scripts, and tests</name>
  <files>
    bootstrap/kind/infra-openshell.yaml (DELETE)
    bootstrap/kind/infra-openshell-supervisor.yaml (DELETE)
    bootstrap/kind/infra-agent-sandbox.yaml (DELETE)
    bootstrap/kind/workload-openshell-gateway.yaml (DELETE)
    bootstrap/kind/workload-openclaw-sandbox.yaml (DELETE)
    bootstrap/kind/projects/openshell-project.yaml (DELETE)
    bootstrap/kinder/infra-openshell.yaml (DELETE)
    bootstrap/kinder/infra-openshell-supervisor.yaml (DELETE)
    bootstrap/kinder/infra-agent-sandbox.yaml (DELETE)
    bootstrap/kinder/workload-openshell-gateway.yaml (DELETE)
    bootstrap/kinder/workload-openclaw-sandbox.yaml (DELETE)
    bootstrap/kinder/projects/openshell-project.yaml (DELETE)
    scripts/verify-supervisor.sh (DELETE)
    tests/unit/openshell-manifests.bats (DELETE)
  </files>
  <action>
Delete the following files using `git rm`:

From bootstrap/kind/:
- infra-openshell.yaml (REM-04)
- infra-openshell-supervisor.yaml (REM-04)
- infra-agent-sandbox.yaml (REM-04)
- workload-openshell-gateway.yaml (REM-04)
- workload-openclaw-sandbox.yaml (REM-04)
- projects/openshell-project.yaml (REM-06)

From bootstrap/kinder/ (identical copies):
- infra-openshell.yaml (REM-04)
- infra-openshell-supervisor.yaml (REM-04)
- infra-agent-sandbox.yaml (REM-04)
- workload-openshell-gateway.yaml (REM-04)
- workload-openclaw-sandbox.yaml (REM-04)
- projects/openshell-project.yaml (REM-06)

Scripts and tests:
- scripts/verify-supervisor.sh -- runtime supervisor verification (no longer needed)
- tests/unit/openshell-manifests.bats -- BATS tests for OpenShell manifests (no longer needed)

Use `git rm` for each file so they are staged for commit.
  </action>
  <verify>
    <automated>test ! -f bootstrap/kind/infra-openshell.yaml && test ! -f bootstrap/kinder/infra-openshell.yaml && test ! -f bootstrap/kind/projects/openshell-project.yaml && test ! -f bootstrap/kinder/projects/openshell-project.yaml && test ! -f scripts/verify-supervisor.sh && test ! -f tests/unit/openshell-manifests.bats && echo "PASS: all files deleted" || echo "FAIL"</automated>
  </verify>
  <done>All 14 files removed from working tree and git index. No ArgoCD Application references OpenShell in either bootstrap directory. openshell-project.yaml removed from both providers.</done>
</task>

</tasks>

<verification>
- `find . -path ./.git -prune -o -name '*openshell*' -print` returns no results
- `find . -path ./.git -prune -o -name '*agent-sandbox*' -print` returns no results (except any test helper references)
- `find . -path ./.git -prune -o -name '*openclaw-sandbox*' -print` returns no results
- `ls infrastructure/` shows only: metallb, envoy-gateway, sealed-secrets, cert-manager
- `ls bootstrap/kind/projects/` shows only: infrastructure.yaml
- `ls bootstrap/kinder/projects/` shows only: infrastructure.yaml
</verification>

<success_criteria>
All OpenShell-related files and directories are deleted from the repository. No file with "openshell", "agent-sandbox", or "openclaw-sandbox" exists in infrastructure/, workloads/, bootstrap/, scripts/, tests/, or schemas/ directories.
</success_criteria>

<output>
After completion, create `.planning/phases/35-remove-openshell-stack/35-01-SUMMARY.md`
</output>
