---
phase: 15-developer-experience-documentation
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - README.md
  - CLAUDE.md
autonomous: true

must_haves:
  truths:
    - "README.md documents Kinder as default provider and KIND as opt-in alternative"
    - "README.md shows how to select provider (`make up` vs `make up PROVIDER=kind`)"
    - "README.md lists what differs between providers (ArgoCD-managed components)"
    - "CLAUDE.md architecture overview shows Kinder as default with KIND differences noted"
    - "CLAUDE.md sync wave table covers both provider paths"
    - "CLAUDE.md repository structure reflects bootstrap/kind/ and bootstrap/kinder/ split"
    - "CLAUDE.md Common Operations table includes `make doctor`"
  artifacts:
    - path: "README.md"
      provides: "Dual-provider usage documentation"
      contains: "PROVIDER=kind"
    - path: "CLAUDE.md"
      provides: "Updated architecture documentation for Kinder as default"
      contains: "Kinder"
  key_links:
    - from: "README.md quick start"
      to: "make up / make up PROVIDER=kind"
      via: "provider selection instructions"
      pattern: "PROVIDER=kind"
    - from: "CLAUDE.md architecture"
      to: "bootstrap/kind/ and bootstrap/kinder/"
      via: "repository structure section"
      pattern: "bootstrap/kind"
---

<objective>
Update README.md and CLAUDE.md to document the dual-provider architecture: Kinder as default, KIND as opt-in, what differs between them, and how to work with both.

Purpose: DX-04 (README dual-provider docs), DX-05 (CLAUDE.md Kinder architecture). Developers and AI assistants need accurate documentation to work confidently in the dual-provider environment.
Output: Updated README.md and CLAUDE.md reflecting the v1.1 architecture.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-developer-experience-documentation/15-RESEARCH.md
@.planning/phases/15-developer-experience-documentation/15-01-SUMMARY.md
@README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md with dual-provider documentation</name>
  <files>README.md</files>
  <action>
Surgical edits to extend the existing README structure. Do NOT rewrite from scratch -- preserve all existing content and extend where needed. Changes:

**1. Architecture section (lines 6-13):**
Update the architecture diagram to show Kinder as default with KIND as alternative:
```
Kinder or KIND multi-node (1 control-plane + 2 workers)
  → [Kinder: built-in MetalLB, Envoy GW controller, cert-manager]
  → [KIND: ArgoCD-managed MetalLB, Envoy GW controller, cert-manager]
    → Envoy Gateway DaemonSet + hostPort (ArgoCD-managed, both providers)
      → ArgoCD (App of Apps pattern, self-managing)
        → OpenClaw Gateway (StatefulSet, single replica, PVC-backed)
```

**2. Architecture -- sync wave table (lines 17-25):**
Add a note after the table: "When using Kinder (default), waves -5 (MetalLB), -4 (Envoy Gateway controller), and -2 (cert-manager) are skipped -- these components are provided by Kinder as built-in addons."

**3. Prerequisites section (lines 29-37):**
Replace the KIND-only prerequisite with a provider choice:
- [Kinder](https://kinder.patrykgolabek.dev/) (default) OR [KIND](https://kind.sigs.k8s.io/) v0.20+

Keep all other prerequisites unchanged.

**4. Bootstrap section (lines 53-58):**
Update to show provider selection:
```bash
make up                    # Bootstrap with Kinder (default)
make up PROVIDER=kind      # Bootstrap with KIND instead
```
Update the description to mention both providers.

**5. After the Bootstrap section, add a new "Provider Differences" subsection:**
```
### Provider Differences

| | Kinder (default) | KIND (opt-in) |
|---|---|---|
| MetalLB | Built-in addon | ArgoCD-managed |
| Envoy Gateway controller | Built-in addon | ArgoCD-managed |
| cert-manager | Built-in addon | ArgoCD-managed |
| Envoy DaemonSet + hostPort | ArgoCD-managed | ArgoCD-managed |
| Sealed Secrets | ArgoCD-managed | ArgoCD-managed |
| OpenClaw | ArgoCD-managed | ArgoCD-managed |
| Bootstrap steps | Fewer (skips addon infrastructure) | Full v1.0 flow |

Both providers produce identical cluster topology (1 control-plane + 2 workers) and OpenClaw is accessible at `http://localhost` after bootstrap.
```

**6. Repository Structure section (lines 142-177):**
Update `bootstrap/` to show the provider-specific directories:
```
├── bootstrap/
│   ├── kind/                        # KIND-specific ArgoCD Applications
│   │   ├── root-app.yaml            # Root Application (includes all infra)
│   │   ├── infra-*.yaml             # Infrastructure Applications
│   │   └── ...
│   ├── kinder/                      # Kinder-specific ArgoCD Applications
│   │   ├── root-app.yaml            # Root Application (excludes Kinder-provided infra)
│   │   ├── infra-*.yaml             # Kinder-compatible infrastructure Applications
│   │   └── ...
```
Update `cluster/` to show both config files:
```
├── cluster/
│   ├── kind-config.yaml             # KIND cluster definition (3 nodes)
│   └── kinder-config.yaml           # Kinder cluster definition (3 nodes + addons)
```
Update unit test count from "68 tests" to reflect current count.

**7. Makefile Targets table (lines 100-138):**
Add `make doctor` row in the Operations section: `make doctor` | Check cluster health for current provider
Update `make down` description from "Destroy the KIND cluster" to "Destroy the cluster"

**8. "What Doesn't Belong Here" section (line 244):**
Change "Production cloud manifests -- this is local-first on KIND" to "Production cloud manifests -- this is local-first on Kinder/KIND"

**9. CI & Guards section (line 209):**
Update the BATS test count from "73 tests" to reflect the current count after Phase 14 additions.
  </action>
  <verify>
1. `grep -c 'PROVIDER=kind' README.md` returns at least 1 (provider selection documented)
2. `grep -c 'Kinder' README.md` returns at least 3 (mentioned in multiple sections)
3. `grep 'bootstrap/kind' README.md` shows both provider directory structures
4. No content that was in the original README is lost (diff should show additions and targeted edits, not mass deletions)
  </verify>
  <done>
README.md documents Kinder as default provider, shows how to select KIND, includes a Provider Differences table, and reflects the updated repository structure with dual bootstrap directories.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md with Kinder architecture and provider details</name>
  <files>CLAUDE.md</files>
  <action>
Surgical edits to CLAUDE.md. This file is the AI-facing architecture reference -- precision matters. Do NOT rewrite from scratch. Changes:

**1. Project Identity paragraph (top section):**
After "running on **KIND (Kubernetes in Docker)** for local development" add mention of Kinder: "running on **Kinder** (default) or **KIND** (opt-in) for local development with production-fidelity networking"

**2. Architecture Overview diagram:**
Update to show dual provider paths:
```
Kinder or KIND multi-node (1 CP + 2 workers)
  → [Kinder: built-in MetalLB, Envoy GW controller, cert-manager]
  → [KIND: ArgoCD-managed MetalLB, Envoy GW controller, cert-manager]
    → Envoy Gateway (Gateway API via DaemonSet + hostPort, ArgoCD-managed)
      → ArgoCD (App of Apps pattern, self-managing)
        → OpenClaw Gateway (StatefulSet, single replica, PVC-backed)
```

**3. Add "Provider Selection" subsection after Architecture Overview:**
```
### Provider Selection

Kinder is the default provider. KIND is opt-in via `CLUSTER_PROVIDER=kind` or `make up PROVIDER=kind`.

**Kinder** provides MetalLB, Envoy Gateway controller, and cert-manager as built-in addons. These are NOT managed by ArgoCD -- they run as Kinder cluster extensions. ArgoCD still manages: Envoy Gateway DaemonSet + hostPort config, Sealed Secrets, and OpenClaw.

**KIND** uses the full v1.0 ArgoCD-managed infrastructure stack. All components (MetalLB, Envoy Gateway, Sealed Secrets, cert-manager, OpenClaw) are ArgoCD Applications deployed via sync waves.

Both providers use the same cluster topology (1 control-plane + 2 workers), same Docker network (`kind`), and same kubectl context prefix (`kind-`).
```

**4. Repository Structure -- update bootstrap/ section:**
Replace the flat `bootstrap/` listing with:
```
├── bootstrap/
│   ├── kind/                          # KIND provider: all ArgoCD Applications
│   │   ├── root-app.yaml             # Root Application scanning bootstrap/kind/
│   │   ├── argocd-cm.yaml            # ConfigMap (same as kinder, byte-identical)
│   │   ├── argocd-self.yaml          # ArgoCD self-management (same)
│   │   ├── projects/                 # AppProjects (same)
│   │   ├── infra-metallb.yaml        # MetalLB (KIND-only)
│   │   ├── infra-envoy-gateway.yaml  # Envoy GW controller (KIND-only)
│   │   ├── infra-cert-manager.yaml   # cert-manager (KIND-only)
│   │   ├── infra-envoy-gateway-config.yaml  # DaemonSet + hostPort config
│   │   ├── infra-sealed-secrets.yaml # Sealed Secrets
│   │   └── workload-openclaw.yaml    # OpenClaw
│   └── kinder/                        # Kinder provider: reduced ArgoCD Applications
│       ├── root-app.yaml             # Root Application scanning bootstrap/kinder/
│       ├── argocd-cm.yaml            # ConfigMap (byte-identical to kind/)
│       ├── argocd-self.yaml          # ArgoCD self-management (same)
│       ├── projects/                 # AppProjects (same)
│       ├── infra-envoy-gateway-config.yaml  # DaemonSet + hostPort config
│       ├── infra-sealed-secrets.yaml # Sealed Secrets
│       └── workload-openclaw.yaml    # OpenClaw
```

**5. Repository Structure -- update cluster/ section:**
Add `kinder-config.yaml` alongside `kind-config.yaml`.

**6. Sync Wave Ordering table:**
Add a "Provider" column or a note explaining which waves apply to which provider:
After the existing table, add:
"**Kinder path:** Waves -5 (MetalLB), -4 (Envoy Gateway controller), and -2 (cert-manager) are skipped. Kinder provides these as built-in addons. The effective wave order is: -10, -3, -1, +10."

**7. Conventions -- Manifests section:**
Add: "Bootstrap manifests are provider-specific: `bootstrap/kind/` for KIND, `bootstrap/kinder/` for Kinder. Shared files are byte-identical copies (not symlinks)."

**8. KIND Cluster Details section:**
Rename to "Cluster Details" and add Kinder info:
- Both providers use cluster name `openclaw-dev`
- Both use Docker network `kind` and kubectl context `kind-openclaw-dev`
- Kinder provides MetalLB, Envoy Gateway controller, cert-manager, Metrics Server as addons
- KIND requires ArgoCD to deploy all infrastructure

**9. Common Operations -- make help output:**
Add `make doctor` to the list:
```
make doctor                # Check cluster health for current provider
```
Also add the PROVIDER variable to make up:
```
make up                    # Bootstrap cluster (Kinder default)
make up PROVIDER=kind      # Bootstrap cluster with KIND
```

**10. Makefile variables section (add near Constants):**
Mention the provider variables:
```
CLUSTER_PROVIDER ?= kinder   # Provider selection (kinder default, kind opt-in)
PROVIDER_BIN     := $(CLUSTER_PROVIDER)
PROVIDER_CONFIG  := cluster/$(CLUSTER_PROVIDER)-config.yaml
```

**11. "What NOT to Do" section:**
Add: "Do not assume Kinder clusters have ArgoCD Applications for MetalLB, Envoy Gateway controller, or cert-manager -- these are Kinder addons, not ArgoCD-managed"
  </action>
  <verify>
1. `grep -c 'Kinder' CLAUDE.md` returns at least 10 (mentioned throughout)
2. `grep 'bootstrap/kind/' CLAUDE.md` shows both provider directories
3. `grep 'CLUSTER_PROVIDER' CLAUDE.md` returns at least 1 (provider variable documented)
4. `grep 'make doctor' CLAUDE.md` returns at least 1 (new target documented)
5. The file still contains all original sections (Architecture, Conventions, OpenClaw Specifics, etc.)
  </verify>
  <done>
CLAUDE.md reflects the dual-provider architecture with Kinder as default, documents provider selection, updated sync wave ordering, provider-specific bootstrap directories, and updated common operations.
  </done>
</task>

</tasks>

<verification>
1. README.md contains "PROVIDER=kind" showing how to select KIND
2. README.md contains "Provider Differences" table
3. CLAUDE.md architecture diagram shows both Kinder and KIND paths
4. CLAUDE.md documents bootstrap/kind/ and bootstrap/kinder/ directory structure
5. CLAUDE.md sync wave section notes Kinder-skipped waves
6. Neither file has massive content deletions -- changes are surgical extensions
</verification>

<success_criteria>
- DX-04: README.md documents dual-provider usage with clear provider selection, quick-start for both, and provider differences table
- DX-05: CLAUDE.md reflects updated architecture including Kinder as default provider, provider-specific directories, and adjusted sync wave ordering
</success_criteria>

<output>
After completion, create `.planning/phases/15-developer-experience-documentation/15-02-SUMMARY.md`
</output>
