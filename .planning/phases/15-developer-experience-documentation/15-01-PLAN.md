---
phase: 15-developer-experience-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - scripts/validate-manifests.sh
  - scripts/setup-mcp.sh
  - scripts/verify-networkpolicy.sh
  - tests/unit/validate-manifests.bats
  - tests/unit/setup-mcp.bats
  - tests/unit/verify-networkpolicy.bats
autonomous: true

must_haves:
  truths:
    - "`make doctor` shows provider name, binary checks, Docker status, cluster existence, and component health (ArgoCD, Envoy DaemonSet, Sealed Secrets, OpenClaw) when cluster is running"
    - "`make doctor` shows KIND-only components (MetalLB, cert-manager) only when CLUSTER_PROVIDER=kind"
    - "`make doctor` exits non-zero when any check fails"
    - "`make validate` validates both bootstrap/kind/ and bootstrap/kinder/ directories"
    - "`make setup-mcp` uses the active provider binary instead of hardcoded `kind`"
    - "`make verify-netpol` uses the active provider binary instead of hardcoded `kind`"
    - "All BATS tests pass after changes"
  artifacts:
    - path: "Makefile"
      provides: "Enhanced make doctor target with cluster component health checks"
      contains: "Cluster Components"
    - path: "scripts/validate-manifests.sh"
      provides: "Dual-directory bootstrap validation"
      contains: "bootstrap/kind"
    - path: "scripts/setup-mcp.sh"
      provides: "Provider-aware cluster check"
      contains: "CLUSTER_PROVIDER"
    - path: "scripts/verify-networkpolicy.sh"
      provides: "Provider-aware cluster check"
      contains: "CLUSTER_PROVIDER"
  key_links:
    - from: "Makefile doctor target"
      to: "kubectl get deploy/statefulset/daemonset"
      via: "jsonpath readiness queries"
      pattern: "kubectl get .* -o jsonpath"
    - from: "scripts/validate-manifests.sh"
      to: "bootstrap/kind/ and bootstrap/kinder/"
      via: "validate_raw calls"
      pattern: 'validate_raw "bootstrap/kind'
    - from: "scripts/setup-mcp.sh"
      to: "CLUSTER_PROVIDER variable"
      via: "provider binary substitution"
      pattern: "CLUSTER_PROVIDER.*kinder"
---

<objective>
Enhance `make doctor` with cluster component health checks, update validate-manifests.sh for dual-provider bootstrap directories, fix hardcoded `kind` binary in setup-mcp.sh and verify-networkpolicy.sh, and update all corresponding BATS tests.

Purpose: DX-03 (doctor health checks), DX-06 (CI validation for both providers), plus script fixes flagged in Phase 12 research as Phase 15 scope.
Output: Working tooling for dual-provider environment with passing test suite.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-developer-experience-documentation/15-RESEARCH.md
@Makefile
@scripts/validate-manifests.sh
@scripts/setup-mcp.sh
@scripts/verify-networkpolicy.sh
@tests/unit/validate-manifests.bats
@tests/unit/setup-mcp.bats
@tests/unit/verify-networkpolicy.bats
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance make doctor and update validate-manifests.sh</name>
  <files>Makefile, scripts/validate-manifests.sh</files>
  <action>
**Makefile -- enhance `doctor` target (lines 122-128):**

Replace the current 5-line doctor target with a comprehensive version that:

1. Prints provider name, binary check, Docker check, kubectl check, cluster existence check (same as current)
2. If cluster exists (the `$(PROVIDER_BIN) get clusters` grep succeeds), print a `--- Cluster Components ---` section:
   - ArgoCD: `kubectl get deploy argocd-server -n argocd -o jsonpath='{.status.readyReplicas}'` -- print "N ready" or "NOT FOUND"
   - Envoy DaemonSet: `kubectl get daemonset -n envoy-gateway-system -l gateway.envoyproxy.io/owning-gateway-name=eg -o name` -- print "running" if output contains "daemonset", else "NOT FOUND"
   - Sealed Secrets: `kubectl get deploy sealed-secrets-controller -n kube-system -o jsonpath='{.status.readyReplicas}'` -- print "N ready" or "NOT FOUND"
   - OpenClaw: `kubectl get statefulset openclaw-gateway -n openclaw -o jsonpath='{.status.readyReplicas}'` -- print "N ready" or "NOT FOUND"
3. If CLUSTER_PROVIDER is `kind`, also check:
   - MetalLB: `kubectl get deploy controller -n metallb-system -o jsonpath='{.status.readyReplicas}'` -- "N ready" or "NOT FOUND"
   - cert-manager: `kubectl get deploy cert-manager -n cert-manager -o jsonpath='{.status.readyReplicas}'` -- "N ready" or "NOT FOUND"
4. Track pass/fail count using shell variables. Print summary line: "N/M components healthy" or "N/M components healthy (M-N issues found)".
5. Exit non-zero if any component check fails (hybrid diagnostic + validation approach per research recommendation).
6. Add hint: "Run 'make status' for ArgoCD sync details."

Use the Makefile recipe pattern from research (shell if-blocks inside the recipe, using `@` prefix for quiet output). Each check should use `2>/dev/null` for kubectl calls to suppress connection errors when cluster is unreachable.

**scripts/validate-manifests.sh -- update bootstrap validation (line 86):**

Replace:
```
validate_raw "bootstrap/" "bootstrap"
```

With:
```
validate_raw "bootstrap/kind/" "bootstrap/kind"
validate_raw "bootstrap/kinder/" "bootstrap/kinder"
```

Also update the comment on line 85 to reflect dual-provider directories.
  </action>
  <verify>
1. `bash -n Makefile` -- no syntax errors (Makefile won't have bash -n, but check with `make -n doctor` for dry run)
2. `make doctor` shows "Provider: kinder" and all binary checks
3. `make validate` runs without errors (integration test if kubeconform available, else verify script parses correctly with `bash -n scripts/validate-manifests.sh`)
4. `grep -c 'bootstrap/kind' scripts/validate-manifests.sh` returns 2 (one for kind/, one for kinder/)
  </verify>
  <done>
`make doctor` prints provider info, binary checks, and cluster component health when cluster is running. `make validate` validates both bootstrap/kind/ and bootstrap/kinder/ directories.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix hardcoded kind in scripts and update all BATS tests</name>
  <files>scripts/setup-mcp.sh, scripts/verify-networkpolicy.sh, tests/unit/validate-manifests.bats, tests/unit/setup-mcp.bats, tests/unit/verify-networkpolicy.bats</files>
  <action>
**scripts/setup-mcp.sh -- provider-aware cluster check:**

1. After `source "${SCRIPT_DIR}/lib/common.sh"` (line 29), add: `CLUSTER_PROVIDER="${CLUSTER_PROVIDER:-kinder}"`
2. Line 69: Change `"Verifying KIND cluster"` to `"Verifying cluster '${CLUSTER_NAME}' is running (provider: ${CLUSTER_PROVIDER})..."`
3. Line 70: Replace `kind get clusters` with `${CLUSTER_PROVIDER} get clusters`
4. Line 71: Change `"KIND cluster '${CLUSTER_NAME}' is not running."` to `"Cluster '${CLUSTER_NAME}' is not running (provider: ${CLUSTER_PROVIDER})."`
5. Line 79 context check: Keep `kind-${CLUSTER_NAME}` -- both providers use `kind-` prefix per research finding.
6. Update the script header comment (lines 5-6) to say "cluster" instead of "KIND cluster".

**scripts/verify-networkpolicy.sh -- provider-aware cluster check:**

1. After `source "${SCRIPT_DIR}/lib/common.sh"` (line 23), add: `CLUSTER_PROVIDER="${CLUSTER_PROVIDER:-kinder}"`
2. Also add: `CLUSTER_NAME="${CLUSTER_NAME:-openclaw-dev}"` (the existing code hardcodes "openclaw-dev" on line 59 instead of using CLUSTER_NAME from common.sh -- fix this too).
3. Line 59: Replace `kind get clusters 2>/dev/null | grep -q "^openclaw-dev$"` with `${CLUSTER_PROVIDER} get clusters 2>/dev/null | grep -q "^${CLUSTER_NAME}$"`
4. Line 60: Change error message from `"KIND cluster 'openclaw-dev' not found"` to `"Cluster '${CLUSTER_NAME}' not found (provider: ${CLUSTER_PROVIDER})"`
5. Update the script header (line 14) dependencies comment to say "kinder or kind" instead of just "kind".

**tests/unit/validate-manifests.bats -- update for dual-directory validation:**

1. Test "validate_raw: PASS when kubeconform succeeds" (line 73): Change `assert_output --partial "PASS: bootstrap"` to `assert_output --partial "PASS: bootstrap/kind"` AND add `assert_output --partial "PASS: bootstrap/kinder"`
2. Test "validate_kustomize: FAIL when pipeline fails" (line 81): Change `assert_output --partial "PASS: bootstrap"` to `assert_output --partial "PASS: bootstrap/kind"`
3. Test "EXIT_CODE: first passes, second fails" (line 94): The `_mock_kubeconform_fail_on_call 2` call needs updating because now there are 2 validate_raw calls (kind + kinder) before kustomize calls. Call 2 will now hit bootstrap/kinder. Update to `_mock_kubeconform_fail_on_call 3` to fail on the first kustomize call (openclaw/dev), AND update assertions: change `assert_output --partial "PASS: bootstrap"` to `assert_output --partial "PASS: bootstrap/kind"`, add `assert_output --partial "PASS: bootstrap/kinder"`, keep `assert_output --partial "FAIL: openclaw/dev"`.
4. Test "kubectl kustomize failure propagates" (line 108): Change `assert_output --partial "PASS: bootstrap"` to `assert_output --partial "PASS: bootstrap/kind"`
5. Test "all validations pass" (line 121): Change `assert_output --partial "PASS: bootstrap"` to TWO assertions: `assert_output --partial "PASS: bootstrap/kind"` and `assert_output --partial "PASS: bootstrap/kinder"`

**tests/unit/setup-mcp.bats -- pin existing tests to KIND and add kinder tests:**

1. Pin all existing tests that create a `kind` mock to use `CLUSTER_PROVIDER=kind`. Add `export CLUSTER_PROVIDER=kind` inside `run bash -c` blocks OR at the top of each test before the `run` call. Follow the pattern from Phase 14: add `export CLUSTER_PROVIDER=kind` to test setup where `kind` binary is mocked.
2. In test "exits 1 when KIND cluster not found" (line 26): Add `export CLUSTER_PROVIDER=kind` before the run, AND rename mock from `kind` to match. Also rename test to include "(KIND)" in name.
3. Add new test: "setup-mcp: exits 1 when kinder cluster not found (kinder)" -- same pattern but with `create_mock "kinder" 0` and `export CLUSTER_PROVIDER=kinder`.
4. For all other tests that mock `kind`, pin them with `export CLUSTER_PROVIDER=kind` before the `run` call (leave the `create_conditional_mock "kind"` unchanged since the binary name will match).
5. Add new test: "setup-mcp: full success path works with kinder provider" -- clone of the success test but with `CLUSTER_PROVIDER=kinder` and `create_conditional_mock "kinder"` instead of `kind`.

**tests/unit/verify-networkpolicy.bats -- pin existing tests to KIND and add kinder tests:**

1. Pin all tests that mock `kind` to `CLUSTER_PROVIDER=kind`. Add `export CLUSTER_PROVIDER=kind` before the `run` call in each test.
2. Update test "exits 1 when KIND cluster not found" to include `export CLUSTER_PROVIDER=kind`.
3. Also update the assertion: the error message now says "Cluster 'openclaw-dev' not found (provider: kind)" instead of "KIND cluster 'openclaw-dev' not found". Update `assert_output --partial` accordingly.
4. Add new test: "verify-networkpolicy: exits 1 when kinder cluster not found" -- same pattern with `CLUSTER_PROVIDER=kinder` and `create_mock "kinder" 0`.
5. Add new test: "verify-networkpolicy: all 4 tests pass with kinder provider" -- clone of `_mock_all_passing` test but with `CLUSTER_PROVIDER=kinder` and `create_conditional_mock "kinder"` instead of `kind`.
  </action>
  <verify>
1. `bash -n scripts/setup-mcp.sh` -- no syntax errors
2. `bash -n scripts/verify-networkpolicy.sh` -- no syntax errors
3. `make test-unit` -- all tests pass
4. `grep 'kind get clusters' scripts/setup-mcp.sh scripts/verify-networkpolicy.sh` -- returns NO matches (hardcoded `kind` removed)
  </verify>
  <done>
setup-mcp.sh and verify-networkpolicy.sh use `${CLUSTER_PROVIDER}` instead of hardcoded `kind`. All BATS tests pass, with existing tests pinned to CLUSTER_PROVIDER=kind and new tests covering kinder paths.
  </done>
</task>

</tasks>

<verification>
1. `make test-unit` passes all tests (including new provider-pinned tests)
2. `make validate` passes (validates both bootstrap/kind/ and bootstrap/kinder/)
3. `grep -rn 'kind get clusters' scripts/` returns 0 matches -- no hardcoded kind binary calls remain
4. `make doctor` displays provider, binary checks, and "Cluster Components" section header
5. `make doctor CLUSTER_PROVIDER=kind` includes MetalLB and cert-manager checks
</verification>

<success_criteria>
- DX-03: `make doctor` validates cluster health for the current provider with component-level checks
- DX-06: validate-manifests.sh validates both bootstrap/kind/ and bootstrap/kinder/ directories
- All hardcoded `kind` binary references removed from setup-mcp.sh and verify-networkpolicy.sh
- BATS test suite passes with provider-pinned tests and new kinder coverage
</success_criteria>

<output>
After completion, create `.planning/phases/15-developer-experience-documentation/15-01-SUMMARY.md`
</output>
