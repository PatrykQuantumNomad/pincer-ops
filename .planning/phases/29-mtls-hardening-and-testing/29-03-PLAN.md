---
phase: 29-mtls-hardening-and-testing
plan: 03
type: execute
wave: 2
depends_on: [29-01, 29-02]
files_modified:
  - tests/unit/openshell-manifests.bats
  - tests/unit/bootstrap.bats
  - tests/unit/validate-manifests.bats
autonomous: true
requirements: [TEST-01, TEST-03, TEST-04, TEST-05]

must_haves:
  truths:
    - "BATS tests verify TLS Certificate CR manifests exist with correct issuerRef and secretName"
    - "BATS tests verify OPENSHELL_DISABLE_TLS is NOT present in gateway StatefulSet"
    - "BATS tests verify TLS env vars (OPENSHELL_TLS_CERT, OPENSHELL_TLS_KEY, OPENSHELL_TLS_CLIENT_CA) are present"
    - "BATS tests verify TLS volume mounts (tls-cert, tls-client-ca) are present in StatefulSet"
    - "BATS tests verify SealedSecret for SSH handshake exists"
    - "BATS tests verify SSH port 2222 NetworkPolicy ingress rule exists"
    - "BATS tests verify bootstrap.sh generate_tls_artifacts is active (not placeholder)"
    - "BATS tests verify dual-provider byte-identical shared files"
    - "make test passes with all new and existing tests"
  artifacts:
    - path: "tests/unit/openshell-manifests.bats"
      provides: "Structural tests for SEC-01 through SEC-04"
      contains: "OPENSHELL_TLS_CERT"
    - path: "tests/unit/bootstrap.bats"
      provides: "Updated shared files list for provider parity"
      contains: "shared_files"
    - path: "tests/unit/validate-manifests.bats"
      provides: "Updated validation target assertions for local schema"
      contains: "SCHEMA_LOCATION_LOCAL"
  key_links:
    - from: "tests/unit/openshell-manifests.bats"
      to: "infrastructure/openshell/gateway/statefulset.yaml"
      via: "grep assertions on TLS env vars and volume mounts"
      pattern: "OPENSHELL_TLS_CERT"
    - from: "tests/unit/openshell-manifests.bats"
      to: "infrastructure/openshell/gateway/certificate-ca.yaml"
      via: "grep assertions on cert-manager Certificate fields"
      pattern: "openshell-ca"
    - from: "tests/unit/openshell-manifests.bats"
      to: "workloads/openclaw-sandbox/base/networkpolicy.yaml"
      via: "grep assertions on SSH port 2222 rule"
      pattern: "port: 2222"
---

<objective>
Add BATS structural tests for all Phase 29 security hardening changes and update bootstrap/validation test files for dual-provider compatibility.

Purpose: Ensure all mTLS, SealedSecret, and NetworkPolicy changes from Plan 01 and the kubeconform schema from Plan 02 are covered by structural tests. This is the final testing plan that closes all TEST-* requirements for v2.0.
Output: Extended openshell-manifests.bats, updated bootstrap.bats and validate-manifests.bats.
</objective>

<execution_context>
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-mtls-hardening-and-testing/29-RESEARCH.md
@.planning/phases/29-mtls-hardening-and-testing/29-01-SUMMARY.md
@.planning/phases/29-mtls-hardening-and-testing/29-02-SUMMARY.md

@tests/unit/openshell-manifests.bats
@tests/unit/bootstrap.bats
@tests/unit/validate-manifests.bats

<interfaces>
<!-- Files created by Plan 01 that tests must validate. -->

Plan 01 creates/modifies:
- infrastructure/openshell/gateway/certificate-ca.yaml (isCA: true, namespace: cert-manager, issuerRef: selfsigned-issuer)
- infrastructure/openshell/gateway/clusterissuer-ca.yaml (name: openshell-ca-issuer, secretName: openshell-ca-tls)
- infrastructure/openshell/gateway/certificate-server.yaml (name: openshell-server-tls, namespace: openshell, issuerRef: openshell-ca-issuer)
- infrastructure/openshell/gateway/certificate-client.yaml (name: openshell-client-tls, namespace: openshell, issuerRef: openshell-ca-issuer)
- infrastructure/openshell/gateway/sealedsecret-ssh.yaml (kind: SealedSecret, name: openshell-ssh-handshake)
- infrastructure/openshell/gateway/kustomization.yaml (12 resources total)
- infrastructure/openshell/gateway/statefulset.yaml (OPENSHELL_TLS_CERT, no OPENSHELL_DISABLE_TLS, https:// endpoint, secretKeyRef for SSH)
- workloads/openclaw-sandbox/base/networkpolicy.yaml (3 policies: deny-all, allow, ssh-only with port 2222)
- scripts/bootstrap.sh (generate_tls_artifacts active, kubectl wait for certificates)

Plan 02 creates/modifies:
- schemas/agents.x-k8s.io/sandbox_v1alpha1.json
- scripts/validate-manifests.sh (SCHEMA_LOCATION_LOCAL added)

Existing test patterns (from openshell-manifests.bats):
- Tests use `grep` assertions against static YAML files
- Tests are grouped by requirement ID sections (e.g., "# SAND-07")
- Provider parity tests use `diff` between kind/ and kinder/ directories
- Existing egress count test: "sandbox NetworkPolicy has exactly 3 egress destinations"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BATS structural tests for SEC-01 through SEC-04</name>
  <files>tests/unit/openshell-manifests.bats</files>
  <action>
    Extend openshell-manifests.bats with new test sections. Follow the existing pattern of `@test "description" { run grep ...; assert_success }`.

    **Replace the existing SAND-07 section** (3 tests for TLS-disabled mode) with new SEC-01 tests. The old tests assert OPENSHELL_DISABLE_TLS exists -- those must become negative assertions or be replaced entirely.

    1. **Remove/replace these existing tests** (SAND-07 section, lines ~382-398):
       - "gateway StatefulSet has OPENSHELL_DISABLE_TLS env" -- replace with negative assertion
       - "gateway StatefulSet has OPENSHELL_DISABLE_GATEWAY_AUTH env" -- replace with negative assertion
       - Keep "gateway StatefulSet has OPENSHELL_SSH_HANDSHAKE_SECRET env" (still true, just sourced differently)

    2. **Add section: "# mTLS Certificate Chain (SEC-01)"** with tests:
       - "CA Certificate has isCA true" -- grep `isCA: true` in certificate-ca.yaml
       - "CA Certificate is in cert-manager namespace" -- grep `namespace: cert-manager` in certificate-ca.yaml
       - "CA Certificate references selfsigned-issuer" -- grep `selfsigned-issuer` in certificate-ca.yaml
       - "CA Certificate has secretName openshell-ca-tls" -- grep `secretName: openshell-ca-tls` in certificate-ca.yaml
       - "CA ClusterIssuer references openshell-ca-tls" -- grep `openshell-ca-tls` in clusterissuer-ca.yaml
       - "CA ClusterIssuer has name openshell-ca-issuer" -- grep `openshell-ca-issuer` in clusterissuer-ca.yaml
       - "server Certificate has secretName openshell-server-tls" -- grep `secretName: openshell-server-tls` in certificate-server.yaml
       - "server Certificate is in openshell namespace" -- grep `namespace: openshell` in certificate-server.yaml
       - "server Certificate references openshell-ca-issuer" -- grep `openshell-ca-issuer` in certificate-server.yaml
       - "server Certificate has server auth usage" -- grep `server auth` in certificate-server.yaml
       - "client Certificate has secretName openshell-client-tls" -- grep `secretName: openshell-client-tls` in certificate-client.yaml
       - "client Certificate references openshell-ca-issuer" -- grep `openshell-ca-issuer` in certificate-client.yaml
       - "client Certificate has client auth usage" -- grep `client auth` in certificate-client.yaml

    3. **Add section: "# Gateway mTLS Configuration (SEC-01)"** with tests:
       - "gateway StatefulSet does NOT have OPENSHELL_DISABLE_TLS" -- grep OPENSHELL_DISABLE_TLS; assert_failure
       - "gateway StatefulSet does NOT have OPENSHELL_DISABLE_GATEWAY_AUTH" -- grep OPENSHELL_DISABLE_GATEWAY_AUTH; assert_failure
       - "gateway StatefulSet has OPENSHELL_TLS_CERT env" -- grep OPENSHELL_TLS_CERT; assert_success
       - "gateway StatefulSet has OPENSHELL_TLS_KEY env" -- grep OPENSHELL_TLS_KEY; assert_success
       - "gateway StatefulSet has OPENSHELL_TLS_CLIENT_CA env" -- grep OPENSHELL_TLS_CLIENT_CA; assert_success
       - "gateway StatefulSet has OPENSHELL_CLIENT_TLS_SECRET_NAME env" -- grep OPENSHELL_CLIENT_TLS_SECRET_NAME; assert_success
       - "gateway StatefulSet has tls-cert volume mount" -- grep 'name: tls-cert'; assert_success
       - "gateway StatefulSet has tls-client-ca volume mount" -- grep 'name: tls-client-ca'; assert_success
       - "gateway StatefulSet mounts TLS server cert at /etc/openshell-tls/server" -- grep '/etc/openshell-tls/server'; assert_success
       - "gateway StatefulSet mounts TLS client CA at /etc/openshell-tls/client-ca" -- grep '/etc/openshell-tls/client-ca'; assert_success
       - "gateway StatefulSet uses https endpoint" -- grep 'https://openshell'; assert_success
       - "gateway StatefulSet has OPENSHELL_SSH_HANDSHAKE_SECRET env" -- grep OPENSHELL_SSH_HANDSHAKE_SECRET; assert_success (keep existing)

    4. **Add section: "# SSH Handshake SealedSecret (SEC-02)"** with tests:
       - "SSH handshake SealedSecret exists" -- test -f sealedsecret-ssh.yaml; assert_success
       - "SSH handshake SealedSecret has correct kind" -- grep 'kind: SealedSecret' in sealedsecret-ssh.yaml; assert_success
       - "SSH handshake SealedSecret has correct name" -- grep 'name: openshell-ssh-handshake' in sealedsecret-ssh.yaml; assert_success
       - "SSH handshake SealedSecret is in openshell namespace" -- grep 'namespace: openshell' in sealedsecret-ssh.yaml; assert_success
       - "gateway StatefulSet references SSH handshake via secretKeyRef" -- grep 'secretKeyRef' in statefulset.yaml; assert_success
       - "gateway StatefulSet does NOT have hardcoded SSH placeholder" -- grep 'dev-placeholder' in statefulset.yaml; assert_failure

    5. **Add section: "# SSH NetworkPolicy (SEC-04)"** with tests:
       - "sandbox NetworkPolicy has openclaw-ssh-only" -- grep 'name: openclaw-ssh-only' in networkpolicy.yaml; assert_success
       - "sandbox SSH NetworkPolicy allows port 2222" -- grep 'port: 2222' in networkpolicy.yaml; assert_success
       - "sandbox SSH NetworkPolicy restricts to openshell gateway pod" -- grep 'app.kubernetes.io/name: openshell' in networkpolicy.yaml; assert_success

    6. **Update the existing egress count test** "sandbox NetworkPolicy has exactly 3 egress destinations":
       - This test should still pass -- the new openclaw-ssh-only policy is Ingress-only and does not add egress rules. The egress count in openclaw-allow remains 3. Keep the test unchanged.

    7. **Add section: "# Gateway Kustomization (SEC-01 additions)"** with tests:
       - "gateway kustomization lists certificate-ca.yaml" -- grep 'certificate-ca.yaml' in kustomization.yaml; assert_success
       - "gateway kustomization lists clusterissuer-ca.yaml" -- grep 'clusterissuer-ca.yaml' in kustomization.yaml; assert_success
       - "gateway kustomization lists certificate-server.yaml" -- grep 'certificate-server.yaml' in kustomization.yaml; assert_success
       - "gateway kustomization lists certificate-client.yaml" -- grep 'certificate-client.yaml' in kustomization.yaml; assert_success
       - "gateway kustomization lists sealedsecret-ssh.yaml" -- grep 'sealedsecret-ssh.yaml' in kustomization.yaml; assert_success
  </action>
  <verify>
    <automated>bats tests/unit/openshell-manifests.bats 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All new BATS tests pass (approximately 40+ new tests for SEC-01 through SEC-04)
    - Old SAND-07 tests for TLS-disabled mode replaced with new SEC-01 negative assertions
    - Existing tests for SAND-04, SAND-05, SAND-06, MIGR-*, SUPV-*, INFER-* still pass
    - Full openshell-manifests.bats suite green
  </done>
</task>

<task type="auto">
  <name>Task 2: Update bootstrap.bats and validate-manifests.bats for dual-provider parity</name>
  <files>
    tests/unit/bootstrap.bats
    tests/unit/validate-manifests.bats
  </files>
  <action>
    1. **bootstrap.bats** (TEST-05): No bootstrap file count changes needed (no new ArgoCD Application files were added -- cert-manager Certificates, ClusterIssuer, and SealedSecret are all in infrastructure/openshell/gateway/ which the existing workload-openshell-gateway Application already manages). However, verify the following:
       - Confirm `workload-openshell-gateway.yaml` is NOT already in the shared_files array. If it is not there but is byte-identical across providers, add it to the shared_files array for completeness.
       - The KIND expected count stays at 15 and Kinder at 12 (no new bootstrap/*.yaml files).
       - If any Plan 01 changes affected bootstrap/ files that need provider parity verification, add those to the shared_files array.

    2. **bootstrap.bats** (TEST-04): Add a test for bootstrap.sh generate_tls_artifacts activation:
       - "bootstrap.sh generate_tls_artifacts is active" -- grep 'kubectl wait.*certificate' in bootstrap.sh; assert_success
       - "bootstrap.sh generate_tls_artifacts is not placeholder" -- grep 'Phase 29 activates this' in bootstrap.sh; assert_failure

    3. **validate-manifests.bats** (TEST-02): Update validation target assertions if needed:
       - Check if there is an existing test that counts validation targets or lists them. If so, update to reflect the SCHEMA_LOCATION_LOCAL addition.
       - Add a test: "validate-manifests.sh includes local schema location" -- grep 'SCHEMA_LOCATION_LOCAL' in validate-manifests.sh; assert_success

    4. Run `make test` to verify the full test suite passes (all unit + integration tests).
  </action>
  <verify>
    <automated>bats tests/unit/bootstrap.bats tests/unit/validate-manifests.bats 2>&1 | tail -5</automated>
  </verify>
  <done>
    - bootstrap.bats tests pass with correct file counts and shared file list
    - bootstrap.sh TLS activation verified by test (not placeholder)
    - validate-manifests.bats includes test for SCHEMA_LOCATION_LOCAL
    - `make test` passes with all unit and integration tests green
  </done>
</task>

</tasks>

<verification>
- `bats tests/unit/openshell-manifests.bats` -- all tests pass
- `bats tests/unit/bootstrap.bats` -- all tests pass
- `bats tests/unit/validate-manifests.bats` -- all tests pass
- `make test` -- full suite green
- `make check` -- validate + test both pass
</verification>

<success_criteria>
- All SEC-01 through SEC-04 changes have BATS structural tests
- TLS Certificate manifests, StatefulSet modifications, SealedSecret, and NetworkPolicy SSH rule all tested
- Old TLS-disabled tests replaced with TLS-enabled assertions
- Dual-provider parity verified via shared_files diff tests
- bootstrap.sh TLS activation tested (not placeholder)
- validate-manifests.sh local schema integration tested
- Full `make check` passes
</success_criteria>

<output>
After completion, create `.planning/phases/29-mtls-hardening-and-testing/29-03-SUMMARY.md`
</output>
