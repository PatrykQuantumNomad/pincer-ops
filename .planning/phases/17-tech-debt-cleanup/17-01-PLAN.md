---
phase: 17-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - .planning/REQUIREMENTS.md
  - bootstrap/kind/infra-envoy-gateway-config.yaml
  - bootstrap/kinder/infra-envoy-gateway-config.yaml
  - CLAUDE.md
autonomous: true

must_haves:
  truths:
    - "DX-04 and DX-05 checkboxes in REQUIREMENTS.md are checked [x]"
    - "Traceability table shows DX-04 and DX-05 as Complete, not Pending"
    - "Makefile setup-mcp and verify-netpol targets propagate CLUSTER_PROVIDER"
    - "Stale wave -4 dependency comment replaced with provider-neutral comment in both provider copies"
    - "Both copies of infra-envoy-gateway-config.yaml remain byte-identical"
    - "Test count in CLAUDE.md matches actual BATS test count (105 unit + 10 integration = 115)"
  artifacts:
    - path: "Makefile"
      provides: "CLUSTER_PROVIDER propagation for setup-mcp and verify-netpol"
      contains: "CLUSTER_PROVIDER=$(CLUSTER_PROVIDER) ./scripts/setup-mcp.sh"
    - path: "Makefile"
      provides: "CLUSTER_PROVIDER propagation for verify-netpol"
      contains: "CLUSTER_PROVIDER=$(CLUSTER_PROVIDER) ./scripts/verify-networkpolicy.sh"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DX-04 and DX-05 marked complete"
      contains: "[x] **DX-04**"
    - path: "bootstrap/kind/infra-envoy-gateway-config.yaml"
      provides: "Provider-neutral CRD comment"
      contains: "CRDs are provider-managed"
    - path: "bootstrap/kinder/infra-envoy-gateway-config.yaml"
      provides: "Provider-neutral CRD comment (byte-identical to kind copy)"
      contains: "CRDs are provider-managed"
    - path: "CLAUDE.md"
      provides: "Correct test count"
      contains: "105 unit tests across 9 files"
  key_links:
    - from: "bootstrap/kind/infra-envoy-gateway-config.yaml"
      to: "bootstrap/kinder/infra-envoy-gateway-config.yaml"
      via: "byte-identity constraint"
      pattern: "diff.*bootstrap/kind.*bootstrap/kinder"
---

<objective>
Fix 4 simple tech debt items from the v1.1 milestone audit: Makefile environment propagation for setup-mcp and verify-netpol targets, REQUIREMENTS.md checkbox updates for DX-04/DX-05, stale comment rewrite in both copies of infra-envoy-gateway-config.yaml, and test count correction in CLAUDE.md.

Purpose: Close cosmetic and tracking tech debt before milestone completion.
Output: 5 files updated with no functional regressions.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/17-tech-debt-cleanup/17-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Makefile env propagation + REQUIREMENTS.md checkboxes + test count</name>
  <files>
    Makefile
    .planning/REQUIREMENTS.md
    CLAUDE.md
  </files>
  <action>
    **Makefile (2 lines):**
    Change the `setup-mcp` target recipe from:
    `@./scripts/setup-mcp.sh`
    to:
    `@CLUSTER_PROVIDER=$(CLUSTER_PROVIDER) ./scripts/setup-mcp.sh`

    Change the `verify-netpol` target recipe from:
    `@./scripts/verify-networkpolicy.sh`
    to:
    `@CLUSTER_PROVIDER=$(CLUSTER_PROVIDER) ./scripts/verify-networkpolicy.sh`

    This matches the existing pattern used by `bootstrap`, `up-verbose`, `down`, and `clean` targets.

    **REQUIREMENTS.md (4 lines):**
    Line 43: Change `- [ ] **DX-04**:` to `- [x] **DX-04**:`
    Line 44: Change `- [ ] **DX-05**:` to `- [x] **DX-05**:`
    Line 90: Change `| DX-04 | Phase 15 | Pending |` to `| DX-04 | Phase 15 | Complete |`
    Line 91: Change `| DX-05 | Phase 15 | Pending |` to `| DX-05 | Phase 15 | Complete |`

    **CLAUDE.md (2 lines):**
    First, verify actual test count by running: `grep -c '@test' tests/unit/*.bats | awk -F: '{s+=$2} END{print s}'` and `grep -c '@test' tests/integration/*.bats | awk -F: '{s+=$2} END{print s}'`

    The actual count is 105 unit + 10 integration = 115 total.

    Line 116: Change `# 106 unit tests across 9 files` to `# 105 unit tests across 9 files`
    Line 225: Change `# Run all 116 BATS tests` to `# Run all 115 BATS tests`
  </action>
  <verify>
    1. `grep 'CLUSTER_PROVIDER=\$(CLUSTER_PROVIDER).*setup-mcp' Makefile` -- matches
    2. `grep 'CLUSTER_PROVIDER=\$(CLUSTER_PROVIDER).*verify-networkpolicy' Makefile` -- matches
    3. `grep '\[x\] \*\*DX-04\*\*' .planning/REQUIREMENTS.md` -- matches
    4. `grep '\[x\] \*\*DX-05\*\*' .planning/REQUIREMENTS.md` -- matches
    5. `grep 'DX-04.*Complete' .planning/REQUIREMENTS.md` -- matches
    6. `grep 'DX-05.*Complete' .planning/REQUIREMENTS.md` -- matches
    7. `grep '105 unit tests' CLAUDE.md` -- matches
    8. `grep '115 BATS tests' CLAUDE.md` -- matches
  </verify>
  <done>
    Makefile setup-mcp and verify-netpol targets include CLUSTER_PROVIDER prefix. DX-04 and DX-05 show [x] and Complete. CLAUDE.md test counts match actual (115 total, 105 unit, 10 integration).
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite stale comment in both infra-envoy-gateway-config.yaml copies</name>
  <files>
    bootstrap/kind/infra-envoy-gateway-config.yaml
    bootstrap/kinder/infra-envoy-gateway-config.yaml
  </files>
  <action>
    In BOTH `bootstrap/kind/infra-envoy-gateway-config.yaml` and `bootstrap/kinder/infra-envoy-gateway-config.yaml`, replace lines 7-9:

    ```
    # Requires Envoy Gateway CRDs (EnvoyProxy, Gateway API kinds) to be present.
    # KIND: CRDs installed by infra-envoy-gateway (wave -4).
    # Kinder: CRDs installed by Kinder addon during cluster creation.
    ```

    With provider-neutral text:

    ```
    # Requires Envoy Gateway CRDs (EnvoyProxy, Gateway API kinds) to be present.
    # CRDs are provider-managed: Kinder provides them as a built-in addon;
    # KIND installs them via the infra-envoy-gateway ArgoCD Application.
    ```

    CRITICAL: Both files must end up byte-identical. Apply the exact same change to both files. Do NOT modify any line below the comment block (the YAML spec starting at `apiVersion:`).

    After editing, verify byte-identity: `diff bootstrap/kind/infra-envoy-gateway-config.yaml bootstrap/kinder/infra-envoy-gateway-config.yaml`
  </action>
  <verify>
    1. `diff bootstrap/kind/infra-envoy-gateway-config.yaml bootstrap/kinder/infra-envoy-gateway-config.yaml` -- no output (byte-identical)
    2. `grep -c 'wave -4' bootstrap/kind/infra-envoy-gateway-config.yaml` -- returns 0 (stale reference removed)
    3. `grep 'CRDs are provider-managed' bootstrap/kind/infra-envoy-gateway-config.yaml` -- matches
    4. `bats tests/unit/bootstrap.bats --filter "shared files are identical"` -- passes
  </verify>
  <done>
    Both copies of infra-envoy-gateway-config.yaml have the provider-neutral comment. No reference to "wave -4" in the comment block. Files remain byte-identical per diff and BATS test.
  </done>
</task>

</tasks>

<verification>
1. `make check` passes (full validate + test suite)
2. All 8 grep checks from Task 1 verify pass
3. `diff` confirms byte-identity for shared files
4. No other files accidentally modified (git diff --stat shows only the 5 expected files)
</verification>

<success_criteria>
- Makefile propagates CLUSTER_PROVIDER to setup-mcp and verify-netpol
- REQUIREMENTS.md DX-04 and DX-05 checked and Complete
- Stale comment replaced in both copies, byte-identity preserved
- CLAUDE.md test count matches reality (115 total)
- `make check` green
</success_criteria>

<output>
After completion, create `.planning/phases/17-tech-debt-cleanup/17-01-SUMMARY.md`
</output>
