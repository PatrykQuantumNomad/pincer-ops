---
phase: 08-reproducibility-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bootstrap/root-app.yaml
  - bootstrap/argocd-self.yaml
  - bootstrap/infra-metallb.yaml
  - bootstrap/infra-envoy-gateway-config.yaml
  - bootstrap/infra-sealed-secrets.yaml
  - bootstrap/infra-cert-manager.yaml
  - bootstrap/workload-openclaw.yaml
  - bootstrap/projects/infrastructure.yaml
  - bootstrap/projects/workloads.yaml
  - scripts/bootstrap.sh
autonomous: true
requirements:
  - GOPS-06

must_haves:
  truths:
    - "All 9 manifest files reference the real GitHub repoURL (PatrykQuantumNomad/pincer-ops.git)"
    - "No file in the repository contains the placeholder OWNER/pincer-ops.git"
    - "bootstrap.sh fallback messages reference 'repo unreachable' instead of 'placeholder URL'"
    - "Changes are committed and pushed to origin/main so ArgoCD can sync from remote"
  artifacts:
    - path: "bootstrap/root-app.yaml"
      provides: "Root Application with real repoURL"
      contains: "PatrykQuantumNomad/pincer-ops.git"
    - path: "bootstrap/argocd-self.yaml"
      provides: "ArgoCD self-management Application with real repoURL"
      contains: "PatrykQuantumNomad/pincer-ops.git"
    - path: "bootstrap/projects/infrastructure.yaml"
      provides: "Infrastructure AppProject with real sourceRepos"
      contains: "PatrykQuantumNomad/pincer-ops.git"
    - path: "bootstrap/projects/workloads.yaml"
      provides: "Workloads AppProject with real sourceRepos"
      contains: "PatrykQuantumNomad/pincer-ops.git"
    - path: "scripts/bootstrap.sh"
      provides: "Updated fallback warning messages"
      contains: "repo unreachable"
  key_links:
    - from: "bootstrap/root-app.yaml"
      to: "https://github.com/PatrykQuantumNomad/pincer-ops.git"
      via: "spec.source.repoURL"
      pattern: "repoURL.*PatrykQuantumNomad"
    - from: "bootstrap/projects/infrastructure.yaml"
      to: "https://github.com/PatrykQuantumNomad/pincer-ops.git"
      via: "spec.sourceRepos"
      pattern: "PatrykQuantumNomad/pincer-ops"
---

<objective>
Replace the placeholder repoURL (OWNER/pincer-ops.git) with the real GitHub URL (PatrykQuantumNomad/pincer-ops.git) across all 9 ArgoCD manifest files, update bootstrap.sh fallback warning messages, and push changes to origin/main so ArgoCD can sync from Git.

Purpose: This is the prerequisite for Phase 8 verification. ArgoCD cannot sync from Git while the placeholder URL is in place -- all Applications show ComparisonError and bootstrap.sh falls back to direct kustomize apply. With the real URL, ArgoCD becomes the actual deployment engine (the core GitOps contract).

Output: All manifest files pointing to the real GitHub repo, committed and pushed to main.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-reproducibility-verification/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace placeholder repoURL in all manifests and update fallback messages</name>
  <files>
    bootstrap/root-app.yaml
    bootstrap/argocd-self.yaml
    bootstrap/infra-metallb.yaml
    bootstrap/infra-envoy-gateway-config.yaml
    bootstrap/infra-sealed-secrets.yaml
    bootstrap/infra-cert-manager.yaml
    bootstrap/workload-openclaw.yaml
    bootstrap/projects/infrastructure.yaml
    bootstrap/projects/workloads.yaml
    scripts/bootstrap.sh
  </files>
  <action>
    1. In each of the 7 ArgoCD Application YAML files (root-app.yaml, argocd-self.yaml, infra-metallb.yaml, infra-envoy-gateway-config.yaml, infra-sealed-secrets.yaml, infra-cert-manager.yaml, workload-openclaw.yaml), replace:
       `repoURL: https://github.com/OWNER/pincer-ops.git`
       with:
       `repoURL: https://github.com/PatrykQuantumNomad/pincer-ops.git`

    2. In the 2 AppProject files (bootstrap/projects/infrastructure.yaml, bootstrap/projects/workloads.yaml), replace:
       `'https://github.com/OWNER/pincer-ops.git'`
       with:
       `'https://github.com/PatrykQuantumNomad/pincer-ops.git'`

    3. In scripts/bootstrap.sh, update the 4 fallback warning messages that reference "placeholder URL?" to say "repo unreachable" instead. Specifically, change all instances of:
       `log_warn "ArgoCD cannot sync from repo (placeholder URL?) -- applying ...`
       to:
       `log_warn "ArgoCD cannot sync from repo (repo unreachable?) -- applying ...`

    Do NOT use sed for all-at-once replacement. Read each file, make the targeted replacement, and write it back. This ensures no accidental changes to other content.

    Do NOT change infra-envoy-gateway.yaml -- it uses OCI Helm source (docker.io/envoyproxy), not the Git repoURL.
  </action>
  <verify>
    1. Run: `grep -r "OWNER/pincer-ops" bootstrap/ scripts/` -- must return NO results
    2. Run: `grep -r "PatrykQuantumNomad/pincer-ops" bootstrap/` -- must return exactly 9 matches (7 Application files + 2 AppProject files)
    3. Run: `grep "placeholder URL" scripts/bootstrap.sh` -- must return NO results
    4. Run: `grep "repo unreachable" scripts/bootstrap.sh` -- must return 4 matches (MetalLB, Sealed Secrets, cert-manager, OpenClaw fallbacks)
  </verify>
  <done>All 9 manifest files reference https://github.com/PatrykQuantumNomad/pincer-ops.git. No placeholder repoURLs remain. Bootstrap.sh fallback messages updated.</done>
</task>

<task type="auto">
  <name>Task 2: Commit and push changes to origin/main</name>
  <files>
    bootstrap/root-app.yaml
    bootstrap/argocd-self.yaml
    bootstrap/infra-metallb.yaml
    bootstrap/infra-envoy-gateway-config.yaml
    bootstrap/infra-sealed-secrets.yaml
    bootstrap/infra-cert-manager.yaml
    bootstrap/workload-openclaw.yaml
    bootstrap/projects/infrastructure.yaml
    bootstrap/projects/workloads.yaml
    scripts/bootstrap.sh
  </files>
  <action>
    CRITICAL: Changes must be on origin/main before the teardown/rebuild cycle in Plan 02. ArgoCD fetches from the remote repo, not the local working directory.

    1. Stage all 10 modified files (9 manifests + bootstrap.sh):
       `git add bootstrap/root-app.yaml bootstrap/argocd-self.yaml bootstrap/infra-metallb.yaml bootstrap/infra-envoy-gateway-config.yaml bootstrap/infra-sealed-secrets.yaml bootstrap/infra-cert-manager.yaml bootstrap/workload-openclaw.yaml bootstrap/projects/infrastructure.yaml bootstrap/projects/workloads.yaml scripts/bootstrap.sh`

    2. Commit with message: `fix(08-01): replace placeholder repoURL with real GitHub URL`

    3. Push to origin/main: `git push origin main`

    4. Verify the push landed: `git log origin/main --oneline -1` should show the commit just made.

    NOTE: If git push fails due to authentication, inform the user they need to push manually before Plan 02 can run.
  </action>
  <verify>
    1. Run: `git status` -- working tree should be clean (no unstaged changes to the 10 files)
    2. Run: `git log origin/main --oneline -1` -- should show the repoURL fix commit
    3. Run: `git diff origin/main..HEAD` -- should be empty (local and remote are in sync)
  </verify>
  <done>All repoURL changes are committed and pushed to origin/main. ArgoCD can now clone from the real GitHub URL during the rebuild cycle.</done>
</task>

</tasks>

<verification>
1. `grep -rn "OWNER/pincer-ops" bootstrap/ scripts/` returns zero results
2. `grep -rn "PatrykQuantumNomad/pincer-ops" bootstrap/` returns exactly 9 matches
3. `git log origin/main --oneline -1` shows the fix commit
4. `git diff origin/main..HEAD` is empty
</verification>

<success_criteria>
- All 9 ArgoCD manifest files reference the real GitHub repoURL
- bootstrap.sh fallback messages no longer reference "placeholder URL"
- Changes are committed and pushed to origin/main
- The persistent blocker "Placeholder repoURL causes ComparisonError" is resolved
</success_criteria>

<output>
After completion, create `.planning/phases/08-reproducibility-verification/08-01-SUMMARY.md`
</output>
