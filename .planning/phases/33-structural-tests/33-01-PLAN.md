---
phase: 33-structural-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/openshell-manifests.bats
autonomous: true

must_haves:
  truths:
    - "BATS tests validate policy ConfigMap contains Landlock filesystem_policy, landlock compatibility, process identity, and network_policies sections"
    - "BATS tests validate registration Job uses PostSync hook, mounts openshell-sandbox-policy ConfigMap, mounts openshell-client-tls Secret, and runs openshell policy set"
    - "BATS tests validate sandbox pod spec has supervisor as entrypoint (PID 1), Unconfined seccomp, tls-client volume with defaultMode 256, and all OPENSHELL_* env vars"
    - "Three existing broken tests are fixed in-place to match Phase 32 manifest changes"
    - "`make test` passes with zero failures"
  artifacts:
    - path: "tests/unit/openshell-manifests.bats"
      provides: "All structural tests for phases 30-32 manifests"
      contains: "Policy ConfigMap"
  key_links:
    - from: "tests/unit/openshell-manifests.bats"
      to: "workloads/openclaw-sandbox/base/policy-configmap.yaml"
      via: "grep assertions against static YAML"
      pattern: "policy-configmap.yaml"
    - from: "tests/unit/openshell-manifests.bats"
      to: "workloads/openclaw-sandbox/base/registration-job.yaml"
      via: "grep assertions against static YAML"
      pattern: "registration-job.yaml"
    - from: "tests/unit/openshell-manifests.bats"
      to: "workloads/openclaw-sandbox/base/sandbox.yaml"
      via: "grep assertions against static YAML"
      pattern: "sandbox.yaml"
---

<objective>
Fix 3 broken existing tests and add ~60 new BATS structural tests covering all manifests created or modified in Phases 30-32 (policy ConfigMap, registration Job, supervisor-enabled sandbox).

Purpose: Satisfy VERT-04 -- structural test coverage for the complete v2.1 manifest set. Catches regressions before runtime verification (Phase 34).
Output: Updated `tests/unit/openshell-manifests.bats` with all tests passing via `make test`.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-structural-tests/33-RESEARCH.md

# Manifest files being tested (read these to know exact field names and values):
@workloads/openclaw-sandbox/base/policy-configmap.yaml
@workloads/openclaw-sandbox/base/registration-job.yaml
@workloads/openclaw-sandbox/base/sandbox.yaml
@workloads/openclaw-sandbox/base/httproute.yaml

# Existing test file (append to this, fix broken tests in-place):
@tests/unit/openshell-manifests.bats
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 3 broken existing tests</name>
  <files>tests/unit/openshell-manifests.bats</files>
  <action>
Fix 3 existing tests that fail because Phase 32 changed manifest values. Use the Edit tool for each fix.

**Fix 1 — Line ~849, test "sandbox HTTPRoute targets openclaw-sandbox service":**
The httproute.yaml backendRefs uses `name: openclaw-gateway` (not `openclaw-sandbox`). Change the test name and grep pattern:
- Test name: `"sandbox HTTPRoute targets openclaw-gateway service"`
- Grep: `name: openclaw-gateway` in httproute.yaml

**Fix 2 — Line ~1178, test "sandbox CR has RuntimeDefault seccomp profile":**
sandbox.yaml now uses `type: Unconfined` (supervisor installs own BPF filter). Change:
- Test name: `"sandbox CR has Unconfined seccomp profile"`
- Grep: `type: Unconfined` in sandbox.yaml

**Fix 3 — Line ~1188, test "sandbox CR has OPENSHELL_GRPC_ENDPOINT env var":**
sandbox.yaml now uses `OPENSHELL_ENDPOINT` (not `OPENSHELL_GRPC_ENDPOINT`). Change:
- Test name: `"sandbox CR has OPENSHELL_ENDPOINT env var"`
- Grep: `OPENSHELL_ENDPOINT` in sandbox.yaml

IMPORTANT: Preserve exact indentation (2 spaces for run/assert lines). Do not change surrounding tests.
  </action>
  <verify>Run `grep -n 'RuntimeDefault\|OPENSHELL_GRPC_ENDPOINT\|openclaw-sandbox.*httproute' tests/unit/openshell-manifests.bats` -- should return zero matches (all old patterns replaced).</verify>
  <done>All 3 broken tests updated to match current manifest values. No stale assertions remain.</done>
</task>

<task type="auto">
  <name>Task 2: Add structural tests for policy ConfigMap, registration Job, and Phase 32 sandbox changes</name>
  <files>tests/unit/openshell-manifests.bats</files>
  <action>
Append new test sections to the END of `tests/unit/openshell-manifests.bats` (after the last existing test at line ~1273). Follow the established section-header pattern with requirement IDs. Use grep-based assertions matching exact field names and values from the manifest files.

All tests use `${PROJECT_ROOT}/workloads/openclaw-sandbox/base/<file>` paths, `run grep`, and `assert_success` or `assert_failure`.

**Section A: Policy ConfigMap (POL-01, VERT-04) — ~22 tests**

Test the policy-configmap.yaml file. Cover:
1. apiVersion is v1
2. kind is ConfigMap
3. name is openshell-sandbox-policy
4. namespace is openshell
5. data key is policy.yaml
6. version: 1 in the policy data
7. filesystem_policy section exists
8. include_workdir: false
9. read_only list contains /usr
10. read_only list contains /etc
11. read_only list contains /proc
12. read_only list contains /opt/openshell/bin
13. read_write list contains /home/node/.openclaw
14. read_write list contains /tmp
15. read_write list contains /dev/null
16. landlock section exists
17. landlock compatibility: best_effort
18. process section exists
19. process run_as_user: "1000"
20. process run_as_group: "1000"
21. network_policies section exists
22. network_policies endpoint host is openshell.openshell.svc.cluster.local
23. network_policies endpoint port: 8080
24. network_policies enforcement: enforce
25. network_policies binary path /opt/openshell/bin/openshell-sandbox
26. No seccomp field in policy data (assert_failure on `grep 'seccomp' policy-configmap.yaml`)

**Section B: Registration Job (POL-02 through POL-05, VERT-04) — ~20 tests**

Test the registration-job.yaml file. Cover:
1. apiVersion is batch/v1
2. kind is Job
3. name is openclaw-sandbox-policy-registration
4. namespace is openshell
5. PostSync hook annotation present
6. BeforeHookCreation delete-policy annotation present
7. backoffLimit: 3
8. activeDeadlineSeconds: 120
9. restartPolicy: OnFailure
10. automountServiceAccountToken: false
11. Pod seccompProfile type: RuntimeDefault
12. runAsNonRoot: true
13. init container named install-cli exists
14. init container image is curlimages/curl:8.12.1
15. CLI download URL contains openshell
16. Main container named register-policy exists
17. Volume cli-bin is emptyDir
18. Volume policy references openshell-sandbox-policy ConfigMap
19. Volume tls-client references openshell-client-tls Secret
20. Command includes `openshell policy set openclaw-sandbox`
21. Command includes `--policy /policy/policy.yaml`
22. CLI scaffold creates ~/.config/openshell/gateways directory
23. Container mounts /tls for mTLS certs (readOnly: true on tls-client mount)
24. Container has resource limits

**Section C: Sandbox Phase 32 Additions (SUPV-01 through SUPV-05, VERT-04) — ~18 tests**

Test sandbox.yaml for Phase 32-specific additions (do NOT duplicate tests already in the file). Cover:
1. Supervisor binary is the container command: `/opt/openshell/bin/openshell-sandbox`
2. Pod-level seccomp is Unconfined (already fixed in Task 1, but verify section header)
3. SYS_PTRACE capability added
4. SYSLOG capability added
5. OPENSHELL_SANDBOX_COMMAND env var with node command
6. OPENSHELL_ENDPOINT env var (already fixed in Task 1, verify new section)
7. OPENSHELL_SANDBOX_ID env var value is openclaw-sandbox
8. OPENSHELL_SANDBOX env var value is openclaw-sandbox
9. OPENSHELL_LOG_LEVEL env var value is info
10. OPENSHELL_TLS_CA env var points to /etc/openshell-tls/client/ca.crt
11. OPENSHELL_TLS_CERT env var points to /etc/openshell-tls/client/tls.crt
12. OPENSHELL_TLS_KEY env var points to /etc/openshell-tls/client/tls.key
13. tls-client volume exists in volumes section
14. tls-client secret is openshell-client-tls
15. tls-client defaultMode is 256
16. tls-client volumeMount path is /etc/openshell-tls/client
17. tls-client volumeMount is readOnly
18. Container runAsUser is 0 (required for supervisor capabilities)

IMPORTANT: Every test name must be globally unique across the file. Do not duplicate any existing test name. Use descriptive prefixes like "policy ConfigMap has..." or "registration Job has..." or "sandbox supervisor has..." to disambiguate.

Each test follows this exact pattern (2-space indent inside @test block):
```bash
@test "descriptive unique name" {
  run grep 'pattern' \
    "${PROJECT_ROOT}/path/to/file.yaml"
  assert_success
}
```

For multi-line patterns where grep of a single line is ambiguous, use a more specific pattern or `grep -c` for count assertions.
  </action>
  <verify>Run `make test` -- all tests pass (exit 0). Run `grep -c '@test' tests/unit/openshell-manifests.bats` -- count should be approximately 246 (186 existing + ~60 new).</verify>
  <done>All new structural tests appended, covering policy ConfigMap, registration Job, and Phase 32 sandbox changes. `make test` passes with zero failures.</done>
</task>

</tasks>

<verification>
1. `make test` exits 0 with all BATS tests passing
2. `grep -c '@test' tests/unit/openshell-manifests.bats` returns >= 240 (186 existing + ~60 new)
3. The 3 previously broken tests now pass (no `RuntimeDefault`, `OPENSHELL_GRPC_ENDPOINT`, or `openclaw-sandbox.*httproute` patterns remain)
4. New tests cover all three manifest files: policy-configmap.yaml, registration-job.yaml, sandbox.yaml
</verification>

<success_criteria>
- `make test` passes with zero failures
- New test count is at least 240 total @test entries
- Policy ConfigMap tests cover Landlock, process, and network_policies sections
- Registration Job tests cover PostSync hook, mTLS secret mount, and policy set command
- Sandbox tests cover supervisor entrypoint, Unconfined seccomp, tls-client volume with defaultMode 256, and all OPENSHELL_* env vars
</success_criteria>

<output>
After completion, create `.planning/phases/33-structural-tests/33-01-SUMMARY.md`
</output>
