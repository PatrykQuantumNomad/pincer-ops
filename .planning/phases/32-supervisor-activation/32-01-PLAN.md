---
phase: 32-supervisor-activation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workloads/openclaw-sandbox/base/sandbox.yaml
autonomous: true
requirements: [SUPV-01, SUPV-02, SUPV-03, SUPV-04, SUPV-05]

must_haves:
  truths:
    - "Supervisor binary is the container entrypoint (PID 1) -- command is /opt/openshell/bin/openshell-sandbox"
    - "OpenClaw node command is passed via OPENSHELL_SANDBOX_COMMAND env var, not as direct container command"
    - "Supervisor can authenticate to gateway via mTLS -- OPENSHELL_TLS_* env vars and tls-client volume are configured"
    - "Supervisor has root privileges and required Linux capabilities for netns/Landlock/seccomp setup"
    - "Pod-level seccomp is Unconfined so supervisor can install its own BPF filter in child process"
    - "Supervisor binary hostPath volume is mounted from DaemonSet-delivered /opt/openshell/bin"
    - "Landlock filesystem restrictions will be enforced -- supervisor fetches policy (via OPENSHELL_ENDPOINT + mTLS) containing filesystem_policy with Landlock rules, and applies them in pre_exec"
    - "Privacy router handles inference.local -- openclaw.json configures baseUrl as https://inference.local/v1, and the supervisor's built-in proxy intercepts this via TLS termination in the network namespace"
  artifacts:
    - path: "workloads/openclaw-sandbox/base/sandbox.yaml"
      provides: "Complete supervisor-enabled Sandbox CR pod template"
      contains: "/opt/openshell/bin/openshell-sandbox"
    - path: "workloads/openclaw-sandbox/base/configmap.yaml"
      provides: "OpenClaw config with inference.local/v1 baseUrl (pre-existing, not modified)"
      contains: "inference.local/v1"
  key_links:
    - from: "workloads/openclaw-sandbox/base/sandbox.yaml"
      to: "infrastructure/openshell/supervisor/daemonset.yaml"
      via: "hostPath volume /opt/openshell/bin"
      pattern: "hostPath.*path.*opt/openshell/bin"
    - from: "workloads/openclaw-sandbox/base/sandbox.yaml"
      to: "infrastructure/openshell/gateway/certificate-client.yaml"
      via: "openshell-client-tls Secret volume"
      pattern: "secretName.*openshell-client-tls"
    - from: "workloads/openclaw-sandbox/base/sandbox.yaml"
      to: "OpenShell gateway gRPC endpoint"
      via: "OPENSHELL_ENDPOINT env var"
      pattern: "OPENSHELL_ENDPOINT.*openshell\\.openshell\\.svc"
    - from: "workloads/openclaw-sandbox/base/configmap.yaml"
      to: "Supervisor privacy router (inference.local TLS interception)"
      via: "openclaw.json baseUrl https://inference.local/v1"
      pattern: "inference\\.local/v1"
---

<objective>
Re-enable the OpenShell supervisor as PID 1 in the sandbox pod by updating the Sandbox CR pod template with supervisor binary as entrypoint, all required OPENSHELL_* environment variables, mTLS certificate volumes, Linux capabilities (SYS_ADMIN, NET_ADMIN, SYS_PTRACE, SYSLOG), and correct security context.

Purpose: The supervisor was bypassed in v2.0 because the gateway could not deliver policies to controller-created sandboxes. Phase 31 solved this with the registration Job. Now the supervisor can be activated to enforce Landlock filesystem restrictions, seccomp-BPF syscall filtering, network namespace isolation, and privacy routing.

Output: Updated sandbox.yaml with supervisor as PID 1, all isolation features enabled, ready for runtime verification in Phase 34.
</objective>

<execution_context>
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-supervisor-activation/32-RESEARCH.md
@.planning/phases/30-policy-definition/30-01-SUMMARY.md
@.planning/phases/31-registration-bridge/31-01-SUMMARY.md

@workloads/openclaw-sandbox/base/sandbox.yaml
@workloads/openclaw-sandbox/base/configmap.yaml
@workloads/openclaw-sandbox/base/networkpolicy.yaml
@workloads/openclaw-sandbox/base/kustomization.yaml

<interfaces>
<!-- Current sandbox.yaml has supervisor config commented out with clear markers. -->
<!-- Research Example 1 provides the complete target container spec. -->
<!-- Key references the executor needs: -->

From infrastructure/openshell/supervisor/daemonset.yaml:
- DaemonSet copies supervisor binary to /opt/openshell/bin on each node via hostPath
- The binary path is /opt/openshell/bin/openshell-sandbox

From infrastructure/openshell/gateway/certificate-client.yaml:
- cert-manager Certificate creates openshell-client-tls Secret in openshell namespace
- Contains ca.crt, tls.crt, tls.key

From workloads/openclaw-sandbox/base/configmap.yaml:
- openclaw.json has baseUrl "https://inference.local/v1" for the "openshell" provider
- This is the privacy router endpoint -- the supervisor's built-in proxy intercepts inference.local via TLS termination inside the network namespace

From workloads/openclaw-sandbox/base/sandbox.yaml current state:
- Lines 113-118: Commented-out OPENSHELL_ENDPOINT and OPENSHELL_SANDBOX_ID env vars
- Lines 126-135: Commented-out supervisor-bin, tls-client, tls-ca volumeMounts
- Lines 171-184: Commented-out supervisor-bin, tls-client, tls-ca volumes
- Container command (lines 92-98): Currently node dist/index.js gateway --bind lan --port 18789
- Security context (lines 84-90): Currently runAsUser 1000, capabilities drop ALL, readOnlyRootFilesystem true
- Pod-level seccompProfile (line 29): Currently RuntimeDefault
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update sandbox.yaml to enable supervisor as PID 1 with full isolation stack</name>
  <files>workloads/openclaw-sandbox/base/sandbox.yaml</files>
  <action>
Modify the Sandbox CR pod template to activate the supervisor binary as PID 1. Make ALL of the following changes:

**1. Pod-level security context (spec.podTemplate.spec.securityContext):**
- Change `seccompProfile.type` from `RuntimeDefault` to `Unconfined` (SUPV-03: supervisor applies its own BPF filter via pre_exec; pod-level RuntimeDefault would block it)
- Keep `fsGroup: 1000` (PVC files owned by uid 1000, child process runs as uid 1000)

**2. Container command (containers[0].command):**
- Replace the multi-line node command with a single entry: `/opt/openshell/bin/openshell-sandbox`
- Remove the old `node`, `dist/index.js`, `gateway`, `--bind`, `lan`, `--port`, `18789` args entirely

**3. Container security context (containers[0].securityContext):**
- Change `runAsUser` from `1000` to `0` (supervisor needs root for unshare(CLONE_NEWNET), Landlock, veth creation)
- Remove `runAsGroup: 1000` (supervisor manages group identity for child process via policy)
- Change `allowPrivilegeEscalation` from `false` to `true` (supervisor needs prctl + capability usage for fork/exec sandboxing)
- Change `capabilities` from `drop: ["ALL"]` to `drop: ["ALL"]` + `add: ["SYS_ADMIN", "NET_ADMIN", "SYS_PTRACE", "SYSLOG"]` (SUPV-04: SYS_ADMIN+NET_ADMIN for netns; SYS_PTRACE for process management; SYSLOG for audit)
- Remove `readOnlyRootFilesystem: true` (supervisor writes ephemeral CA certs to /etc/openshell-tls/ for proxy TLS termination)

**4. Container env vars (containers[0].env):**
Keep the existing OPENCLAW_GATEWAY_TOKEN, NODE_ENV, and HOME env vars. Add the following new env vars (replacing the commented-out ones):

```yaml
- name: OPENSHELL_SANDBOX_COMMAND
  value: "node dist/index.js gateway --bind lan --port 18789"
- name: OPENSHELL_ENDPOINT
  value: "https://openshell.openshell.svc.cluster.local:8080"
- name: OPENSHELL_SANDBOX_ID
  value: "openclaw-sandbox"
- name: OPENSHELL_SANDBOX
  value: "openclaw-sandbox"
- name: OPENSHELL_LOG_LEVEL
  value: "info"
- name: OPENSHELL_TLS_CA
  value: "/etc/openshell-tls/client/ca.crt"
- name: OPENSHELL_TLS_CERT
  value: "/etc/openshell-tls/client/tls.crt"
- name: OPENSHELL_TLS_KEY
  value: "/etc/openshell-tls/client/tls.key"
```

Remove the old commented-out OPENSHELL_ENDPOINT and OPENSHELL_SANDBOX_ID lines and their surrounding comment block.

**5. Container volumeMounts (containers[0].volumeMounts):**
Add (replacing the commented-out ones):

```yaml
- name: supervisor-bin
  mountPath: /opt/openshell/bin
  readOnly: true
- name: tls-client
  mountPath: /etc/openshell-tls/client
  readOnly: true
```

Remove the old commented-out supervisor-bin, tls-client, and tls-ca volumeMount lines and their surrounding comment block. Note: tls-ca is NOT needed as a separate mount -- the ca.crt is included in the openshell-client-tls Secret alongside tls.crt and tls.key.

**6. Volumes (spec.podTemplate.spec.volumes):**
Add (replacing the commented-out ones):

```yaml
- name: supervisor-bin
  hostPath:
    path: /opt/openshell/bin
    type: Directory
- name: tls-client
  secret:
    secretName: openshell-client-tls
    defaultMode: 256
```

Note: `defaultMode: 256` is `0o400` in octal -- restricts cert file permissions. Remove the old commented-out supervisor-bin, tls-client, and tls-ca volume definitions and their surrounding comment block. The separate tls-ca volume is NOT needed -- all certs (ca.crt, tls.crt, tls.key) come from the single openshell-client-tls Secret.

**7. Header comment:**
The existing header comment (lines 1-9) already describes the supervisor architecture correctly. No changes needed.

**What NOT to change:**
- initContainers (seed-config) -- unchanged, still runs as root with CHOWN to seed openclaw.json
- ports (18789 gateway) -- unchanged, OpenClaw still listens on this port as child process
- resources (requests/limits) -- unchanged
- startupProbe/livenessProbe/readinessProbe -- unchanged, /health on port 18789 still works through supervisor (150s startup timeout is sufficient per research)
- volumeClaimTemplates (20Gi PVC) -- unchanged
- The data, tmp, cache, and config volumes and their mounts -- keep as-is
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/pincer-ops && RENDERED=$(kustomize build workloads/openclaw-sandbox/base/) && echo "$RENDERED" | grep -q 'openshell-sandbox' && echo "$RENDERED" | grep -q 'OPENSHELL_SANDBOX_COMMAND' && echo "$RENDERED" | grep -q 'SYS_ADMIN' && echo "$RENDERED" | grep -q 'runAsUser: 0' && echo "$RENDERED" | grep -q 'Unconfined' && echo "$RENDERED" | grep -q 'OPENSHELL_ENDPOINT' && echo "$RENDERED" | grep -q 'OPENSHELL_TLS_CERT' && echo "$RENDERED" | grep -q 'openshell-client-tls' && echo "$RENDERED" | grep -q 'hostPath' && kustomize build workloads/openclaw-sandbox/overlays/dev/ > /dev/null 2>&1 && make validate 2>&1 | tail -5 && grep -q 'inference.local/v1' workloads/openclaw-sandbox/base/configmap.yaml && echo "ALL CHECKS PASS" || echo "FAIL"</automated>
  </verify>
  <done>sandbox.yaml has supervisor as PID 1 (/opt/openshell/bin/openshell-sandbox), all 8 OPENSHELL_* env vars, capabilities [SYS_ADMIN, NET_ADMIN, SYS_PTRACE, SYSLOG], runAsUser 0, seccompProfile Unconfined, supervisor-bin hostPath volume, tls-client Secret volume, no commented-out supervisor blocks remaining. Privacy router prerequisites confirmed: configmap.yaml has inference.local/v1 baseUrl (SUPV-05). kustomize build succeeds for base and dev overlay, make validate passes with 0 errors.</done>
</task>

</tasks>

<verification>
1. `kustomize build workloads/openclaw-sandbox/base/` renders valid YAML with supervisor as container command
2. `kustomize build workloads/openclaw-sandbox/overlays/dev/` renders valid YAML (overlay compatibility)
3. `make validate` passes with 0 kubeconform errors
4. No commented-out supervisor or mTLS blocks remain in sandbox.yaml
5. The existing NetworkPolicy does NOT need changes -- the supervisor proxy operates at pod level (outer netns), egress to gateway:8080 and HTTPS:443 are already allowed
6. `configmap.yaml` contains `inference.local/v1` as the OpenClaw model provider baseUrl -- this is the privacy router endpoint that the supervisor's built-in proxy intercepts (SUPV-05)
</verification>

<success_criteria>
- sandbox.yaml container command is `/opt/openshell/bin/openshell-sandbox` (SUPV-01)
- OPENSHELL_SANDBOX_COMMAND env var carries the OpenClaw node command with --bind lan (SUPV-01)
- OPENSHELL_ENDPOINT and OPENSHELL_SANDBOX_ID env vars enable gRPC policy fetch containing Landlock filesystem_policy rules (SUPV-02)
- OPENSHELL_TLS_* env vars and tls-client volume enable mTLS gateway auth for policy fetch (SUPV-02)
- runAsUser: 0 with SYS_ADMIN, NET_ADMIN, SYS_PTRACE, SYSLOG capabilities (SUPV-04)
- seccompProfile: Unconfined at pod level (SUPV-03)
- supervisor-bin hostPath volume mounted at /opt/openshell/bin (SUPV-01)
- Privacy router prerequisites in place: configmap.yaml has inference.local/v1 baseUrl, supervisor binary contains built-in proxy that intercepts inference.local via TLS termination in netns, mTLS volumes enable route refresh from gateway (SUPV-05)
- All manifests pass kustomize build and kubeconform validation
</success_criteria>

<output>
After completion, create `.planning/phases/32-supervisor-activation/32-01-SUMMARY.md`
</output>
