---
phase: 20-security-hardening
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified: []
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-04

must_haves:
  truths:
    - "Rendered OpenClaw StatefulSet YAML contains all SEC-01 security fields in correct locations"
    - "Rendered OpenClaw StatefulSet YAML contains all SEC-02 security fields on both containers"
    - "ArgoCD Application manifests contain SEC-04 PSS labels in managedNamespaceMetadata"
    - "LiteLLM deployment already satisfies SEC-02 (no changes needed)"
    - "All manifests pass kubeconform schema validation"
  artifacts: []
  key_links:
    - from: "workloads/openclaw/base/statefulset.yaml"
      to: "workloads/openclaw/overlays/dev/kustomization.yaml"
      via: "kustomize build renders base through overlay"
      pattern: "readOnlyRootFilesystem: true"
    - from: "workloads/litellm/base/deployment.yaml"
      to: "SEC-02 compliance"
      via: "already has seccompProfile and capabilities.drop from Phase 19"
      pattern: "type: RuntimeDefault"
---

<objective>
Verify the complete security posture across both namespaces and pods by inspecting rendered manifests for SEC-01, SEC-02, and SEC-04 compliance.

Purpose: Confirm that Plan 01 changes produce correct rendered output and that LiteLLM already satisfies SEC-02, completing the phase verification gate.
Output: Verification report confirming all three requirements are met.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-security-hardening/20-RESEARCH.md
@.planning/phases/20-security-hardening/20-01-SUMMARY.md
@workloads/openclaw/base/statefulset.yaml
@workloads/litellm/base/deployment.yaml
@bootstrap/kinder/workload-openclaw.yaml
@bootstrap/kind/workload-openclaw.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify SEC-01, SEC-02, and SEC-04 across all manifests</name>
  <files></files>
  <action>
Run a comprehensive verification of all three security requirements. This task produces NO file changes -- it is purely verification.

**SEC-01 Verification (OpenClaw readOnlyRootFilesystem + writable mounts):**
1. Render the OpenClaw kustomize overlay: `kubectl kustomize workloads/openclaw/overlays/dev`
2. Verify readOnlyRootFilesystem: true appears exactly twice (once per container)
3. Verify emptyDir volumes "tmp" and "cache" exist with sizeLimit: 100Mi
4. Verify initContainer has volumeMount for /tmp
5. Verify main container has volumeMounts for /tmp and /home/node/.cache
6. Verify fsGroup: 1000 is set at pod level

**SEC-02 Verification (seccomp + capabilities on OpenClaw AND LiteLLM):**
1. From the rendered OpenClaw overlay, verify seccompProfile.type: RuntimeDefault at pod level
2. Verify capabilities.drop: ["ALL"] on BOTH OpenClaw containers (init + main)
3. Render the LiteLLM kustomize overlay: `kubectl kustomize workloads/litellm/overlays/dev`
4. Verify LiteLLM already has seccompProfile.type: RuntimeDefault and capabilities.drop: ALL
5. Verify both workloads have allowPrivilegeEscalation: false on all containers

**SEC-04 Verification (PSS labels on openclaw namespace):**
1. Verify bootstrap/kind/workload-openclaw.yaml has managedNamespaceMetadata with:
   - pod-security.kubernetes.io/audit: restricted
   - pod-security.kubernetes.io/audit-version: latest
   - pod-security.kubernetes.io/warn: restricted
   - pod-security.kubernetes.io/warn-version: latest
2. Verify bootstrap/kinder/workload-openclaw.yaml is byte-identical to kind/ version
3. Verify CreateNamespace=true still present (required for managedNamespaceMetadata to work)
4. Verify NO enforce label is set (initContainer runs as root)

**Cross-cutting:**
1. Run `make validate` to confirm all manifests pass kubeconform
2. Run `make test` to confirm no existing tests are broken by the changes

Report each check as PASS/FAIL. If ANY check fails, describe the specific failure and what needs fixing.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/pincer-ops && make validate && make test</automated>
  </verify>
  <done>
- All SEC-01 checks pass: readOnlyRootFilesystem on both containers, emptyDir mounts for /tmp and /home/node/.cache, fsGroup: 1000
- All SEC-02 checks pass: seccompProfile.type: RuntimeDefault and capabilities.drop: ["ALL"] on OpenClaw (both containers) and LiteLLM
- All SEC-04 checks pass: managedNamespaceMetadata with PSS audit+warn labels on both provider files, no enforce label, CreateNamespace=true present
- `make validate` passes
- `make test` passes (no regressions)
  </done>
</task>

</tasks>

<verification>
Final phase gate:
```bash
make check   # validate + test
```

Manual verification checklist (for executor to confirm in SUMMARY):
- [ ] SEC-01: OpenClaw StatefulSet has readOnlyRootFilesystem: true on both containers with /tmp and /home/node/.cache writable mounts
- [ ] SEC-02: OpenClaw has seccompProfile + capabilities.drop on both containers; LiteLLM already had them from Phase 19
- [ ] SEC-04: openclaw namespace will get PSS audit+warn labels via managedNamespaceMetadata (not enforce)
- [ ] No existing tests broken
- [ ] LiteLLM readOnlyRootFilesystem intentionally left as false (not feasible, per research)
</verification>

<success_criteria>
1. `make check` passes (validate + test, zero errors)
2. Every SEC-01, SEC-02, SEC-04 verification check reported as PASS
3. No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/20-security-hardening/20-02-SUMMARY.md`
</output>
