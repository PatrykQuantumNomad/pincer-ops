---
phase: 37-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/bootstrap.bats
  - scripts/setup-repo.sh
  - tests/unit/setup-repo.bats
  - tests/integration/setup-repo.bats
autonomous: true
requirements:
  - VAL-01
  - VAL-02

must_haves:
  truths:
    - "`make validate` passes (kubeconform validates all manifests)"
    - "`make test` passes (all 116+ BATS tests green)"
    - "bootstrap.bats file counts match actual bootstrap directory contents"
    - "shared files identity check includes workload-openclaw.yaml"
    - "setup-repo.sh REPO_FILES contains no phantom references to deleted files"
    - "setup-repo test temp projects match real repo file structure"
  artifacts:
    - path: "tests/unit/bootstrap.bats"
      provides: "Updated file count and file list assertions for v3.0 bootstrap directories"
      contains: "workload-openclaw.yaml"
    - path: "scripts/setup-repo.sh"
      provides: "REPO_FILES array matching actual repo file inventory"
    - path: "tests/unit/setup-repo.bats"
      provides: "create_temp_project() matching real repo structure"
    - path: "tests/integration/setup-repo.bats"
      provides: "Integration test file creation matching real repo structure"
  key_links:
    - from: "tests/unit/bootstrap.bats"
      to: "bootstrap/kind/ and bootstrap/kinder/"
      via: "find + expected_files arrays"
      pattern: "actual_count.*-eq (11|8)"
    - from: "scripts/setup-repo.sh"
      to: "bootstrap/{kind,kinder}/"
      via: "REPO_FILES array"
      pattern: "REPO_FILES="
    - from: "tests/unit/setup-repo.bats"
      to: "scripts/setup-repo.sh"
      via: "create_temp_project mirrors REPO_FILES"
      pattern: "create_temp_project"
---

<objective>
Update all BATS test assertions and script file references to match the v3.0 bootstrap directory structure after OpenShell removal (Phase 35) and OpenClaw restoration (Phase 36).

Purpose: The 2 failing bootstrap.bats tests and phantom workloads.yaml references in setup-repo.sh/tests are the only blockers to `make test` passing. `make validate` already passes.
Output: All BATS tests green, setup-repo.sh REPO_FILES clean, test infrastructure matching reality.
</objective>

<execution_context>
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-validation/37-RESEARCH.md

Current file inventory (verified 2026-03-22):
- bootstrap/kind/: 11 YAML files (root-app, argocd-self, argocd-cm, argocd-rbac-cm, argocd-notifications-cm, infra-metallb, infra-envoy-gateway, infra-envoy-gateway-config, infra-cert-manager, infra-sealed-secrets, workload-openclaw) + projects/infrastructure.yaml
- bootstrap/kinder/: 8 YAML files (root-app, argocd-self, argocd-cm, argocd-rbac-cm, argocd-notifications-cm, infra-envoy-gateway-config, infra-sealed-secrets, workload-openclaw) + projects/infrastructure.yaml
- Shared (byte-identical): argocd-cm.yaml, argocd-rbac-cm.yaml, argocd-notifications-cm.yaml, infra-envoy-gateway-config.yaml, infra-sealed-secrets.yaml, workload-openclaw.yaml, projects/infrastructure.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix bootstrap.bats file counts, expected_files, and shared_files</name>
  <files>tests/unit/bootstrap.bats</files>
  <action>
Update tests/unit/bootstrap.bats with these exact changes:

1. **Test "kind bootstrap directory contains all v1.0 Applications" (line 127):**
   - Rename test to "kind bootstrap directory contains all Applications" (remove "v1.0" -- misleading per Research pitfall 5)
   - Add `workload-openclaw.yaml` to the `expected_files` array (after infra-sealed-secrets.yaml)
   - Change count assertion from `[ "${actual_count}" -eq 10 ]` to `[ "${actual_count}" -eq 11 ]`

2. **Test "kinder bootstrap directory contains shared Applications" (line 163):**
   - Add `workload-openclaw.yaml` to the `expected_files` array (after infra-sealed-secrets.yaml)
   - Change count assertion from `[ "${actual_count}" -eq 7 ]` to `[ "${actual_count}" -eq 8 ]`

3. **Test "shared files are identical across provider directories" (line 190):**
   - Add `workload-openclaw.yaml` to the `shared_files` array (after infra-sealed-secrets.yaml, before projects/infrastructure.yaml)

No other tests in this file need changes. The other 14 tests are already passing.
  </action>
  <verify>
    <automated>bats tests/unit/bootstrap.bats</automated>
  </verify>
  <done>All 17 tests in bootstrap.bats pass. Tests 7, 10, and 12 reflect v3.0 file inventory (kind=11, kinder=8, shared includes workload-openclaw.yaml).</done>
</task>

<task type="auto">
  <name>Task 2: Remove phantom workloads.yaml from setup-repo.sh and its tests</name>
  <files>scripts/setup-repo.sh, tests/unit/setup-repo.bats, tests/integration/setup-repo.bats</files>
  <action>
Three files need phantom `workloads.yaml` references removed:

**A. scripts/setup-repo.sh (lines 39-58):**
Remove these two lines from the REPO_FILES array:
- `"${BOOTSTRAP_DIR}/kind/projects/workloads.yaml"` (line 49)
- `"${BOOTSTRAP_DIR}/kinder/projects/workloads.yaml"` (line 57)

The resulting REPO_FILES array should have 14 entries (was 16):
  - KIND: root-app, argocd-self, workload-openclaw, infra-metallb, infra-envoy-gateway-config, infra-sealed-secrets, infra-cert-manager, projects/infrastructure (8 entries)
  - Kinder: root-app, argocd-self, workload-openclaw, infra-envoy-gateway-config, infra-sealed-secrets, projects/infrastructure (6 entries)

**B. tests/unit/setup-repo.bats -- create_temp_project() (lines 21-76):**
In the create_temp_project() function:
- Line 45: Change `for f in infrastructure.yaml workloads.yaml; do` to `for f in infrastructure.yaml; do` (KIND projects loop)
- Line 67: Change `for f in infrastructure.yaml workloads.yaml; do` to `for f in infrastructure.yaml; do` (Kinder projects loop)

Also update the "file count" test (line 358-385):
- The test removes most files keeping root-app.yaml, argocd-self.yaml, and projects/workloads.yaml per dir. After removing workloads.yaml from create_temp_project(), projects/workloads.yaml won't be created, so only root-app.yaml and argocd-self.yaml remain (2 per dir = 4 total).
- Update comment from "3 per dir = 6 total remaining" to "2 per dir = 4 total remaining"
- Change assertion from `assert_output --partial "Updated 6 file(s)"` to `assert_output --partial "Updated 4 file(s)"`

**C. tests/integration/setup-repo.bats -- setup() function (lines 14-104):**
- Line 62: Change `for f in infrastructure.yaml workloads.yaml; do` to `for f in infrastructure.yaml; do` (KIND projects loop)
- Line 88: Change `for f in infrastructure.yaml workloads.yaml; do` to `for f in infrastructure.yaml; do` (Kinder projects loop)
  </action>
  <verify>
    <automated>bats tests/unit/setup-repo.bats && bats tests/integration/setup-repo.bats</automated>
  </verify>
  <done>All 15 unit setup-repo tests and all 5 integration setup-repo tests pass. No references to workloads.yaml remain in setup-repo.sh, create_temp_project(), or integration setup().</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full test suite and manifest validation:

```bash
# Full BATS test suite (all unit + integration tests)
./scripts/run-tests.sh all

# Manifest validation
./scripts/validate-manifests.sh
```

Both commands must exit 0 with no failures.
</verification>

<success_criteria>
- `bats tests/unit/bootstrap.bats` passes all 17 tests (0 failures)
- `bats tests/unit/setup-repo.bats` passes all 15 tests (0 failures)
- `bats tests/integration/setup-repo.bats` passes all 5 tests (0 failures)
- `./scripts/run-tests.sh all` passes with 0 failures
- `./scripts/validate-manifests.sh` exits 0
- No references to `projects/workloads.yaml` in setup-repo.sh or test helpers
- `workload-openclaw.yaml` appears in bootstrap.bats expected_files (both providers) and shared_files
</success_criteria>

<output>
After completion, create `.planning/phases/37-validation/37-01-SUMMARY.md`
</output>
