---
phase: 22-validation-and-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/validate-manifests.bats
autonomous: true
requirements:
  - CI-01

must_haves:
  truths:
    - "validate-manifests.bats asserts PASS label for litellm/dev"
    - "validate-manifests.bats asserts PASS label for nemoclaw/dev"
    - "bats tests/unit/validate-manifests.bats passes with all assertions green"
  artifacts:
    - path: "tests/unit/validate-manifests.bats"
      provides: "PASS label assertions for NemoClaw validation targets"
      contains: "PASS: litellm/dev"
  key_links:
    - from: "tests/unit/validate-manifests.bats"
      to: "scripts/validate-manifests.sh"
      via: "mock-based execution asserting output labels"
      pattern: "PASS: litellm/dev|PASS: nemoclaw/dev"
---

<objective>
Extend the existing validate-manifests.bats unit test to assert that kubeconform validation covers NemoClaw and LiteLLM manifests by checking for their PASS labels in the script output.

Purpose: CI-01 requires that `make validate` runs kubeconform against NemoClaw infrastructure manifests. The validation script already validates litellm/dev (line 91) and nemoclaw/dev (line 102), but the unit test does not assert these PASS labels. This plan closes that gap.
Output: Updated tests/unit/validate-manifests.bats with PASS label assertions for litellm/dev and nemoclaw/dev.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/unit/validate-manifests.bats
@tests/test_helper.bash
@scripts/validate-manifests.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NemoClaw PASS label assertions to validate-manifests.bats</name>
  <files>tests/unit/validate-manifests.bats</files>
  <action>
Update the existing "all validations pass -> exit 0 with summary" test (line 125-135) to add two new PASS label assertions:

1. Add `assert_output --partial "PASS: litellm/dev"` after the existing `PASS: openclaw/dev` assertion (line 132).
2. Add `assert_output --partial "PASS: nemoclaw/dev"` after the litellm assertion.

The final test should assert all 7 labels:
- PASS: bootstrap/kind
- PASS: bootstrap/kinder
- PASS: openclaw/dev
- PASS: litellm/dev (NEW)
- PASS: nemoclaw/dev (NEW)
- PASS: envoy-gateway
- All validations passed

Also update the "validate_kustomize: FAIL when pipeline fails" test (line 82-90) to verify the failure is specific to openclaw/dev and does NOT affect litellm/dev or nemoclaw/dev. The _mock_kubeconform_fail_piped mock fails ALL piped calls, so litellm/dev and nemoclaw/dev will also FAIL. Update this test to also assert `FAIL: litellm/dev` and `FAIL: nemoclaw/dev` so the test reflects the actual behavior of the mock (raw calls pass, all piped calls fail).

Similarly update the "kubectl kustomize failure propagates as FAIL" test (line 111-119) -- the kubectl mock fails ALL kustomize calls, so litellm/dev and nemoclaw/dev also fail. Add `FAIL: litellm/dev` and `FAIL: nemoclaw/dev` assertions.

Do NOT change any mock helpers or test infrastructure. Only add assert_output lines to existing tests.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/pincer-ops && bats tests/unit/validate-manifests.bats</automated>
  </verify>
  <done>All 5 tests in validate-manifests.bats pass. The "all validations pass" test asserts PASS: litellm/dev and PASS: nemoclaw/dev. The failure tests assert the corresponding FAIL labels.</done>
</task>

</tasks>

<verification>
Run the full test suite to confirm no regressions:
```bash
bats tests/unit/validate-manifests.bats
make test
```
</verification>

<success_criteria>
- `bats tests/unit/validate-manifests.bats` exits 0 with all tests passing
- The "all validations pass" test explicitly asserts PASS: litellm/dev and PASS: nemoclaw/dev
- `make test` (full suite) still passes
</success_criteria>

<output>
After completion, create `.planning/phases/22-validation-and-testing/22-01-SUMMARY.md`
</output>
