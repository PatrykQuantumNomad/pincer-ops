---
phase: 22-validation-and-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/nemoclaw-manifests.bats
autonomous: true
requirements:
  - CI-02
  - CI-03

must_haves:
  truths:
    - "BATS tests verify LiteLLM Deployment has correct API version, resource limits, and probes"
    - "BATS tests verify LiteLLM Service exposes port 4000 as ClusterIP"
    - "BATS tests verify LiteLLM ConfigMap has model_list with NVIDIA, OpenAI, and Anthropic providers"
    - "BATS tests verify LiteLLM NetworkPolicy allows ingress from openclaw on port 4000 and egress on 443"
    - "BATS tests verify OpenClaw NetworkPolicy allows egress to litellm-proxy in nemoclaw on port 4000"
    - "BATS tests verify OpenClaw StatefulSet has no LLM API key environment variables"
    - "BATS tests verify NemoClaw namespace has PSS enforce: restricted label"
  artifacts:
    - path: "tests/unit/nemoclaw-manifests.bats"
      provides: "Structural tests for LiteLLM manifests and OpenClaw network isolation"
      min_lines: 100
  key_links:
    - from: "tests/unit/nemoclaw-manifests.bats"
      to: "workloads/litellm/base/deployment.yaml"
      via: "grep-based structural assertions"
      pattern: "grep.*deployment\\.yaml"
    - from: "tests/unit/nemoclaw-manifests.bats"
      to: "workloads/openclaw/base/networkpolicy.yaml"
      via: "grep-based egress rule assertions"
      pattern: "grep.*networkpolicy\\.yaml"
    - from: "tests/unit/nemoclaw-manifests.bats"
      to: "workloads/openclaw/base/statefulset.yaml"
      via: "grep-based credential isolation assertions"
      pattern: "grep.*statefulset\\.yaml"
---

<objective>
Create a new BATS test file that structurally verifies all LiteLLM manifests (Deployment, Service, ConfigMap, NetworkPolicy), NemoClaw infrastructure (namespace PSS, default-deny), and OpenClaw NetworkPolicy egress rules including credential isolation.

Purpose: CI-02 and CI-03 require BATS tests proving LiteLLM manifest correctness and OpenClaw network isolation. These structural tests catch regressions when manifests are modified -- no running cluster needed.
Output: New tests/unit/nemoclaw-manifests.bats covering all CI-02 and CI-03 requirements.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/test_helper.bash
@tests/unit/bootstrap.bats

<interfaces>
<!-- Manifest files being tested. Executor needs these paths and their contents. -->

workloads/litellm/base/deployment.yaml:
  - apiVersion: apps/v1, kind: Deployment, name: litellm-proxy, namespace: nemoclaw
  - resources.requests (cpu: 100m, memory: 256Mi), resources.limits (cpu: 500m, memory: 512Mi)
  - livenessProbe, readinessProbe, startupProbe present
  - securityContext: runAsNonRoot: true, seccompProfile.type: RuntimeDefault, capabilities.drop: ["ALL"]
  - env: NVIDIA_API_KEY, OPENAI_API_KEY, ANTHROPIC_API_KEY (from secretKeyRef litellm-api-keys)

workloads/litellm/base/service.yaml:
  - apiVersion: v1, kind: Service, name: litellm-proxy, namespace: nemoclaw
  - type: ClusterIP, port: 4000

workloads/litellm/base/configmap.yaml:
  - apiVersion: v1, kind: ConfigMap, name: litellm-config, namespace: nemoclaw
  - data.config.yaml contains model_list with nvidia-nim, openai, anthropic entries

workloads/litellm/base/networkpolicy.yaml:
  - apiVersion: networking.k8s.io/v1, kind: NetworkPolicy, name: litellm-proxy-allow, namespace: nemoclaw
  - ingress from openclaw namespace on port 4000
  - egress to kube-system:53 (DNS), egress to 0.0.0.0/0:443 (HTTPS)

infrastructure/nemoclaw/base/namespace.yaml:
  - apiVersion: v1, kind: Namespace, name: nemoclaw
  - label pod-security.kubernetes.io/enforce: restricted

infrastructure/nemoclaw/base/networkpolicy.yaml:
  - default-deny-all, podSelector: {}, policyTypes: Ingress + Egress

workloads/openclaw/base/networkpolicy.yaml (multi-document):
  - Document 1: default-deny-all (podSelector: {})
  - Document 2: openclaw-allow with egress to:
    - kube-system:53 (DNS)
    - nemoclaw/litellm-proxy:4000 (governance proxy)
    - 0.0.0.0/0:443 (HTTPS for messaging, credential isolation comment)
  - 3 total "- to:" blocks in egress

workloads/openclaw/base/statefulset.yaml:
  - env vars: OPENCLAW_GATEWAY_TOKEN, NODE_ENV, HOME
  - NO NVIDIA_API_KEY, OPENAI_API_KEY, or ANTHROPIC_API_KEY
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create nemoclaw-manifests.bats with LiteLLM structural tests (CI-02)</name>
  <files>tests/unit/nemoclaw-manifests.bats</files>
  <action>
Create `tests/unit/nemoclaw-manifests.bats` following the established project pattern (see bootstrap.bats for reference). The file structure:

1. Shebang: `#!/usr/bin/env bats`
2. Comment: purpose of the file
3. `load '../test_helper'` (NO setup/teardown needed -- no mocks, just grep against static files)

Section: NemoClaw Infrastructure
- "nemoclaw namespace has PSS enforce restricted label": grep `pod-security.kubernetes.io/enforce: restricted` in `infrastructure/nemoclaw/base/namespace.yaml`, assert_success
- "nemoclaw namespace has default-deny-all NetworkPolicy": grep `name: default-deny-all` in `infrastructure/nemoclaw/base/networkpolicy.yaml`, assert_success
- "nemoclaw default-deny-all blocks all ingress and egress": grep `Ingress` and `Egress` in the policyTypes of `infrastructure/nemoclaw/base/networkpolicy.yaml`, assert_success for both

Section: LiteLLM Deployment
- "LiteLLM Deployment uses apps/v1 API version": grep `apiVersion: apps/v1` in deployment.yaml
- "LiteLLM Deployment has resource requests": grep `requests:` in deployment.yaml, then grep `cpu:` and `memory:` with `-A5` context from requests
- "LiteLLM Deployment has resource limits": grep `limits:` in deployment.yaml, then grep `cpu:` and `memory:` with `-A5` context from limits
- "LiteLLM Deployment has livenessProbe": grep `livenessProbe:` in deployment.yaml
- "LiteLLM Deployment has readinessProbe": grep `readinessProbe:` in deployment.yaml
- "LiteLLM Deployment has securityContext with runAsNonRoot": grep `runAsNonRoot: true` in deployment.yaml
- "LiteLLM Deployment has seccompProfile RuntimeDefault": grep `type: RuntimeDefault` in deployment.yaml
- "LiteLLM Deployment drops all capabilities": grep `drop:` in deployment.yaml, then use `-A1` to assert `ALL` on the next line

Section: LiteLLM Service
- "LiteLLM Service uses v1 API version": grep `apiVersion: v1` in service.yaml
- "LiteLLM Service exposes port 4000": grep `port: 4000` in service.yaml
- "LiteLLM Service is ClusterIP type": grep `type: ClusterIP` in service.yaml

Section: LiteLLM ConfigMap
- "LiteLLM ConfigMap has model_list": grep `model_list:` in configmap.yaml
- "LiteLLM ConfigMap includes NVIDIA NIM provider": grep `nvidia` (case-insensitive or exact `nvidia-nim`) in configmap.yaml
- "LiteLLM ConfigMap includes OpenAI provider": grep `openai` in configmap.yaml
- "LiteLLM ConfigMap includes Anthropic provider": grep `anthropic` in configmap.yaml

Section: LiteLLM NetworkPolicy
- "LiteLLM NetworkPolicy uses networking.k8s.io/v1 API": grep `apiVersion: networking.k8s.io/v1` in litellm networkpolicy.yaml
- "LiteLLM NetworkPolicy allows ingress from openclaw namespace": grep `kubernetes.io/metadata.name: openclaw` in litellm networkpolicy.yaml
- "LiteLLM NetworkPolicy allows ingress on port 4000": grep `port: 4000` in litellm networkpolicy.yaml (appears in both ingress and egress section but presence confirms the port is used)
- "LiteLLM NetworkPolicy allows HTTPS egress on port 443": grep `port: 443` in litellm networkpolicy.yaml

All tests use `${PROJECT_ROOT}` prefix for file paths (set by test_helper.bash). Use `run grep ...` followed by `assert_success` and optionally `assert_output --partial` where output content matters.

IMPORTANT: Do NOT add setup/teardown functions. These tests do not need mocks -- they inspect static YAML files.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/pincer-ops && bats tests/unit/nemoclaw-manifests.bats</automated>
  </verify>
  <done>All LiteLLM structural tests and NemoClaw infrastructure tests pass. Each manifest (Deployment, Service, ConfigMap, NetworkPolicy, Namespace) has at least one test verifying its key properties.</done>
</task>

<task type="auto">
  <name>Task 2: Add OpenClaw NetworkPolicy egress and credential isolation tests (CI-03)</name>
  <files>tests/unit/nemoclaw-manifests.bats</files>
  <action>
Append CI-03 tests to the same `tests/unit/nemoclaw-manifests.bats` file created in Task 1.

Section: OpenClaw NetworkPolicy Egress (CI-03)
- "OpenClaw NetworkPolicy allows egress to litellm-proxy in nemoclaw namespace": grep `kubernetes.io/metadata.name: nemoclaw` in `workloads/openclaw/base/networkpolicy.yaml`, assert_success
- "OpenClaw NetworkPolicy targets litellm-proxy pod for egress": grep `app.kubernetes.io/name: litellm-proxy` in openclaw networkpolicy.yaml, assert_success
- "OpenClaw NetworkPolicy allows egress on port 4000 for LiteLLM proxy": grep `port: 4000` in openclaw networkpolicy.yaml, assert_success
- "OpenClaw NetworkPolicy has exactly 3 egress destinations": use `grep -c '  - to:' "$file"` (note the leading spaces to match YAML indentation) against openclaw networkpolicy.yaml. The 3 destinations are: DNS (kube-system:53), LiteLLM proxy (nemoclaw:4000), HTTPS (0.0.0.0/0:443). Assert output equals "3". This catches any future addition of unrestricted egress rules.

    IMPORTANT for the egress count test: The `- to:` pattern needs leading whitespace to avoid matching YAML comments. Use `grep -c '    - to:' "$file"` with 4-space indent to match the actual YAML structure. Verify the exact indentation by looking at the file -- the egress `- to:` blocks are indented 4 spaces under the `egress:` key.

- "OpenClaw NetworkPolicy documents credential isolation for HTTPS egress": grep `credential isolation` (case-insensitive with `-i`) in openclaw networkpolicy.yaml, assert_success. This verifies the comment explaining the security model is present in the manifest.

Section: OpenClaw Credential Isolation (CI-03)
- "OpenClaw StatefulSet does not have NVIDIA_API_KEY env var": run `grep 'NVIDIA_API_KEY' "${PROJECT_ROOT}/workloads/openclaw/base/statefulset.yaml"`, assert_failure (grep exits 1 when no match)
- "OpenClaw StatefulSet does not have OPENAI_API_KEY env var": run `grep 'OPENAI_API_KEY' "${PROJECT_ROOT}/workloads/openclaw/base/statefulset.yaml"`, assert_failure
- "OpenClaw StatefulSet does not have ANTHROPIC_API_KEY env var": run `grep 'ANTHROPIC_API_KEY' "${PROJECT_ROOT}/workloads/openclaw/base/statefulset.yaml"`, assert_failure

These negative tests prove credential isolation -- LLM API keys exist only in the LiteLLM pod (deployment.yaml has them), not in OpenClaw.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/pincer-ops && bats tests/unit/nemoclaw-manifests.bats</automated>
  </verify>
  <done>All OpenClaw NetworkPolicy egress tests pass: egress to nemoclaw/litellm-proxy:4000 confirmed, exactly 3 egress destinations verified, credential isolation comment present, and no LLM API keys in OpenClaw StatefulSet env.</done>
</task>

</tasks>

<verification>
Run the full test suite to confirm no regressions and all new tests pass:
```bash
bats tests/unit/nemoclaw-manifests.bats
make check
```
</verification>

<success_criteria>
- `bats tests/unit/nemoclaw-manifests.bats` exits 0 with all tests passing
- At least 20 tests in the file covering LiteLLM Deployment, Service, ConfigMap, NetworkPolicy, NemoClaw infrastructure, OpenClaw egress, and credential isolation
- `make check` (validate + full test suite) passes
- No test uses yq, kubectl, or any external tool beyond grep
</success_criteria>

<output>
After completion, create `.planning/phases/22-validation-and-testing/22-02-SUMMARY.md`
</output>
