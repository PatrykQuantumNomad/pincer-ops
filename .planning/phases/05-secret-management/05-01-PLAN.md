---
phase: 05-secret-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bootstrap/infra-sealed-secrets.yaml
  - bootstrap/infra-cert-manager.yaml
  - infrastructure/sealed-secrets/base/kustomization.yaml
  - infrastructure/cert-manager/base/kustomization.yaml
  - infrastructure/cert-manager/base/selfsigned-clusterissuer.yaml
autonomous: true
requirements:
  - SECR-01
  - SECR-04

must_haves:
  truths:
    - "Sealed Secrets ArgoCD Application exists at sync wave -3 targeting kube-system namespace"
    - "cert-manager ArgoCD Application exists at sync wave -2 targeting cert-manager namespace"
    - "Kustomize build of both infrastructure bases succeeds without errors"
    - "Self-signed ClusterIssuer manifest exists for cert-manager verification"
  artifacts:
    - path: "bootstrap/infra-sealed-secrets.yaml"
      provides: "ArgoCD Application for Sealed Secrets controller"
      contains: "sync-wave.*-3"
    - path: "bootstrap/infra-cert-manager.yaml"
      provides: "ArgoCD Application for cert-manager"
      contains: "sync-wave.*-2"
    - path: "infrastructure/sealed-secrets/base/kustomization.yaml"
      provides: "Kustomize remote resource for Sealed Secrets v0.35.0"
      contains: "sealed-secrets"
    - path: "infrastructure/cert-manager/base/kustomization.yaml"
      provides: "Kustomize remote resource for cert-manager v1.19.2"
      contains: "cert-manager"
    - path: "infrastructure/cert-manager/base/selfsigned-clusterissuer.yaml"
      provides: "Self-signed ClusterIssuer for verification"
      contains: "ClusterIssuer"
  key_links:
    - from: "bootstrap/infra-sealed-secrets.yaml"
      to: "infrastructure/sealed-secrets/base"
      via: "spec.source.path"
      pattern: "path: infrastructure/sealed-secrets/base"
    - from: "bootstrap/infra-cert-manager.yaml"
      to: "infrastructure/cert-manager/base"
      via: "spec.source.path"
      pattern: "path: infrastructure/cert-manager/base"
---

<objective>
Create all declarative Kubernetes and ArgoCD manifests for Sealed Secrets and cert-manager infrastructure components.

Purpose: Sealed Secrets encrypts credentials for Git-safe storage (SECR-01). cert-manager provides TLS certificate management (SECR-04). Both are infrastructure prerequisites for OpenClaw deployment in Phase 6.

Output: Two ArgoCD Application YAMLs in bootstrap/ (discoverable by root-app), two kustomize bases in infrastructure/, and a self-signed ClusterIssuer for cert-manager verification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-secret-management/05-RESEARCH.md

# Existing patterns to match
@bootstrap/infra-metallb.yaml
@infrastructure/metallb/base/kustomization.yaml
@bootstrap/projects/infrastructure.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Sealed Secrets ArgoCD Application and kustomize base</name>
  <files>
    bootstrap/infra-sealed-secrets.yaml
    infrastructure/sealed-secrets/base/kustomization.yaml
  </files>
  <action>
Create the Sealed Secrets infrastructure following the established MetalLB pattern exactly.

**bootstrap/infra-sealed-secrets.yaml:**
- ArgoCD Application named `infra-sealed-secrets` in namespace `argocd`
- Sync wave annotation: `-3` (per ROADMAP wave ordering table)
- manifest-generate-paths annotation: `infrastructure/sealed-secrets/base`
- Finalizer: `resources-finalizer.argocd.argoproj.io`
- Project: `infrastructure`
- Source: `repoURL: https://github.com/OWNER/pincer-ops.git`, `targetRevision: main`, `path: infrastructure/sealed-secrets/base`
- Destination: `server: https://kubernetes.default.svc`, `namespace: kube-system` (upstream default -- avoids kubeseal CLI requiring extra flags; see research Pitfall 1)
- syncPolicy: automated with selfHeal and prune
- syncOptions: `ServerSideApply=true` (CRD-heavy component per CLAUDE.md), `CreateNamespace=false` (kube-system already exists)
- ignoreDifferences: CRD caBundle at `/spec/conversion/webhook/clientConfig/caBundle` (matching MetalLB pattern)
- Include a header comment explaining the Application purpose, matching the infra-metallb.yaml comment style

**infrastructure/sealed-secrets/base/kustomization.yaml:**
- apiVersion: `kustomize.config.k8s.io/v1beta1`
- Kind: `Kustomization`
- resources: `https://github.com/bitnami-labs/sealed-secrets/releases/download/v0.35.0/controller.yaml`
- Include a header comment explaining the remote resource, matching MetalLB kustomization comment style
  </action>
  <verify>
Run `kubectl kustomize infrastructure/sealed-secrets/base/` (or `kustomize build`) -- must produce valid YAML output containing the sealed-secrets-controller Deployment and SealedSecret CRD. Check that `bootstrap/infra-sealed-secrets.yaml` is valid YAML with `python3 -c "import yaml; yaml.safe_load(open('bootstrap/infra-sealed-secrets.yaml'))"` or equivalent.
  </verify>
  <done>
bootstrap/infra-sealed-secrets.yaml exists with sync wave -3, destination namespace kube-system, ServerSideApply, and CRD ignoreDifferences. infrastructure/sealed-secrets/base/kustomization.yaml exists referencing Sealed Secrets v0.35.0 controller.yaml. Kustomize build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create cert-manager ArgoCD Application, kustomize base, and self-signed ClusterIssuer</name>
  <files>
    bootstrap/infra-cert-manager.yaml
    infrastructure/cert-manager/base/kustomization.yaml
    infrastructure/cert-manager/base/selfsigned-clusterissuer.yaml
  </files>
  <action>
Create the cert-manager infrastructure following the established MetalLB pattern, with additional care for cert-manager-specific requirements.

**bootstrap/infra-cert-manager.yaml:**
- ArgoCD Application named `infra-cert-manager` in namespace `argocd`
- Sync wave annotation: `-2` (per ROADMAP wave ordering table)
- manifest-generate-paths annotation: `infrastructure/cert-manager/base`
- Finalizer: `resources-finalizer.argocd.argoproj.io`
- Project: `infrastructure`
- Source: `repoURL: https://github.com/OWNER/pincer-ops.git`, `targetRevision: main`, `path: infrastructure/cert-manager/base`
- Destination: `server: https://kubernetes.default.svc`, `namespace: cert-manager` (MUST stay in cert-manager namespace -- kustomize namespace transformation will break internal references; see research Pitfall 3)
- syncPolicy: automated with selfHeal and prune
- syncOptions: `ServerSideApply=true` (CRITICAL -- cert-manager CRDs exceed annotation size limit without SSA; see research Pitfall 4 and CLAUDE.md rule), `CreateNamespace=true`
- ignoreDifferences (three entries per research pattern):
  1. CRD caBundle: group `apiextensions.k8s.io`, kind `CustomResourceDefinition`, jsonPointers `/spec/conversion/webhook/clientConfig/caBundle`
  2. MutatingWebhookConfiguration: group `admissionregistration.k8s.io`, kind `MutatingWebhookConfiguration`, jqPathExpressions `.webhooks[]?.clientConfig.caBundle`
  3. ValidatingWebhookConfiguration: group `admissionregistration.k8s.io`, kind `ValidatingWebhookConfiguration`, jqPathExpressions `.webhooks[]?.clientConfig.caBundle`
- Include a header comment explaining the Application purpose

**infrastructure/cert-manager/base/kustomization.yaml:**
- apiVersion: `kustomize.config.k8s.io/v1beta1`
- Kind: `Kustomization`
- resources:
  - `https://github.com/cert-manager/cert-manager/releases/download/v1.19.2/cert-manager.yaml`
  - `selfsigned-clusterissuer.yaml`
- Do NOT set `namespace:` field (see research Pitfall 3 -- cert-manager hard-codes namespace references internally)
- Include a header comment explaining both resources

**infrastructure/cert-manager/base/selfsigned-clusterissuer.yaml:**
- apiVersion: `cert-manager.io/v1`
- Kind: `ClusterIssuer`
- metadata.name: `selfsigned-issuer`
- spec.selfSigned: `{}`
- Include a header comment explaining this is for verification and dev TLS
  </action>
  <verify>
Run `kubectl kustomize infrastructure/cert-manager/base/` -- must produce valid YAML containing cert-manager Deployment, CRDs, and the selfsigned-issuer ClusterIssuer. Validate `bootstrap/infra-cert-manager.yaml` is valid YAML. Verify that the kustomization.yaml does NOT contain a `namespace:` field.
  </verify>
  <done>
bootstrap/infra-cert-manager.yaml exists with sync wave -2, destination namespace cert-manager, ServerSideApply, CreateNamespace, and three ignoreDifferences entries (CRD + MutatingWebhook + ValidatingWebhook caBundle). infrastructure/cert-manager/base/kustomization.yaml exists referencing cert-manager v1.19.2 and the self-signed ClusterIssuer. Kustomize build succeeds. No namespace transformation in kustomization.yaml.
  </done>
</task>

</tasks>

<verification>
1. Both ArgoCD Applications follow the infra-metallb.yaml pattern: live in bootstrap/, reference infrastructure/{component}/base, have sync wave annotations, ServerSideApply, and caBundle ignoreDifferences
2. Wave ordering is correct: -3 (Sealed Secrets) before -2 (cert-manager) -- Sealed Secrets must be ready before cert-manager in case cert-manager SealedSecrets are created later
3. Kustomize build succeeds for both bases independently
4. cert-manager kustomization has NO namespace transformation
5. Sealed Secrets targets kube-system (upstream default), cert-manager targets cert-manager namespace
</verification>

<success_criteria>
- 5 new files created (2 ArgoCD Applications, 2 kustomizations, 1 ClusterIssuer)
- `kubectl kustomize infrastructure/sealed-secrets/base/` succeeds
- `kubectl kustomize infrastructure/cert-manager/base/` succeeds
- Both ArgoCD Application YAMLs are valid and match established patterns
- Root-app will discover both Applications via bootstrap/ directory scan
</success_criteria>

<output>
After completion, create `.planning/phases/05-secret-management/05-01-SUMMARY.md`
</output>
