---
phase: 24-agent-sandbox-crd-controller
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - tests/unit/openshell-manifests.bats
  - scripts/validate-manifests.sh
autonomous: true
requirements:
  - SAND-01
  - SAND-02
  - SAND-03

must_haves:
  truths:
    - "BATS structural tests verify patch-deployment.yaml contains probes, resources, securityContext"
    - "BATS structural tests verify kustomization.yaml references remote manifest.yaml URL"
    - "BATS structural tests verify ArgoCD Application at sync wave 2"
    - "BATS structural tests verify Lua health check key exists in argocd-cm"
    - "BATS structural tests verify provider parity (byte-identical files)"
    - "validate-manifests.sh skips agent-sandbox base (remote resource) to avoid CI flakes"
    - "Full BATS test suite passes without regressions"
  artifacts:
    - path: "tests/unit/openshell-manifests.bats"
      provides: "BATS structural tests extended with Phase 24 CRD controller tests"
      contains: "patch-deployment.yaml"
    - path: "scripts/validate-manifests.sh"
      provides: "Validation script updated to skip agent-sandbox remote resource base"
      contains: "agent-sandbox"
  key_links:
    - from: "tests/unit/openshell-manifests.bats"
      to: "infrastructure/agent-sandbox/base/patch-deployment.yaml"
      via: "grep assertions on static YAML"
      pattern: "patch-deployment.yaml"
    - from: "tests/unit/openshell-manifests.bats"
      to: "bootstrap/kind/argocd-cm.yaml"
      via: "grep assertions on ConfigMap"
      pattern: "agents.x-k8s.io_Sandbox"
---

<objective>
Extend the existing BATS test suite with structural tests for all Phase 24 artifacts and update validate-manifests.sh to handle the agent-sandbox remote resource base.

Purpose: Ensures Phase 24 manifests comply with repo conventions through static analysis. The validate-manifests.sh update prevents CI flakes from the remote resource fetch (same pattern as metallb, sealed-secrets, cert-manager).
Output: Extended openshell-manifests.bats with ~15 new tests, updated validate-manifests.sh
</objective>

<execution_context>
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-agent-sandbox-crd-controller/24-RESEARCH.md
@.planning/phases/24-agent-sandbox-crd-controller/24-01-SUMMARY.md

@tests/unit/openshell-manifests.bats
@scripts/validate-manifests.sh
@tests/test_helper.bash

<interfaces>
<!-- Existing test patterns from openshell-manifests.bats -->

Test structure uses load '../test_helper', ${PROJECT_ROOT} for file paths, grep assertions:
```bash
@test "openshell namespace has PSS enforce privileged label" {
  run grep 'pod-security.kubernetes.io/enforce: privileged' \
    "${PROJECT_ROOT}/infrastructure/openshell/base/namespace.yaml"
  assert_success
}
```

Provider parity tests use diff:
```bash
@test "infra-openshell.yaml byte-identical across providers" {
  run diff "${PROJECT_ROOT}/bootstrap/kind/infra-openshell.yaml" \
    "${PROJECT_ROOT}/bootstrap/kinder/infra-openshell.yaml"
  assert_success
}
```

validate-manifests.sh skip pattern:
```bash
# metallb: remote resource (github.com/metallb/...) -- SKIP kustomize build
# sealed-secrets: remote resource (github.com/bitnami-labs/...) -- SKIP kustomize build
# cert-manager: remote resource (github.com/cert-manager/...) -- SKIP kustomize build
echo "=== Skipped infrastructure bases with remote resources ==="
echo "  - infrastructure/metallb/base (remote: github.com/metallb/metallb)"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend BATS tests with Phase 24 structural tests</name>
  <files>
    tests/unit/openshell-manifests.bats
  </files>
  <action>
    Append new test sections to the existing tests/unit/openshell-manifests.bats file. Add the tests AFTER the existing "Provider Parity" section. Do NOT modify any existing tests.

    Add the following sections:

    **Section: Agent-Sandbox CRD Controller (SAND-01, SAND-02)**

    Tests for infrastructure/agent-sandbox/base/kustomization.yaml:
    - Test: "agent-sandbox kustomization references remote manifest.yaml" -- grep for `kubernetes-sigs/agent-sandbox` in kustomization.yaml
    - Test: "agent-sandbox kustomization references v0.2.1" -- grep for `v0.2.1` in kustomization.yaml
    - Test: "agent-sandbox kustomization includes deployment patch" -- grep for `patch-deployment.yaml` in kustomization.yaml

    Tests for infrastructure/agent-sandbox/base/patch-deployment.yaml:
    - Test: "agent-sandbox deployment patch has readinessProbe" -- grep for `readinessProbe` in patch-deployment.yaml
    - Test: "agent-sandbox deployment patch has livenessProbe" -- grep for `livenessProbe` in patch-deployment.yaml
    - Test: "agent-sandbox deployment patch has resource requests" -- grep for `requests:` in patch-deployment.yaml
    - Test: "agent-sandbox deployment patch has resource limits" -- grep for `limits:` in patch-deployment.yaml
    - Test: "agent-sandbox deployment patch has securityContext" -- grep for `securityContext` in patch-deployment.yaml
    - Test: "agent-sandbox deployment patch sets runAsNonRoot" -- grep for `runAsNonRoot: true` in patch-deployment.yaml
    - Test: "agent-sandbox deployment patch drops all capabilities" -- grep for `drop:` in patch-deployment.yaml (followed by ALL on next line, but single-line grep for `drop:` is sufficient)
    - Test: "agent-sandbox deployment patch sets imagePullPolicy IfNotPresent" -- grep for `imagePullPolicy: IfNotPresent` in patch-deployment.yaml

    **Section: ArgoCD Application Sync Wave Update (SAND-01)**
    - Test: "infra-agent-sandbox Application at sync wave 2" -- grep for `sync-wave: "2"` in bootstrap/kind/infra-agent-sandbox.yaml (note: this REPLACES the existing test "infra-agent-sandbox Application at sync wave 0" from Phase 23 -- update the existing test in place rather than adding a duplicate)

    **IMPORTANT:** The existing test file from Phase 23 has a test "infra-agent-sandbox Application at sync wave 0". This test must be UPDATED (not duplicated) to check for sync wave 2 instead of 0. Change the grep pattern from `sync-wave: "0"` to `sync-wave: "2"` and update the test name accordingly.

    **Section: Sandbox Lua Health Check (SAND-03)**
    - Test: "argocd-cm has Sandbox Lua health check key" -- grep for `resource.customizations.health.agents.x-k8s.io_Sandbox` in bootstrap/kind/argocd-cm.yaml
    - Test: "Sandbox Lua health check maps Ready condition" -- grep for `condition.type == "Ready"` in bootstrap/kind/argocd-cm.yaml
    - Test: "Sandbox Lua health check returns Healthy on True" -- grep for `hs.status = "Healthy"` in bootstrap/kind/argocd-cm.yaml (note: use single quotes around the grep pattern to avoid shell interpretation issues with quotes inside)

    **Section: Provider Parity (Phase 24 additions)**
    - Test: "argocd-cm.yaml byte-identical across providers" -- diff between kind/ and kinder/ argocd-cm.yaml

    Note: The existing provider parity test for infra-agent-sandbox.yaml already exists from Phase 23 -- no need to add another.

    All tests use the same pattern: `run grep 'pattern' "${PROJECT_ROOT}/path/to/file"` followed by `assert_success`.
  </action>
  <verify>
    <automated>bats tests/unit/openshell-manifests.bats</automated>
  </verify>
  <done>
    - All existing Phase 23 tests pass (no regressions)
    - ~15 new Phase 24 tests pass covering kustomization.yaml, patch-deployment.yaml, sync wave, Lua health check, provider parity
    - The sync wave 0 test from Phase 23 is updated to check for sync wave 2
  </done>
</task>

<task type="auto">
  <name>Task 2: Update validate-manifests.sh to skip agent-sandbox remote resource</name>
  <files>
    scripts/validate-manifests.sh
  </files>
  <action>
    The existing validate-manifests.sh already validates `infrastructure/agent-sandbox/base` with kustomize build (added in Phase 23 when it was local-only). Now that the kustomization.yaml references a remote URL (manifest.yaml from GitHub), this validation will download ~4000 lines on every CI run and may fail due to rate limiting.

    **Update validate-manifests.sh:**

    1. Remove the existing validation entry for agent-sandbox:
       ```
       # agent-sandbox: namespace only
       validate_kustomize "infrastructure/agent-sandbox/base" "agent-sandbox"
       ```

    2. Add agent-sandbox to the skip list, updating the existing "Skipped infrastructure bases with remote resources" echo block:
       ```
       echo "  - infrastructure/agent-sandbox/base (remote: github.com/kubernetes-sigs/agent-sandbox)"
       ```

    This follows the exact pattern used for metallb, sealed-secrets, and cert-manager bases.

    After updating, run `./scripts/validate-manifests.sh` to verify it passes (agent-sandbox should appear in the skip list output, not in the validation section).
  </action>
  <verify>
    <automated>./scripts/validate-manifests.sh && ./scripts/validate-manifests.sh 2>&1 | grep -q 'agent-sandbox' && echo "PASS: validate-manifests.sh updated and passing"</automated>
  </verify>
  <done>
    - agent-sandbox base is no longer validated via kustomize build (skipped due to remote resource)
    - agent-sandbox appears in the "Skipped infrastructure bases with remote resources" output
    - All other validations still pass
    - Full test suite passes: ./scripts/run-tests.sh all
  </done>
</task>

</tasks>

<verification>
1. `bats tests/unit/openshell-manifests.bats` -- all tests pass (Phase 23 + Phase 24)
2. `./scripts/validate-manifests.sh` -- passes, agent-sandbox in skip list
3. `./scripts/run-tests.sh all` -- full suite passes, no regressions
4. No duplicate tests (sync wave test updated, not duplicated)
</verification>

<success_criteria>
- ~15 new BATS structural tests pass covering CRD controller manifests, sync wave, Lua health check, provider parity
- Existing Phase 23 tests pass without regressions (sync wave test updated from 0 to 2)
- validate-manifests.sh skips agent-sandbox base (remote resource pattern)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/24-agent-sandbox-crd-controller/24-02-SUMMARY.md`
</output>
