---
phase: 25-openshell-gateway
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - tests/unit/openshell-manifests.bats
  - scripts/validate-manifests.sh
autonomous: true
requirements: [SAND-04, SAND-05, SAND-06, SAND-07, SAND-08]

must_haves:
  truths:
    - "BATS structural tests validate all 7 gateway resource files"
    - "BATS tests verify RBAC permissions for sandboxes CRUD and nodes/runtimeclasses read"
    - "BATS tests verify StatefulSet image, PVC, TLS-disabled env var, and probes"
    - "BATS tests verify Service ClusterIP:8080 with gRPC appProtocol"
    - "BATS tests verify ArgoCD Application at wave 5 in openshell project"
    - "BATS tests verify provider parity for workload-openshell-gateway.yaml"
    - "validate-manifests.sh includes gateway kustomize validation"
    - "make check passes (validate + test)"
  artifacts:
    - path: "tests/unit/openshell-manifests.bats"
      provides: "Structural tests for SAND-04 through SAND-08"
      contains: "SAND-04"
    - path: "scripts/validate-manifests.sh"
      provides: "Gateway kustomize validation entry"
      contains: "infrastructure/openshell/gateway"
  key_links:
    - from: "tests/unit/openshell-manifests.bats"
      to: "infrastructure/openshell/gateway/statefulset.yaml"
      via: "grep assertions against static YAML"
      pattern: "infrastructure/openshell/gateway"
    - from: "scripts/validate-manifests.sh"
      to: "infrastructure/openshell/gateway"
      via: "validate_kustomize function call"
      pattern: 'validate_kustomize.*infrastructure/openshell/gateway'
---

<objective>
Add BATS structural tests and kubeconform validation for all Phase 25 gateway artifacts.

Purpose: Ensure all gateway manifests are structurally correct and conform to Kubernetes schemas. Tests provide regression protection for SAND-04 through SAND-08 requirements. The validate-manifests.sh update ensures CI catches schema violations.

Output: Extended openshell-manifests.bats with gateway tests + validate-manifests.sh gateway entry
</objective>

<execution_context>
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-openshell-gateway/25-RESEARCH.md
@.planning/phases/25-openshell-gateway/25-01-SUMMARY.md

<interfaces>
<!-- Files created by Plan 01 that this plan tests against -->

From infrastructure/openshell/gateway/statefulset.yaml:
- StatefulSet named "openshell", namespace "openshell"
- Image: ghcr.io/nvidia/openshell/gateway:0.0.12
- Env: OPENSHELL_DISABLE_TLS=true, OPENSHELL_SSH_HANDSHAKE_SECRET
- PVC: openshell-data, 1Gi
- Probes: startupProbe, livenessProbe, readinessProbe (tcpSocket on grpc port)
- Resources: requests + limits
- SecurityContext: runAsNonRoot, runAsUser 1000, drop ALL

From infrastructure/openshell/gateway/service.yaml:
- Service named "openshell", ClusterIP, port 8080, appProtocol: grpc

From infrastructure/openshell/gateway/role.yaml:
- Role named "openshell-sandbox", agents.x-k8s.io sandboxes CRUD

From infrastructure/openshell/gateway/clusterrole.yaml:
- ClusterRole named "openshell-sandbox-runtimeclass", nodes + runtimeclasses read

From infrastructure/openshell/gateway/kustomization.yaml:
- Lists 7 resource files, no namespace: field

From bootstrap/kind/workload-openshell-gateway.yaml:
- ArgoCD Application, sync-wave "5", project openshell, SSA enabled

From tests/unit/openshell-manifests.bats (existing structure):
- Uses load '../test_helper', PROJECT_ROOT variable
- Section headers with === separators and requirement IDs
- grep-based assertions with assert_success
- Provider parity tests use diff
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gateway BATS structural tests</name>
  <files>tests/unit/openshell-manifests.bats</files>
  <action>
    Append new test sections to the existing `tests/unit/openshell-manifests.bats` file.
    Follow the established pattern: section headers with `# ===` separators, requirement ID
    comments, grep-based assertions with `assert_success`, using `${PROJECT_ROOT}` prefix
    for all file paths.

    Add these test sections after the existing "Provider Parity (Phase 24 additions)" section:

    **Section: OpenShell Gateway Manifests (SAND-08)**
    - Test that infrastructure/openshell/gateway/kustomization.yaml exists and lists all 7 resource files
      (serviceaccount.yaml, clusterrole.yaml, clusterrolebinding.yaml, role.yaml, rolebinding.yaml,
      service.yaml, statefulset.yaml)
    - Test that kustomization.yaml does NOT have a `namespace:` field (grep -v, assert_success)

    **Section: Gateway StatefulSet (SAND-04)**
    - Test StatefulSet uses correct image: ghcr.io/nvidia/openshell/gateway:0.0.12
    - Test StatefulSet has imagePullPolicy: IfNotPresent
    - Test StatefulSet has replicas: 1
    - Test StatefulSet has volumeClaimTemplates with openshell-data
    - Test StatefulSet has startupProbe
    - Test StatefulSet has livenessProbe
    - Test StatefulSet has readinessProbe
    - Test StatefulSet has resource requests
    - Test StatefulSet has resource limits

    **Section: Gateway TLS Disabled (SAND-07)**
    - Test StatefulSet env has OPENSHELL_DISABLE_TLS
    - Test StatefulSet env has OPENSHELL_SSH_HANDSHAKE_SECRET

    **Section: Gateway RBAC (SAND-05)**
    - Test Role has agents.x-k8s.io apiGroup (for sandboxes CRUD)
    - Test Role has sandboxes/status subresource
    - Test ClusterRole has runtimeclasses resource
    - Test ClusterRole has nodes resource
    - Test ClusterRoleBinding references ServiceAccount openshell
    - Test RoleBinding exists in openshell namespace

    **Section: Gateway Service (SAND-06)**
    - Test Service is type ClusterIP
    - Test Service has port 8080
    - Test Service has appProtocol: grpc

    **Section: Gateway ArgoCD Application**
    - Test workload-openshell-gateway has sync-wave "5"
    - Test workload-openshell-gateway uses project openshell
    - Test workload-openshell-gateway has ServerSideApply=true
    - Test workload-openshell-gateway source path is infrastructure/openshell/gateway

    **Section: Provider Parity (Phase 25 additions)**
    - Test workload-openshell-gateway.yaml is byte-identical across providers (diff)

    Use descriptive test names following the pattern: "gateway [component] [assertion]".
    Target approximately 20-25 tests total for Phase 25. All tests use grep against static YAML
    files -- no cluster needed.
  </action>
  <verify>
    <automated>bats tests/unit/openshell-manifests.bats</automated>
  </verify>
  <done>
    All new gateway tests pass. Tests cover SAND-04 (StatefulSet image, PVC, probes, resources),
    SAND-05 (Role sandboxes CRUD, ClusterRole nodes/runtimeclasses), SAND-06 (Service ClusterIP:8080),
    SAND-07 (TLS disabled env var), SAND-08 (kustomization lists all files, no namespace field).
    Provider parity test confirms byte-identical ArgoCD Application.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add gateway to validate-manifests.sh</name>
  <files>scripts/validate-manifests.sh</files>
  <action>
    Add a new `validate_kustomize` call for the gateway directory in `scripts/validate-manifests.sh`.
    Insert it immediately after the existing `validate_kustomize "infrastructure/openshell/base" "openshell"`
    line (around line 105).

    Add:
    ```
    # openshell gateway: local resources only (pre-rendered from Helm chart)
    validate_kustomize "infrastructure/openshell/gateway" "openshell-gateway"
    ```

    This is a separate Kustomize root from infrastructure/openshell/base (which manages only
    the namespace). The gateway directory contains only local static YAML files (no remote
    resources), so kustomize build will not download anything.

    Do NOT modify any other lines in the script. Do NOT add the gateway to the "skipped" list
    since it has no remote resources.
  </action>
  <verify>
    <automated>./scripts/validate-manifests.sh 2>&1 | grep -q 'PASS: openshell-gateway' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    validate-manifests.sh includes gateway kustomize validation. Running the script shows
    "PASS: openshell-gateway" in output. `make check` passes (validate + full test suite).
  </done>
</task>

</tasks>

<verification>
- `bats tests/unit/openshell-manifests.bats` -- all tests pass (existing + new gateway tests)
- `./scripts/validate-manifests.sh` -- includes and passes openshell-gateway validation
- `make check` -- full validate + test suite passes
</verification>

<success_criteria>
- 20+ new BATS tests appended to openshell-manifests.bats covering SAND-04 through SAND-08
- All tests pass with `bats tests/unit/openshell-manifests.bats`
- validate-manifests.sh includes `validate_kustomize "infrastructure/openshell/gateway"` entry
- Gateway validation passes in validate-manifests.sh output
- `make check` passes (full suite including all pre-existing tests)
</success_criteria>

<output>
After completion, create `.planning/phases/25-openshell-gateway/25-02-SUMMARY.md`
</output>
