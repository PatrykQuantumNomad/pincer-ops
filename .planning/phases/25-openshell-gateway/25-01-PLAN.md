---
phase: 25-openshell-gateway
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infrastructure/openshell/gateway/kustomization.yaml
  - infrastructure/openshell/gateway/serviceaccount.yaml
  - infrastructure/openshell/gateway/clusterrole.yaml
  - infrastructure/openshell/gateway/clusterrolebinding.yaml
  - infrastructure/openshell/gateway/role.yaml
  - infrastructure/openshell/gateway/rolebinding.yaml
  - infrastructure/openshell/gateway/service.yaml
  - infrastructure/openshell/gateway/statefulset.yaml
  - bootstrap/kind/workload-openshell-gateway.yaml
  - bootstrap/kinder/workload-openshell-gateway.yaml
autonomous: true
requirements: [SAND-04, SAND-05, SAND-06, SAND-07, SAND-08]

must_haves:
  truths:
    - "Gateway manifests exist as pre-rendered static YAML in infrastructure/openshell/gateway/"
    - "ArgoCD Application workload-openshell-gateway exists at sync wave 5 in both providers"
    - "StatefulSet uses image ghcr.io/nvidia/openshell/gateway:0.0.12 with SQLite PVC"
    - "RBAC grants Sandbox CRUD in openshell namespace and node/runtimeclass read cluster-wide"
    - "Service exposes ClusterIP:8080 with gRPC appProtocol"
    - "TLS is disabled via OPENSHELL_DISABLE_TLS env var"
  artifacts:
    - path: "infrastructure/openshell/gateway/statefulset.yaml"
      provides: "Gateway StatefulSet with SQLite PVC, probes, resource limits"
      contains: "ghcr.io/nvidia/openshell/gateway:0.0.12"
    - path: "infrastructure/openshell/gateway/service.yaml"
      provides: "ClusterIP Service on port 8080"
      contains: "appProtocol: grpc"
    - path: "infrastructure/openshell/gateway/role.yaml"
      provides: "Namespace-scoped Sandbox CRUD"
      contains: "agents.x-k8s.io"
    - path: "infrastructure/openshell/gateway/clusterrole.yaml"
      provides: "Cluster-scoped node and runtimeclass read"
      contains: "runtimeclasses"
    - path: "infrastructure/openshell/gateway/kustomization.yaml"
      provides: "Kustomize resource listing for gateway"
      min_lines: 5
    - path: "bootstrap/kind/workload-openshell-gateway.yaml"
      provides: "ArgoCD Application at wave 5"
      contains: "sync-wave"
    - path: "bootstrap/kinder/workload-openshell-gateway.yaml"
      provides: "Provider parity copy"
      contains: "sync-wave"
  key_links:
    - from: "bootstrap/kind/workload-openshell-gateway.yaml"
      to: "infrastructure/openshell/gateway"
      via: "ArgoCD source path"
      pattern: "path: infrastructure/openshell/gateway"
    - from: "infrastructure/openshell/gateway/statefulset.yaml"
      to: "infrastructure/openshell/gateway/serviceaccount.yaml"
      via: "serviceAccountName reference"
      pattern: "serviceAccountName: openshell"
    - from: "bootstrap/kind/workload-openshell-gateway.yaml"
      to: "bootstrap/kind/projects/openshell-project.yaml"
      via: "project reference"
      pattern: "project: openshell"
---

<objective>
Create all OpenShell gateway Kubernetes manifests and ArgoCD Application wiring.

Purpose: Deploy the OpenShell gateway as a StatefulSet with RBAC, Service, and SQLite storage at ArgoCD sync wave 5. The gateway is the control plane API server that coordinates sandbox lifecycle via gRPC/HTTP on port 8080. All manifests are pre-rendered from the upstream NVIDIA/OpenShell Helm chart as static YAML -- no Helm runtime in ArgoCD.

Output: 8 new YAML files in infrastructure/openshell/gateway/ + ArgoCD Application in both bootstrap directories
</objective>

<execution_context>
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-openshell-gateway/25-RESEARCH.md
@.planning/phases/24-agent-sandbox-crd-controller/24-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From bootstrap/kind/projects/openshell-project.yaml:
```yaml
# AppProject already whitelists ClusterRole and ClusterRoleBinding
clusterResourceWhitelist:
  - group: 'rbac.authorization.k8s.io'
    kind: ClusterRole
  - group: 'rbac.authorization.k8s.io'
    kind: ClusterRoleBinding
destinations:
  - namespace: 'openshell'
    server: https://kubernetes.default.svc
```

From infrastructure/openshell/base/kustomization.yaml:
```yaml
# Existing: namespace only. Gateway is a SEPARATE Kustomize root.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
```

From bootstrap/kind/infra-openshell.yaml:
```yaml
# Existing namespace Application at wave 0. Gateway is a NEW Application at wave 5.
annotations:
  argocd.argoproj.io/sync-wave: "0"
spec:
  source:
    path: infrastructure/openshell/base  # NOT infrastructure/openshell/gateway
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pre-rendered gateway manifests</name>
  <files>
    infrastructure/openshell/gateway/kustomization.yaml
    infrastructure/openshell/gateway/serviceaccount.yaml
    infrastructure/openshell/gateway/clusterrole.yaml
    infrastructure/openshell/gateway/clusterrolebinding.yaml
    infrastructure/openshell/gateway/role.yaml
    infrastructure/openshell/gateway/rolebinding.yaml
    infrastructure/openshell/gateway/service.yaml
    infrastructure/openshell/gateway/statefulset.yaml
  </files>
  <action>
    Create the `infrastructure/openshell/gateway/` directory with 8 files. All manifests are
    pre-rendered from the upstream NVIDIA/OpenShell Helm chart (deploy/helm/openshell v0.1.0,
    image tag 0.0.12) with TLS-disabled dev values. Use the exact YAML from the RESEARCH.md
    code examples as the reference, with these specifications:

    **kustomization.yaml** (per SAND-08):
    - apiVersion: kustomize.config.k8s.io/v1beta1, kind: Kustomization
    - List all 7 resource files (serviceaccount, clusterrole, clusterrolebinding, role, rolebinding, service, statefulset)
    - WARNING: Do NOT add a `namespace:` field -- ClusterRole/ClusterRoleBinding are cluster-scoped
    - Add comment header explaining provenance (chart version, image tag)

    **serviceaccount.yaml**:
    - ServiceAccount named `openshell` in namespace `openshell`
    - Labels: app.kubernetes.io/name: openshell, app.kubernetes.io/instance: openshell
    - No Helm labels (no helm.sh/chart, no managed-by: Helm)

    **clusterrole.yaml** (per SAND-05):
    - ClusterRole named `openshell-sandbox-runtimeclass`
    - Rules: node.k8s.io/runtimeclasses (get, list), core/nodes (get, list)
    - Same label pattern, no Helm labels

    **clusterrolebinding.yaml** (per SAND-05):
    - ClusterRoleBinding named `openshell-sandbox-runtimeclass`
    - Binds ClusterRole to ServiceAccount `openshell` in namespace `openshell`
    - No namespace on the ClusterRoleBinding metadata itself

    **role.yaml** (per SAND-05):
    - Role named `openshell-sandbox` in namespace `openshell`
    - Rules: agents.x-k8s.io/sandboxes and sandboxes/status (create, delete, get, list, patch, update, watch)
    - Rules: core/events (get, list, watch)

    **rolebinding.yaml** (per SAND-05):
    - RoleBinding named `openshell-sandbox` in namespace `openshell`
    - Binds Role to ServiceAccount `openshell`

    **service.yaml** (per SAND-06):
    - Service named `openshell` in namespace `openshell`, type ClusterIP
    - Port 8080, targetPort grpc, appProtocol: grpc, protocol: TCP

    **statefulset.yaml** (per SAND-04, SAND-07):
    - StatefulSet named `openshell` in namespace `openshell`, serviceName: openshell, replicas: 1
    - Image: ghcr.io/nvidia/openshell/gateway:0.0.12, imagePullPolicy: IfNotPresent
    - Container args: --port 8080 --log-level info --db-url sqlite:/var/openshell/openshell.db
    - Env vars: OPENSHELL_SANDBOX_NAMESPACE=openshell, OPENSHELL_SANDBOX_IMAGE=ghcr.io/nvidia/openshell-community/sandboxes/base:latest,
      OPENSHELL_GRPC_ENDPOINT=http://openshell.openshell.svc.cluster.local:8080,
      OPENSHELL_SSH_HANDSHAKE_SECRET=dev-placeholder-not-a-real-secret,
      OPENSHELL_DISABLE_TLS=true
    - VolumeClaimTemplate: openshell-data, 1Gi, ReadWriteOnce, mounted at /var/openshell
    - SecurityContext: pod fsGroup 1000, container runAsNonRoot true, runAsUser 1000,
      allowPrivilegeEscalation false, capabilities drop ALL
    - Probes: startupProbe tcpSocket grpc (period 2s, failure 30), livenessProbe tcpSocket grpc
      (initial 2s, period 5s, failure 3), readinessProbe tcpSocket grpc (initial 1s, period 2s, failure 3)
    - Resources: requests cpu 100m memory 256Mi, limits cpu 500m memory 512Mi
    - terminationGracePeriodSeconds: 5

    Each file gets a comment header explaining it was pre-rendered from the upstream Helm chart,
    with chart version 0.1.0 and image tag 0.0.12.

    Do NOT include: helm.sh/chart labels, app.kubernetes.io/managed-by: Helm labels, SPDX headers,
    or any Helm runtime annotations.
  </action>
  <verify>
    <automated>kubectl kustomize infrastructure/openshell/gateway/ | grep -c 'kind:' | grep -q '7' && echo "PASS: 7 resources" || echo "FAIL"</automated>
  </verify>
  <done>
    8 files exist in infrastructure/openshell/gateway/. kubectl kustomize renders 7 resources
    without errors. StatefulSet has correct image, PVC, probes, env vars. RBAC has correct
    permissions. Service is ClusterIP:8080. No Helm labels present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ArgoCD Application for both providers</name>
  <files>
    bootstrap/kind/workload-openshell-gateway.yaml
    bootstrap/kinder/workload-openshell-gateway.yaml
  </files>
  <action>
    Create `bootstrap/kind/workload-openshell-gateway.yaml` as a new ArgoCD Application.
    Use the exact YAML from the RESEARCH.md ArgoCD Application code example:

    - name: workload-openshell-gateway
    - namespace: argocd
    - sync-wave: "5" (after CRD controller at wave 2, before workload-openclaw at wave 10)
    - manifest-generate-paths: infrastructure/openshell/gateway
    - Notification annotations (on-sync-failed, on-health-degraded, on-sync-status-unknown)
    - Finalizer: resources-finalizer.argocd.argoproj.io
    - project: openshell (NOT workloads -- gateway needs cluster-scoped RBAC from openshell AppProject)
    - source path: infrastructure/openshell/gateway
    - source repoURL: https://github.com/PatrykQuantumNomad/pincer-ops.git
    - source targetRevision: main
    - destination namespace: openshell
    - syncPolicy: automated (selfHeal, prune), syncOptions [ServerSideApply=true, CreateNamespace=false]
    - SSA is appropriate because the gateway manages cluster-scoped RBAC resources

    Add the sync wave map comment header showing the updated wave layout including
    "Wave 5: workload-openshell-gateway [this app]".

    Copy the file byte-identical to `bootstrap/kinder/workload-openshell-gateway.yaml`.
    Verify byte identity with diff.
  </action>
  <verify>
    <automated>diff bootstrap/kind/workload-openshell-gateway.yaml bootstrap/kinder/workload-openshell-gateway.yaml && grep -q 'sync-wave: "5"' bootstrap/kind/workload-openshell-gateway.yaml && grep -q 'project: openshell' bootstrap/kind/workload-openshell-gateway.yaml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    ArgoCD Application exists in both bootstrap directories. Byte-identical across providers.
    Sync wave is 5. Project is openshell. Source path points to infrastructure/openshell/gateway.
    SSA enabled. CreateNamespace=false.
  </done>
</task>

</tasks>

<verification>
- `kubectl kustomize infrastructure/openshell/gateway/` renders 7 resources without errors
- `diff bootstrap/kind/workload-openshell-gateway.yaml bootstrap/kinder/workload-openshell-gateway.yaml` exits 0
- `grep -c 'helm.sh/chart' infrastructure/openshell/gateway/*.yaml` returns 0 (no Helm labels)
- `grep 'OPENSHELL_DISABLE_TLS' infrastructure/openshell/gateway/statefulset.yaml` finds the env var
- `grep 'sync-wave: "5"' bootstrap/kind/workload-openshell-gateway.yaml` confirms wave placement
</verification>

<success_criteria>
- 8 files exist in infrastructure/openshell/gateway/ (7 resource YAML + kustomization.yaml)
- kustomize builds without errors, produces exactly 7 Kubernetes resources
- ArgoCD Application in both bootstrap/ directories, byte-identical, at sync wave 5
- No Helm-specific labels in any rendered output
- StatefulSet image is ghcr.io/nvidia/openshell/gateway:0.0.12
- OPENSHELL_DISABLE_TLS=true is set in StatefulSet env
- RBAC covers sandboxes CRUD + nodes/runtimeclasses read
</success_criteria>

<output>
After completion, create `.planning/phases/25-openshell-gateway/25-01-SUMMARY.md`
</output>
