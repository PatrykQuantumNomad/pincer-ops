---
phase: 28-privacy-router-and-network-transition
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - bootstrap/kind/infra-nemoclaw.yaml
  - bootstrap/kinder/infra-nemoclaw.yaml
  - bootstrap/kind/workload-litellm.yaml
  - bootstrap/kinder/workload-litellm.yaml
  - bootstrap/kind/projects/workloads.yaml
  - bootstrap/kinder/projects/workloads.yaml
  - infrastructure/nemoclaw/base/namespace.yaml
  - infrastructure/nemoclaw/base/networkpolicy.yaml
  - infrastructure/nemoclaw/base/kustomization.yaml
  - infrastructure/nemoclaw/overlays/dev/kustomization.yaml
  - workloads/litellm/base/deployment.yaml
  - workloads/litellm/base/service.yaml
  - workloads/litellm/base/configmap.yaml
  - workloads/litellm/base/sealedsecret.yaml
  - workloads/litellm/base/networkpolicy.yaml
  - workloads/litellm/base/kustomization.yaml
  - workloads/litellm/overlays/dev/kustomization.yaml
  - tests/unit/nemoclaw-manifests.bats
  - scripts/validate-manifests.sh
  - tests/unit/bootstrap.bats
  - tests/unit/validate-manifests.bats
autonomous: true
requirements:
  - INFER-03
  - INFER-04

must_haves:
  truths:
    - "No LiteLLM ArgoCD Application files exist in either bootstrap directory"
    - "No nemoclaw namespace Application files exist in either bootstrap directory"
    - "The workloads AppProject is deleted from both bootstrap directories"
    - "The infrastructure/nemoclaw directory tree does not exist"
    - "The workloads/litellm directory tree does not exist"
    - "nemoclaw-manifests.bats test file does not exist"
    - "validate-manifests.sh does not reference litellm or nemoclaw"
    - "All BATS tests pass (make test)"
  artifacts:
    - path: "scripts/validate-manifests.sh"
      provides: "Validation script without litellm/nemoclaw lines"
      min_lines: 90
    - path: "tests/unit/bootstrap.bats"
      provides: "Bootstrap tests with updated file lists and counts"
      contains: "15"
    - path: "tests/unit/validate-manifests.bats"
      provides: "Validation tests without litellm/nemoclaw assertions"
      min_lines: 60
  key_links:
    - from: "bootstrap/{kind,kinder}/"
      to: "ArgoCD root-app auto-discovery"
      via: "Removing Application YAML files removes ArgoCD management"
      pattern: "infra-nemoclaw|workload-litellm"
    - from: "scripts/validate-manifests.sh"
      to: "make validate target"
      via: "Removing litellm/nemoclaw validation lines"
      pattern: "litellm|nemoclaw"
---

<objective>
Remove all LiteLLM Proxy and nemoclaw namespace resources from the repository.

Purpose: With inference routing transitioned to the OpenShell privacy router (Plan 28-01), LiteLLM and the nemoclaw namespace are no longer needed. This plan deletes 18 files (ArgoCD Applications, Kustomize manifests, workload definitions), removes the now-empty workloads AppProject, deletes the nemoclaw test file, and updates all remaining tests and scripts to reflect the cleanup.

Output: Clean repository with no LiteLLM or nemoclaw references. All BATS tests pass.
</objective>

<execution_context>
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/pincer-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-privacy-router-and-network-transition/28-RESEARCH.md
@.planning/phases/28-privacy-router-and-network-transition/28-01-SUMMARY.md

Source files to read before modifying:
@scripts/validate-manifests.sh
@tests/unit/bootstrap.bats
@tests/unit/validate-manifests.bats
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete LiteLLM, nemoclaw, and workloads AppProject files</name>
  <files>
    bootstrap/kind/infra-nemoclaw.yaml,
    bootstrap/kinder/infra-nemoclaw.yaml,
    bootstrap/kind/workload-litellm.yaml,
    bootstrap/kinder/workload-litellm.yaml,
    bootstrap/kind/projects/workloads.yaml,
    bootstrap/kinder/projects/workloads.yaml,
    infrastructure/nemoclaw/base/namespace.yaml,
    infrastructure/nemoclaw/base/networkpolicy.yaml,
    infrastructure/nemoclaw/base/kustomization.yaml,
    infrastructure/nemoclaw/overlays/dev/kustomization.yaml,
    workloads/litellm/base/deployment.yaml,
    workloads/litellm/base/service.yaml,
    workloads/litellm/base/configmap.yaml,
    workloads/litellm/base/sealedsecret.yaml,
    workloads/litellm/base/networkpolicy.yaml,
    workloads/litellm/base/kustomization.yaml,
    workloads/litellm/overlays/dev/kustomization.yaml,
    tests/unit/nemoclaw-manifests.bats
  </files>
  <action>
Delete all LiteLLM, nemoclaw, and related files. Execute these deletions:

**ArgoCD Application files (4 files):**
```bash
rm bootstrap/kind/infra-nemoclaw.yaml
rm bootstrap/kinder/infra-nemoclaw.yaml
rm bootstrap/kind/workload-litellm.yaml
rm bootstrap/kinder/workload-litellm.yaml
```

**workloads AppProject (2 files):**
The `workloads` AppProject has no remaining destinations after nemoclaw removal. All workloads are now under the `openshell` AppProject.
```bash
rm bootstrap/kind/projects/workloads.yaml
rm bootstrap/kinder/projects/workloads.yaml
```

**nemoclaw infrastructure (entire directory tree):**
```bash
rm -r infrastructure/nemoclaw/
```

**LiteLLM workload (entire directory tree):**
```bash
rm -r workloads/litellm/
```

**nemoclaw test file:**
All 30 tests in nemoclaw-manifests.bats reference deleted files. Delete the entire file.
```bash
rm tests/unit/nemoclaw-manifests.bats
```

**Verification after deletion:** Confirm no references remain:
```bash
grep -r 'nemoclaw' bootstrap/ infrastructure/ workloads/ tests/ scripts/ --include='*.yaml' --include='*.sh' --include='*.bats' || echo "Clean"
grep -r 'litellm' bootstrap/ infrastructure/ workloads/ tests/ scripts/ --include='*.yaml' --include='*.sh' --include='*.bats' || echo "Clean"
```

Note: References in `.planning/` and documentation files are expected and not actionable.
  </action>
  <verify>
    <automated>test ! -f bootstrap/kind/infra-nemoclaw.yaml && test ! -f bootstrap/kind/workload-litellm.yaml && test ! -f bootstrap/kind/projects/workloads.yaml && test ! -d infrastructure/nemoclaw && test ! -d workloads/litellm && test ! -f tests/unit/nemoclaw-manifests.bats && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All 18 files and 2 directory trees deleted. workloads AppProject removed. nemoclaw-manifests.bats deleted. No litellm/nemoclaw references remain in bootstrap/, infrastructure/, workloads/, tests/, or scripts/ directories (checked via grep).</done>
</task>

<task type="auto">
  <name>Task 2: Update validate-manifests.sh and BATS test files</name>
  <files>scripts/validate-manifests.sh, tests/unit/bootstrap.bats, tests/unit/validate-manifests.bats</files>
  <action>
**validate-manifests.sh:**

Remove these two lines from the validation script:
```bash
validate_kustomize "workloads/litellm/overlays/dev" "litellm/dev"
```
and:
```bash
validate_kustomize "infrastructure/nemoclaw/overlays/dev" "nemoclaw/dev"
```

The remaining validations are: bootstrap/kind, bootstrap/kinder, openclaw-sandbox/dev, envoy-gateway, openshell, openshell-gateway, openshell-supervisor. Total: 2 raw + 5 kustomize = 7 validation targets (down from 9).

**tests/unit/bootstrap.bats:**

1. **KIND expected_files array** (the test "kind bootstrap directory contains all v1.0 Applications"):
   Remove these entries from the array:
   - `infra-nemoclaw.yaml`
   - `workload-litellm.yaml`
   Update the count assertion from `[ "${actual_count}" -eq 17 ]` to `[ "${actual_count}" -eq 15 ]`.

2. **KIND project files test** (the test "kind bootstrap directory contains all project files"):
   Remove the assertion:
   ```bash
   assert_file_exists "${PROJECT_ROOT}/bootstrap/kind/projects/workloads.yaml"
   ```

3. **Kinder expected_files array** (the test "kinder bootstrap directory contains shared Applications"):
   Remove these entries from the array:
   - `infra-nemoclaw.yaml`
   - `workload-litellm.yaml`
   Update the count assertion from `[ "${actual_count}" -eq 14 ]` to `[ "${actual_count}" -eq 12 ]`.

4. **Kinder project files test** (the test "kinder bootstrap directory contains all project files"):
   Remove the assertion:
   ```bash
   assert_file_exists "${PROJECT_ROOT}/bootstrap/kinder/projects/workloads.yaml"
   ```

5. **Shared files test** (the test "shared files are identical across provider directories"):
   Remove `projects/workloads.yaml` from the `shared_files` array.

**tests/unit/validate-manifests.bats:**

Remove ALL references to `litellm/dev` and `nemoclaw/dev` assertions. Specifically:

1. In test "validate_kustomize: FAIL when pipeline fails":
   Remove: `assert_output --partial "FAIL: litellm/dev"`
   Remove: `assert_output --partial "FAIL: nemoclaw/dev"`

2. In test "kubectl kustomize failure propagates as FAIL":
   Remove: `assert_output --partial "FAIL: litellm/dev"`
   Remove: `assert_output --partial "FAIL: nemoclaw/dev"`

3. In test "all validations pass -> exit 0 with summary":
   Remove: `assert_output --partial "PASS: litellm/dev"`
   Remove: `assert_output --partial "PASS: nemoclaw/dev"`

The `_mock_kubeconform_fail_on_call` test ("EXIT_CODE: first passes, second fails -> exit 1") uses call number 3 to trigger the first kustomize failure. With litellm/nemoclaw removed, call 3 is now the first kustomize overlay (openclaw-sandbox/dev) since calls 1 and 2 are the two raw bootstrap validations. This is correct -- the test still triggers FAIL on the first kustomize target. No change needed for this test.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/pincer-ops && bats tests/unit/bootstrap.bats tests/unit/validate-manifests.bats tests/unit/openshell-manifests.bats</automated>
  </verify>
  <done>validate-manifests.sh has no litellm/nemoclaw validation lines. bootstrap.bats: KIND count=15, Kinder count=12, no workloads.yaml in project files or shared files. validate-manifests.bats has no litellm/nemoclaw assertions. All BATS tests pass.</done>
</task>

</tasks>

<verification>
1. `test ! -d infrastructure/nemoclaw` -- nemoclaw infrastructure tree deleted
2. `test ! -d workloads/litellm` -- litellm workload tree deleted
3. `test ! -f bootstrap/kind/projects/workloads.yaml` -- workloads AppProject deleted
4. `test ! -f tests/unit/nemoclaw-manifests.bats` -- nemoclaw test file deleted
5. `! grep -rq 'litellm' scripts/validate-manifests.sh` -- no litellm in validation script
6. `! grep -rq 'nemoclaw' scripts/validate-manifests.sh` -- no nemoclaw in validation script
7. `bats tests/unit/bootstrap.bats tests/unit/validate-manifests.bats tests/unit/openshell-manifests.bats` -- all tests pass
</verification>

<success_criteria>
- All 18 LiteLLM/nemoclaw files deleted, both directory trees removed
- workloads AppProject deleted from both provider directories
- nemoclaw-manifests.bats test file deleted
- validate-manifests.sh has no litellm or nemoclaw references
- bootstrap.bats file counts updated (KIND: 15, Kinder: 12)
- bootstrap.bats shared_files and project_files arrays updated
- validate-manifests.bats has no litellm or nemoclaw assertions
- `bats tests/unit/` passes all remaining tests
</success_criteria>

<output>
After completion, create `.planning/phases/28-privacy-router-and-network-transition/28-02-SUMMARY.md`
</output>
