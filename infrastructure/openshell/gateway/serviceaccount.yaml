# serviceaccount.yaml -- ServiceAccount for the OpenShell gateway.
#
# Pre-rendered from deploy/helm/openshell v0.1.0, image tag 0.0.12.
# Grants the gateway pod identity for RBAC bindings that allow
# Sandbox CRUD and node/runtimeclass read access.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: openshell
  namespace: openshell
  labels:
    app.kubernetes.io/name: openshell
    app.kubernetes.io/instance: openshell
